# 假设文档：tsmom_dual_engine_v2_riskbudget（引擎 L 风险预算版）

**家族：** TSMOM regime（Baseline）——家族变体，不耗独立计数 ｜ **预登记：** 2026-06-12（Claude）
**动机：** v1 引擎 L 仅败于爆仓概率门（P(年DD≥35%)=29.7%>20%），信号四项全过且基准超额为正——定仓问题非信号问题。

## 单一新变量（其余 100% 继承 v1 引擎 L，含 2024-12-09 硬切断）

**组合级波动率目标缩放：** 每根 4H 开盘，按组合过去 42 根 4H（=7天）已实现波动率年化值 σ_t 计算缩放系数 k_t = min(1, 25%/σ_t)，当期全部仓位名义乘以 k_t（gross ≤1x 不变）。
**冻结值与依据（无搜索）：** 目标 25% 年化——取 BTC 长期年化波动（~60-80%）的约 1/3 与标准档爆仓门的几何增长保留之折中，**预登记一次性给定；若失败禁止换目标值重跑**（那是参数搜索，违反单变量原则）。窗口 7 天=与 4H 周期匹配的最短稳健窗（同样禁调）。
**引擎 S 不重跑**（已 FAILED 入墓园）。

## 检验（预算：1 次全样本 + WF 三段）

四件套（标准档）+ 第五件（基准=同门控 8 币等权买入持有，**不缩放**——基准是"不做任何聪明事"的对照）+ WF 三段 + 分年表 + 与 v1 引擎 L 的逐年对比表（缩放代价多少几何增长、换多少爆仓概率下降）。

## 判定（锁死）

四件套+第五件全过 → PASSED（按样本档级），列扩样本确认候选（动 Holdout 须 Founder 批另案）；任一不过 → FAILED 入墓园（"风险预算版"行），**本家族定仓维度宣告穷尽，禁止第三个定仓变体**（v1全仓/v2缩放已覆盖光谱两端，再试=局部修补搜索风险D）。

## AI 证据三行

①Claude 由 v1 失败模式推导（已有结果驱动，非 LLM 信号）；②最可能错处：vol-targeting 在动量策略上会在趋势爆发期降杠杆错过肥尾（牛市收益集中少数月份），缩放可能砍掉的恰是 E[R] 主要来源；③无多 agent 共识依据。
