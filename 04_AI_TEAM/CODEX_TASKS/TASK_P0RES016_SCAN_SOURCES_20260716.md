# TASK P0-RES-016：投研扫描源拓宽（thesis_hf_scan.py 三事件源扩展）

**任务ID：** P0-RES-016 | **派发：** 2026-07-16（L1审计处方R7） | **执行：** Codex | **验收：** Claude

## 背景与目的（七问审查已由Claude完成）

投研线1个月冲刺（进度2/10-20，剩~22天）最大风险=候选吞吐。现扫描器 `06_RESEARCH/CODE/thesis_hf_scan.py` 只覆盖 Binance funding/OI 挤压复合签名单一机制，REJECTED_LOG 显示大量候选死于"无复合签名"。本任务把候选面从1个机制源扩到4个——全部免费源，机制均有明确付钱方：

1. **上币/退市公告流**：Binance 公告（新上永续=A-4机制族；退市=强制平仓流，付钱方=被迫平仓者；KORU 2026-07-15 崩盘即活体样本）。公告接口/页面公开免费。
2. **公开 upcoming 解锁日历**：小盘代币 cliff 解锁（付钱方=被解锁强制供给冲击的持有者；THESIS_001/002 同族）。只用免费公开源（DropsTab/CryptoRank 公开页、CoinGecko 等），best-effort，抓不到全量不硬凑。
3. **脱锚监控**：主要稳定币/锚定资产偏离锚>2%（付钱方=被迫repeg套利/挤兑方；MIM 2026-07-01 案例同族）。CoinGecko/Binance 现货价免费。

## 交付物

1. `thesis_hf_scan.py` 扩展：新增三个源模块（各自独立小函数，可单独开关），输出**合并候选清单**（JSON+控制台表格），每条带 `source` 标签+原始证据字段+URL留痕。
2. **回归保证**：现有 funding/OI 挤压逻辑不改动——同输入产出与改动前一致（跑一次对照记录在报告里）。
3. `REPORT_P0RES016_SCAN_SOURCES_20260716.md`（改动说明+各源实测样例输出+已知限制诚实标注）。
4. `04_AI_TEAM/TASK_INBOX/P0RES016_DONE.json`。

## 硬护栏（违反=任务作废）

- **扫描器只产候选清单，禁止自动登记 thesis**——登记权留每日巡检/Claude（闸0容量edge+闸1强制流签名仍逐条人裁）。
- 不碰 Holdout；不做量化实验；不花钱（全部免费源，付费墙内容直接标"不可得"）；不改判据/模板。
- 网络：优先 Mac 直连（代理 env 全 unset）；被 geo/SSL 挡时走通道B（SSH SG `root@43.160.200.224`，密钥 `~/.ssh/id_ed25519_aiquant`，只读取数）。预检纪律：HTTP 4xx/5xx=可达。
- 代码=可审计小函数，禁引入黑箱依赖（DEC-061）；某源抓不稳就标注降级，不伪造数据。
- 发现方向性问题（如某源根本无免费可用数据）→ 在报告里提专业异议，不硬实现。
