# 任务书：P0-RES-008 修复价格面板6个刷新失败symbol（含BTCUSDT，SSL错误）

**派发时间：** 2026-07-06　**派发人：** Claude　**执行：** Codex　**验收：** Claude
**性质：** 数据基建修复类任务，不占WIP=1配额（属"被动数据积累/基建修复"层，非"重量化验证线"）。

---

## 背景

`06_RESEARCH/CODE/panel_refresh_2026.py` 于2026-06-22执行过一次价格面板刷新，37个symbol中31个成功刷新至2026-06-22，但以下6个因SSL报错失败：
ALGOUSDT / BTCUSDT / ETCUSDT / FILUSDT / MKRUSDT / OMGUSDT

**BTCUSDT完全失败（0行数据）**——这是核心资产，缺数据会直接拖累forced-flow v2重启后的分档检验（BTCUSDT是清算数据采集器的核心覆盖标的之一）。原始失败记录见 `06_RESEARCH/CODE/output/panel_refresh_2026_audit.json`，诊断报告见 `REPORT_PANEL_REFRESH_2026_20260622.md`（若存在，先读取核实原报告的诊断结论）。

## 任务

1. **诊断根因**：读取 `panel_refresh_2026.py` 和失败记录，确认这6个symbol失败的具体原因（大概率是代理/SSL证书问题，也可能是特定symbol的API响应格式异常——不要预设结论，实际跑一次单symbol请求看报错细节）。
2. **修复**：针对根因修复（如需绕开系统代理直连、调整重试逻辑、处理证书验证等），不要为了"跑通"而静默吞掉错误或用假数据填充。
3. **重新执行刷新**：把这6个symbol的数据补齐到与其余31个symbol一致的最新日期（今天是2026-07-06，尽量刷到最新可得交易日，不要求必须精确到今天，但应显著晚于2026-06-22这个陈旧断点）。
4. **顺带核实**：另外31个已成功symbol是否需要一并增量刷新到同一截止日期（如果`panel_refresh_2026.py`设计上是全量重跑，说明一下增量成本；如果是分symbol独立执行，只需处理这6个失败的，不必重跑全部）。
5. **产出验证**：确认BTCUSDT等6个symbol现在有完整、连续、无缺口的K线数据，行数与其他symbol量级相符（不能只是"能跑通脚本"，要实际核对数据质量）。

## 产出

- 修复后的代码变更（如有）
- 报告文件 `04_AI_TEAM/CODEX_TASKS/REPORT_P0RES008_PANEL_SSL_FIX_20260706.md`，包含：根因诊断、修复方式、修复前后6个symbol的行数/日期范围对比、是否发现新的数据质量问题
- 若修复后仍有symbol无法解决，如实报告失败原因，不得虚报"已修复"

## 禁止事项

- 不得伪造/插值数据填补空缺
- 不得为跑通而放宽SSL验证到不安全的程度（如全局禁用证书验证）而不在报告中明确说明并标注风险
- 不涉及任何信号研究/回测逻辑改动，只做数据层修复
