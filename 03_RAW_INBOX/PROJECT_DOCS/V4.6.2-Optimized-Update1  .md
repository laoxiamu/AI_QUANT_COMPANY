# V4\.6\.2\-Optimized\-Update1  

# V4\.6\.2 智能组合量化交易系统完整方案



**版本**：V4\.6\.2\-Optimized\-Update1  

**最后更新**：2026\-05\-01  

**基于版本**：V4\.6\.2\-Optimized \(2026\-04\-27\)  

**适用市场**：加密货币永续合约（BTC、ETH、SOL及高流动性山寨币）  

**设计哲学**：宏观择时 \+ 微观执行优化 \+ 自适应权重 \+ 路径风险控制 \+ 环境自适应策略路由 \+ 记忆自进化 \+ 数据驱动迭代





## 📁 完整目录



### 第一部分（本次输出）

- **第一章：系统总览与架构蓝图**

    - 1\.1 系统定位与设计哲学（含单向控制流、风险绝对优先、唯一真实数据源、环境自适应策略路由）

    - 1\.2 分层架构全景图（四层：学习层、决策层、策略调度层、执行数据层）

    - 1\.3 数据流向与单向控制铁律

    - 1\.4 核心组件职责速查表（含新增组件）

- **第二章：策略层详解**

    - 2\.1 选币雷达：三层动态候选池 \+ 五因子加权评分

    - 2\.2 市场环境分类与策略路由（含ADX区间细化优化）

    - 2\.3 多空强弱评分（10分制）

    - 2\.4 三周期入场信号（硬过滤 \+ 22分制评分 \+ 环境自适应）

    - 2\.5 微观结构层：HFT\-lite 信号过滤器（含成交方向连续性增强）

    - 2\.6 卖出管理（止损/止盈优先级 \+ 冷却期 \+ 趋势保护止盈）

    - 2\.7 策略状态机

        

### 第二部分

- **第三章：决策与风控层——风控裁决与资金分配**

    - 3\.1 Final Risk Gate（含ADJUSTED状态）

    - 3\.2 资金分配模型（Kelly \+ 环境系数 \+ Risk Parity）

    - 3\.3 决策层输出与执行层交接规范

- **第四章：决策与风控层——动态仓位与熔断体系**

    - 4\.1 动态仓位控制系统（含ATR动态仓位）

    - 4\.2 硬性熔断与破产保护体系

    - 4\.3 动态仓位与熔断协同工作流

        

### 第三部分

- **第五章：执行层——OpenClaw 执行规范与多 Agent 路由**

    - 5\.1 订单管理规范

    - 5\.2 Cron 定时任务自包含规范

    - 5\.3 OpenClaw 多 Agent 路由配置

    - 5\.4 会话管理规范

- **第六章：学习层——Hermes RL 引擎与记忆进化**

    - 6\.1 Hermes RL 权重自适应

    - 6\.2 技能自沉淀机制

    - 6\.3 记忆系统详细设计

    - 6\.4 上下文压缩配置

    - 6\.5 离线 Monte Carlo 引擎

        

### 第四部分

- **第七章：部署手册与运维监控**

    - 7\.1 Docker Compose 完整编排文件

    - 7\.2 冷启动四阶段流程

    - 7\.3 Grafana 双仪表盘监控

    - 7\.4 Token 成本优化策略

    - 7\.5 网络隔离与权限控制

- **第八章：附录——配置参数与接口大全**

    - 8\.1 `system\_config\.json` 完整字段释义

    - 8\.2 核心 API 调用链示例

    - 8\.3 异常检测触发阈值速查表

    - 8\.4 复盘报告标准模板

    - 8\.5 策略铁律（11条）

    - 8\.6 执行口诀

        

### 第五部分（新增）

- **第九章：执行落地计划**

    - 9\.1 总体阶段规划

    - 9\.2 已执行阶段详细清单

    - 9\.3 待完成事项（按优先级）

    - 9\.4 后续计划

    - 9\.5 明确排除事项

    - 9\.6 全程优化调整清单

- **第十章：V4\.6\.2\-Optimized\-Update1 新增内容**

    - 10\.1 版本演进概述

    - 10\.2 回测系统完整数据（含三币种对比、Mode B验证、DX\-1信号alpha验证）

    - 10\.3 两份系统诊断报告核心发现

    - 10\.4 基于诊断结果的修复清单

    - 10\.5 因子驱动验证路线图

    - 10\.6 当前线上参数快照

        

        

# 第一部分：系统总览与策略层



## 第一章：系统总览与架构蓝图



### 1\.1 系统定位与设计哲学



#### 1\.1\.1 系统定位

V4\.6\.2 是一个面向生产环境的、具备自进化能力的智能交易操作系统，不是一个单纯的量化策略脚本。



|定位维度|描述|
|---|---|
|**功能定位**|加密货币永续合约的全自动、多策略、组合式交易系统。|
|**技术定位**|基于 Agent 架构的 Alpha 策略与 Infrastructure 风控解耦的分布式系统。|
|**风险定位**|以生存为首要目标的防御型系统，单日亏损与最大回撤具有不可绕过的硬件级熔断。|
|**进化定位**|具备环境自适应策略路由、数据驱动迭代与因子验证能力，越运行越匹配当前市场状态。|



#### 1\.1\.2 核心设计哲学

系统遵循以下五条铁律：



1. **单向控制流（One\-Way Control Flow）**  

    - 决策权与执行权分离。Hermes（大脑）只能建议，不能下令；OpenClaw（手脚）只能执行，不能决策。  

    - Decision Gateway 是唯一拥有否决权的组件。

        

2. **风险绝对优先（Risk\-First Architecture）**  

    - 任何信号在触碰交易所 API 前，必须经过独立的 Decision Gateway 校验。  

    - 风控规则属于系统层硬编码，禁止任何 Agent 或 RL 模型修改。

        

3. **唯一真实数据源（Single Source of Truth）**  

    - 所有模块不持有私有状态，必须读写同一个 State Center（Redis \+ PostgreSQL）。

        

4. **环境自适应策略路由（Environment\-Adaptive Strategy Routing）**  

    - 系统先判断市场状态（趋势/回调/震荡/极端），再选择对应的子策略。  

    - 避免单一趋势跟踪逻辑在震荡市失效，解决信号评分\&\#34;趋势偏置\&\#34;问题。

        

5. **数据驱动迭代（Data\-Driven Iteration）——Update1 新增**  

    - 所有参数修改必须有回测数据支撑，不拍脑袋。  

    - 评分系统的有效性必须通过统计验证（IC分析、固定持仓回测），而非主观判断。  

    - 当数据证明某个模块失效时（如22分制无预测力），系统具备替换该模块的架构灵活性。

        

        

### 1\.2 分层架构全景图



系统采用严格的四层逻辑架构，层与层之间通过 API 或内部网络通信，严禁跨层调用。



```Plain Text
┌─────────────────────────────────────────────────────────────────┐
│               Layer 4: 学习与进化层                               │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  Hermes Agent（RL权重建议 / 技能沉淀 / 记忆管理 / Monte Carlo）│ │
│  │  ⚠️ 禁止执行交易，仅通过 API 提供建议                         │ │
│  └───────────────────────────┬─────────────────────────────────┘ │
│                              │ GET /v1/weights/suggest            │
└──────────────────────────────┼──────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│               Layer 3: 决策与风控层                               │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  Decision Gateway（信号裁决 / 资金分配 / 熔断触发）           │ │
│  │  ⚠️ 唯一拥有交易否决权的组件                                   │ │
│  └───────────────────────────┬─────────────────────────────────┘ │
│                              │ Approved / ADJUSTED / REJECTED     │
└──────────────────────────────┼──────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│               Layer 2: 策略与调度层                               │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  OpenClaw Core（选币雷达 / 环境分类 / 三周期信号 / 微结构过滤）│ │
│  │  多 Agent 架构：Main / Signal / Risk                         │ │
│  └───────────────────────────┬─────────────────────────────────┘ │
│                              │ Signal Object（已合规）            │
└──────────────────────────────┼──────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│               Layer 1: 执行与数据层                               │
│  ┌──────────────────────┐  ┌──────────────────────────────────┐ │
│  │  Execution Engine    │  │  Market Data Service             │ │
│  │  交易所 API 交互      │  │  WebSocket 订单簿流 / K线聚合     │ │
│  └──────────────────────┘  └──────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  State Center: Redis（实时状态）+ PostgreSQL（持久化数据）    │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```





### 1\.3 数据流向与单向控制铁律



#### 1\.3\.1 标准交易请求流（Happy Path）



```Plain Text
[1] 行情到达 → State Center（Redis 更新价格/OBI）
       ↓
[2] OpenClaw Signal Agent 读取状态 → 运行策略逻辑 → 生成 Signal
       ↓
[3] OpenClaw Main Agent 请求 Hermes 权重 → Hermes 返回建议
       ↓
[4] OpenClaw 组装 Final Signal → 发送至 Decision Gateway
       ↓
[5] Decision Gateway 读取 State Center → 风控校验 → 裁决
       ↓
[6] Gateway 生成 Approved Order → 发送至 Execution Engine
       ↓
[7] Execution Engine 签名发送至交易所 → 更新 State Center
       ↓
[8] 交易平仓后，Hermes 读取结果 → 更新 RL 模型 / 写入记忆
```



#### 1\.3\.2 单向控制强制约束（代码级防呆设计）



|约束项|规则内容|违规后果|
|---|---|---|
|**API 密钥隔离**|Execution Engine 持有交易密钥；Hermes 仅持有只读密钥|Hermes 无法构造有效签名|
|**网络防火墙**|Hermes 容器禁止访问 Execution Engine 容器端口|即使入侵也无法直接下单|
|**代码接口限制**|OpenClaw 的 `execute\_order\(\)` 仅接收带 Gateway 签名的 ApprovedOrder|绕过 Gateway 的请求会被拒绝|





### 1\.4 核心组件职责速查表



|组件 ID|层级|核心职责|关键限制|
|---|---|---|---|
|**Market Data Service**|L1|行情采集、订单簿 WebSocket 推送、消息面爬虫|只读，禁止修改原始数据|
|**Execution Engine**|L1|将合法订单转化为交易所指令，滑点监控|唯一可触碰交易所交易 API|
|**State Center**|L1|Redis（实时状态）\+ PostgreSQL（持久化数据）|禁止模块本地缓存状态|
|**OpenClaw Core**|L2|策略信号生成、多 Agent 路由、订单拆分|禁止自行计算仓位大小|
|**Decision Gateway**|L3|风控裁决、资金分配、熔断触发|唯一拥有否决权|
|**Hermes Agent**|L4|RL 权重学习、技能沉淀、记忆管理、Monte Carlo|禁止直接下单，禁止写入关键字段|
|**FastAPI Proxy**|L4|模型转发（替代 Hermes HTTP API 功能）|仅监听 127\.0\.0\.1，已由 systemd 托管自动重启|





## 第二章：策略层详解



策略层是系统的信号发生器。在 V4\.6\.2 优化版中，策略层实现了**环境自适应**——先判断市场状态，再选择对应的子策略，解决了原版单一趋势跟踪在震荡市失效的问题。在 Update1 中，通过两份系统诊断报告和 DX\-1 固定持仓回测，发现22分制评分系统不具备预测区分力，正在推进因子驱动验证路线。



### 2\.1 选币雷达：三层动态候选池 \+ 五因子加权评分



#### 2\.1\.1 三层动态候选池



|池类型|来源规则|数量上限|设计意图|
|---|---|---|---|
|**Core Pool**|BTCUSDT, ETHUSDT, SOLUSDT（固定）|3|稳定流动性基准，系统永不离线|
|**Momentum Pool**|同时满足：① 成交量 \&gt; 24h均量×1\.5；② ATR环比上升 \&gt;10%；③ 价格突破近20根1h K线区间|3|捕捉刚启动的趋势币|
|**Extreme Pool**|24h 涨跌幅排名前3及后3（流动性合格）|6|捕捉情绪顶/恐慌底|



总候选池每日动态更新，控制在 6\-10 个。



#### 2\.1\.2 流动性硬性过滤

- **必要条件**：24h 成交额 ≥ 50,000,000 USD。

- 未通过过滤的币种直接放入禁做池，不参与后续任何评分。

    

#### 2\.1\.3 五因子加权评分模型（满分 17 分）



|因子|权重|计算方式|说明|
|---|---|---|---|
|**Trend**（趋势因子）|0\.25|\(收盘价/MA20 \- 1\) × 100|衡量价格相对于均线的偏离程度|
|**Volatility**（波动率因子）|0\.20|\(High \- Low\) / Price|评估波幅的适应性，甜点区 0\.3%\-1\.5%|
|**Liquidity**（流动性因子）|0\.20|log\(24h成交额\)|对数压缩后参与横截面排序|
|**RS**（相对强弱因子）|0\.20|币种涨跌幅 \- BTC涨跌幅|衡量独立 Alpha|
|**Micro**（微观因子）|0\.15|买卖价差、深度不平衡|评估盘口质量|



> **已知局限**：`micro` 因子当前实现永远返回 1 分，五因子实际退化为四因子。此问题已在诊断报告中记录，修复优先级排在因子验证之后。
> 
> 



**评级与动作映射**：

- A 级 \(14\-17分\)：主做池，满额风险预算

- B 级 \(11\-13分\)：备选池，轻仓介入

- C 级 \(≤10分\)：禁做池

    

### 2\.2 市场环境分类与策略路由



#### 2\.2\.1 技术环境判定矩阵



|环境名称|量化条件（满足 ≥2 条触发）|对应子策略倾向|
|---|---|---|
|**强趋势日**|① MA20 斜率 \&gt; 0\.1%/K；② ADX\(14\) \&gt; 25；③ BOLL 带宽 \&gt; 过去20期均值×1\.2|**TrendStrategy**|
|**趋势回调日**|① 1h 方向明确（ADX 15\-25）；② 15m 回撤未破大周期结构；③ 5m 回踩 MA20 后反弹|**PullbackStrategy**|
|**震荡日**|① MA20 斜率绝对值 \&lt; 0\.05%/K；② ADX \&lt; 23（已从20调整）；③ BOLL 带宽收窄至 20 期均值的 0\.8 倍|**RangeStrategy**（区间反转）|
|**情绪极端日**|① 最近1h 振幅 \&gt; 5%；② 1m/5m K线连续3根反向吞没；③ 成交量暴增200%但价格原地踏步|**禁止开仓，等待**|



> **Update1 优化**：ADX 阈值从 20 调整为 23（FIX\-6），覆盖 ADX 20\-25 的常见灰色区间，减少误判为 trend 环境。
> 
> 



#### 2\.2\.2 消息面强度评分算法



`Score = BaseWeight × 0\.5^\(hours/halflife\) × \(1 ± sentiment\_coefficient\)`



- BaseWeight：1\-10（美联储决议 10、日常新闻 1）

- 半衰期：2小时

- 极端消息（强度≥9）→ 触发\&\#34;消息风暴模式\&\#34;，强制减仓至50%或空仓

    

**关键修正规则**：

- 利多不涨 → 强度降级 50%

- 利空不跌 → 强度降级 50%

    

#### 2\.2\.3 环境综合判定与策略路由



|最终环境|触发逻辑|策略响应|
|---|---|---|
|**强趋势**|技术面=强趋势 且 消息强度\&lt;4|TrendStrategy：趋势跟踪|
|**趋势回调**|技术面=回调 且 消息强度\&lt;5|PullbackStrategy：回调介入|
|**震荡**|技术面=震荡 且 消息强度\&lt;4|RangeStrategy：区间反转|
|**消息驱动**|消息强度≥6|NewsStrategy：超短线动能|
|**极端避险**|技术极端 或 消息强度≥9|空仓，禁止开仓|



**策略路由实现**：`environment\.py` 输出环境标签后，`entry\_signal\.py` 的 `\_score\_22\(\)` 根据环境选择评分逻辑——趋势市保留原22分制，震荡市切换为区间反转评分（接近区间下沿→做多，接近区间上沿→做空）。



### 2\.3 多空强弱评分（10分制）



#### 2\.3\.1 多头强势评分



|维度|分值|标准|
|---|---|---|
|回踩浅|0\-2|回调 \&lt; 0\.5×ATR → \+2|
|修复快|0\-2|突破前高 ≤ 5根K → \+2|
|创高能力|0\-2|连续新高 ≥3次 → \+2|
|上涨放量回踩缩量|0\-2|量比\&gt;1\.5 且 回踩量\&lt;0\.8 → \+2|
|相对BTC强|0\-2|涨幅差 \&gt;1% → \+2|



≥8分 → 强多候选。≤4分 → 不适合做多。



#### 2\.3\.2 空头弱势评分



|维度|分值|标准|
|---|---|---|
|反弹弱|0\-2|反弹 \&lt; 0\.3×ATR → \+2|
|破低快|0\-2|连续新低 ≥3次 → \+2|
|下跌放量反弹缩量|0\-2|量比\&gt;1\.5 且 反弹量\&lt;0\.8 → \+2|
|相对BTC弱|0\-2|跌幅 \&gt; BTC跌幅\+1% → \+2|
|中轨压制|0\-2|连续5根K收盘价在中轨下方 → \+2|



≥8分 → 强空候选。≤4分 → 不适合做空。



### 2\.4 三周期入场信号（硬过滤 \+ 22分制评分 \+ 环境自适应）



#### 2\.4\.1 硬过滤条件（必须全部满足）



|编号|条件|检查逻辑|
|---|---|---|
|F1|大周期方向一致|4h MA20同侧 且 斜率\&gt;0\.05%/K|
|F2|中周期结构清晰|1h 近10根K形成≥2个推进波，未反向突破|
|F3|小周期到达关键区|15m 距离支撑/压力 \&lt; 0\.5×ATR|
|F4|盈亏比预估值 ≥ 1\.5|\(TP\-Entry\)/\(Entry\-SL\) ≥ 1\.5|



#### 2\.4\.2 22分制评分细则



**① 大周期方向（4分）**

- 价格在MA20同侧：\+1

- MA20斜率\&gt;0\.1%/K：\+1

- 高低点递增/递减：\+1

- 与中周期方向一致：\+1

    

**② 中周期趋势（6分）**

- 价格在1h MA20同侧：\+1

- 推进波次≥2：\+2（1次\+1）

- BOLL中轨同侧支撑：\+1

- ADX\(14\)\&gt;25：\+1

- 趋势方向量\&gt;反向量×1\.2：\+1

    

**③ 小周期位置（6分）**

- 触及BOLL轨或关键位（差值\&lt;0\.2×ATR）：\+2

- 多重共振：\+1

- 回踩幅度\&lt;1\.5×ATR且未破结构：\+1

- 盈亏比≥1\.5：\+1

- 距离关键位\&lt;0\.2×ATR：\+1

    

**④ 执行周期确认（6分）**

- 反转K线（实体\&gt;70%，影线\&lt;30%）：\+2

- 假突破（突破后2根K内收回）：\+1

- 反转K线量\&gt;前5根均量×1\.5：\+1

- 价格重新站上/跌破15m MA20：\+1

- RSI\&gt;50（多）/RSI\&lt;50（空）或MACD金叉死叉：\+1

    

> **已知局限性（Update1 新增）**：
> 
> - 成交量支持子项存在代码死路径（`if False`），永远不得分。此问题已在诊断报告中记录，修复优先级排在因子验证之后。
> 
> - DX\-1固定持仓回测（8000笔样本）证明22分制评分系统整体不具备方向预测力，准确率在48%\-50%之间浮动，等同于抛硬币。
> 
> - 因此，Update1 的后续路线中，22分制评分系统可能在因子验证通过后被整体替换。
> 
> 



#### 2\.4\.3 环境自适应调整



**震荡市（Range）调整**：

- 评分逻辑切换：不再依赖趋势条件，改用区间反转逻辑。

- 接近区间下沿 → 倾向 LONG，给予评分加成。

- 接近区间上沿 → 倾向 SHORT，给予评分加成。

- 区间内无明显趋势 → 不强制大周期方向一致。

- 止损止盈更紧（幅度约为趋势市的50%）。

    

**趋势回调日（Pullback）调整**：

- 保留趋势评分体系，但降低 ADX 权重。

- 增加\&\#34;回踩 MA20 后反弹确认\&\#34;作为加分项。

- 止损设置更紧（趋势回调风险高于趋势延续）。

    

#### 2\.4\.4 稳定性字段



每个信号增加 `stability` 字段：基于最近5周期K线方向一致性计算，衡量信号可靠性。高稳定性 \+ 高评分 = 强信号。



#### 2\.4\.5 评级与仓位系数映射



|总分|等级|仓位系数|动作|
|---|---|---|---|
|18\-22|A|1\.0|正常开仓（震荡市禁止A级）|
|14\-17|B|0\.5|轻仓介入（震荡市放宽至13分，趋势市放宽至10分）|
|10\-13|C|0\.0|仅记录，不发送（趋势环境10\-12分可发送）|
|\&lt;10|D|0\.0|放弃|



### 2\.5 微观结构层：HFT\-lite 信号过滤器



#### 2\.5\.1 核心特征与降噪处理



|特征|计算公式|降噪要求|
|---|---|---|
|**OBI**|`\(Bid\_Vol\_10 \- Ask\_Vol\_10\) / \(Bid\_Vol\_10 \+ Ask\_Vol\_10\)`|EMA平滑=5，连续3个tick维持在阈值同侧。当前代码阈值0\.12，Gateway配置0\.20（待同步）|
|**Order Flow**|`Market\_Buy\_Vol\_1s / Market\_Sell\_Vol\_1s`|比值\&gt;1\.5且持续0\.5秒。当前flow\_ratio=1\.5，待调整为1\.2|
|**Liquidity Sweep**|前5档挂单量0\.5秒内减少\&gt;50%，价格跳变\&gt;0\.05%|标记为待确认，等待2秒后重评OBI。**已知局限**：`\_detect\_sweep`方法体为`return False`，扫单检测被完全禁用|
|**成交方向连续性**|近30笔成交买卖量比|Update1新增第四层过滤。LONG需要买入占比≥55%（待调整为52%），SHORT需要买入占比≤45%（待调整为48%）|



#### 2\.5\.2 两阶段过滤



- **阶段一**：OBI或订单流通过方向确认（阈值±0\.15），否则延迟最多5秒后放弃。

- **阶段二**：价差保护（\&gt;0\.05%挂单在中间价，\&gt;0\.1%放弃）。

    

**当前实现状态**：`microstructure\.py` 已集成到 `strategy\_runner\.py` 主循环中，在信号发送前进行二次确认。被拦截的信号记录为 `MICRO\_BLOCKED` 并写入数据库。



> **Update1 优化**：
> 
> - OBI阈值已从0\.2下调至0\.12，Gateway配置待同步（FIX\-3）。
> 
> - flow\_ratio待从1\.5调低至1\.2（FIX\-4），改善SHORT方向拦截率。
> 
> - 成交方向连续性阈值待微调（FIX\-5）：LONG 0\.55→0\.52，SHORT 0\.45→0\.48。
> 
> - 信号入库 status 匹配待修复（FIX\-1）：`\&\#34;SUCCESS\&\#34;` → `\&\#34;SUCCESS\&\#34;, \&\#34;APPROVED\&\#34;`。
> 
> 



### 2\.6 卖出管理（止损/止盈优先级 \+ 冷却期）



#### 2\.6\.1 卖出检查优先级



|优先级|类型|触发条件|
|---|---|---|
|**1**|止损（Stop Loss）|LONG：现价≤入场×0\.985（1\.5%止损）；SHORT：现价≥入场×1\.015|
|**2**|跟踪止盈（Trailing）|浮盈≥1\.5%激活，从峰价回撤≥3%触发|
|**3**|回撤止盈（Retracement）|最大浮盈≥2%，回撤比例≥50%触发（强趋势中≥70%）|
|**4**|持仓时间到期|持仓≥5天 → 主动退出|



#### 2\.6\.2 冷却期机制

卖出成功后该币种进入60秒冷却期，防止同一信号重复触发。`can\_sell\(\)` 只检查不写入，`mark\_sold\(\)` 在确认卖出后才写入冷却时间。



> **Update1 新增**：同向信号6小时冷却期（P1\-4），同一币种\+同一方向6小时内只发送第一个信号，避免连续同向信号堆积。冷却期拦截记录为 `COOLDOWN\_BLOCKED`。
> 
> 



### 2\.7 策略状态机



状态流转：`Draft → Backtest → Paper → Live → Deprecated`



每个状态转换记录原因和时间戳，存储在 `state\_manager\.py` 中。当前策略处于 **Paper** 状态。



我已逐项核对第一部分（第一章\+第二章）的内容，确认如下：



---

### 一、与V4\.6\.2\-Optimized原方案对齐



|原方案内容|Update1第一部分|状态|
|---|---|---|
|第一章 1\.1 系统定位与设计哲学|保留原四条铁律，新增第五条\&\#34;数据驱动迭代\&\#34;|✅|
|第一章 1\.2 分层架构全景图|完全保留，未修改|✅|
|第一章 1\.3 数据流向与单向控制铁律|完全保留|✅|
|第一章 1\.4 核心组件职责速查表|更新FastAPI Proxy备注\&\#34;已由systemd托管自动重启\&\#34;|✅|
|第二章 2\.1 选币雷达|保留五因子模型，新增micro因子已知局限说明|✅|
|第二章 2\.2 环境分类|ADX阈值20→23（FIX\-6），已融入判定矩阵|✅|
|第二章 2\.3 多空强弱评分|完全保留|✅|
|第二章 2\.4 三周期入场信号|保留22分制评分，新增成交量代码死路径局限性说明、DX\-1固定持仓回测结论、后续替换路线图|✅|
|第二章 2\.5 微观结构|新增成交方向连续性（第四层过滤）、OBI阈值/flow\_ratio调整记录、FIX\-1\~FIX\-5修复标注|✅|
|第二章 2\.6 卖出管理|保留原优先级\+冷却期，新增同向6小时冷却期（P1\-4）说明|✅|
|第二章 2\.7 策略状态机|完全保留|✅|



### 二、后期优化补充内容覆盖



|优化编号|内容|在文档中的位置|状态|
|---|---|---|---|
|P0\-1|震荡市禁止A级|2\.4\.5 评级表格\+2\.4\.3 环境自适应|✅|
|P0\-2|止损1\.5%|2\.6\.1 \+ 2\.4\.1 F4|✅|
|P1\-1|区间位置反转|2\.4\.3 震荡市调整|✅|
|P1\-4|6小时冷却期|2\.6\.2 \+ 2\.5\.2|✅|
|P2\-1|Range仓位减半|2\.4\.5 评级表格|✅|
|P2\-2|ATR动态仓位|将在第四章详述|⏳ 第二部分|
|P2\-3|趋势B级门槛10分|2\.4\.5 评级表格|✅|
|FIX\-1\~FIX\-6|诊断报告修复项|2\.5\.2 微观结构局限说明|✅|
|DX\-1|固定持仓回测结论|2\.4\.2 22分制局限性说明|✅|
|因子验证路线图|IC分析计划|2\.4\.2 后续路线说明|✅|



# 第二部分：决策与风控层



## 第三章：决策与风控层——风控裁决与资金分配



### 3\.1 Final Risk Gate（最终裁决器）完整逻辑



#### 3\.1\.1 模块定位

`Decision Gateway` 是系统中 **唯一拥有交易否决权** 的组件。策略层产生的任何信号，无论评分多高、微观结构多配合，**必须经过此网关校验**，否则无法到达执行层。



**输入数据源**：

|输入项|来源|格式|
|---|---|---|
|`Signal`|OpenClaw 策略层|包含 symbol、direction、score、confidence、entry\_zone、stop\_loss、tp1|
|`State`|State Center \(Redis\)|实时持仓列表、当前总风险占用、当日已实现亏损、权益曲线高点与当前回撤|
|`Weights`|Hermes Agent|各子策略的推荐权重（若无则为默认等权）|
|`Config`|system\_config\.json|风险上限参数、熔断阈值、相关性惩罚系数|



**输出**：

- `APPROVED`：生成 `ExecutionOrder` 发送至执行引擎。

- `REJECTED`：记录拒绝原因，丢弃信号。

- `ADJUSTED`：修改仓位大小后批准（如因相关性惩罚降低名义仓位）。

    

> **Update1 修复**：信号入库时 `strategy\_runner\.py` L91 原代码 `if status == \&\#34;SUCCESS\&\#34;` 与 Gateway 返回的 `\&\#34;APPROVED\&\#34;` 不匹配，导致所有 paper\-trade 信号丢失。已修复为 `if status in \(\&\#34;SUCCESS\&\#34;, \&\#34;APPROVED\&\#34;\)`（FIX\-1）。
> 
> 



#### 3\.1\.2 裁决逻辑判断树（强制执行顺序）



Gateway 按照以下 **优先级顺序** 逐条校验，**任一条件触发 REJECTED 则立即终止**，不再进行后续检查。



```Plain Text
[1] 账户级熔断检查
    ├── 当前回撤 (Peak - Current) / Peak ≥ 0.20 ？
    │   └── YES → REJECTED (Max Drawdown Liquidation)
    ├── 当日累计已实现亏损 ≥ 账户余额 × 0.03 ？
    │   └── YES → REJECTED (Daily Loss Stop)
    └── 消息风暴模式激活 (消息强度 ≥ 9 持续 1h) ？
        └── YES → 禁止新开仓，REJECTED (News Storm)

[2] 总风险预算检查
    ├── 计算当前已占用风险 = Σ(各持仓 |Entry - Stop| / Entry × 名义仓位 / 账户余额)
    ├── 计算新信号预估风险 = |Entry - Stop| / Entry × (单笔风险上限 1% × 评分仓位系数)
    └── 当前风险 + 新信号风险 > total_risk_cap ？
        └── YES → REJECTED (Risk Budget Exceeded)

[3] 信号置信度与冲突检查
    ├── Signal.score 对应的仓位系数 == 0 ？
    │   └── YES → REJECTED (Low Score)
    ├── Hermes 推荐该策略权重 < 0.1 ？
    │   └── YES → REJECTED (RL Downgrade)
    └── 与现有持仓方向相反且相关系数 > 0.7 ？
        └── YES → REJECTED (Hedging Conflict, 不允许对锁)

[4] 微观结构滑点预估检查
    └── 当前买卖价差 > 0.1% 且 OBI 未显著偏向信号方向 ？
        └── YES → REJECTED (Liquidity Insufficient)

[5] 通过全部检查
    └── 进入资金分配计算模块 → 生成 APPROVED ExecutionOrder
```



#### 3\.1\.3 裁决结果的数据结构

```JSON
{
  "order_id": "uuid",
  "symbol": "BTCUSDT",
  "direction": "LONG",
  "approved_quantity": 0.35,
  "limit_price": 64250.0,
  "stop_loss": 63800.0,
  "take_profit": 65200.0,
  "risk_amount_usd": 120.0,
  "gateway_signature": "hmac_sha256"
}
```



> **Update1 已知局限**：Gateway `evaluate\(\)` 函数中 Kelly 公式的胜率 `p=0\.45` 和盈亏比 `b=1\.5` 为硬编码假设，与回测实际数据（胜率约39\.5%、盈亏比受固定止损止盈影响）存在偏差。此问题已在诊断报告中记录，待因子验证通过后同步校准。
> 
> 





### 3\.2 资金分配模型：风险约束 Kelly \+ 相关性调整 Risk Parity



#### 3\.2\.1 总风险预算（Total Risk Cap）的三层计算



**第一层：理论 Kelly 参考值**

```Plain Text
f_star = (b × p - q) / b
```

- `p` = 策略层滚动 50 笔交易的胜率（来自回测引擎）。

- `b` = 平均盈亏比 = avg\_win\_r / \|avg\_loss\_r\|（R 倍数口径）。

- `q` = 1 \- p。

    

**第二层：环境系数调节**

```Plain Text
Kelly_adj = f_star × Env_Coefficient
```

|环境标签|Env\_Coefficient|逻辑说明|
|---|---|---|
|强趋势|0\.5|允许使用理论值的一半。|
|趋势回调|0\.4|回调介入风险略高。|
|震荡|0\.25|反转策略胜率虽高但盈亏比受限。|
|消息驱动|0\.15|高不确定性，极度保守。|
|极端避险|0\.0|强制不交易。|



**第三层：硬性风险上限（来自 Monte Carlo 安全约束）**

```Plain Text
Total_Risk_Cap = min(
    Account_Balance × 0.02,                     // 铁律：2% 总风险上限
    Account_Balance × (1 - MonteCarlo_DD_P95)   // 模拟得出的安全边际
)
```

- `MonteCarlo\_DD\_P95`：每月离线模拟输出的 95% 分位数最大回撤（例如 0\.34）。

    

**最终总风险预算**：

```Plain Text
Total_Risk = min(Total_Risk_Cap, Account_Balance × Kelly_adj)
```



#### 3\.2\.2 单币种风险平价基础权重



对于候选池中通过评分的币种，先计算其 **波动率倒数权重**：

```Plain Text
Raw_Weight_i = 1 / ATR_Percent_i
```

- `ATR\_Percent\_i = ATR\(14\)\_i / Price\_i`（波动率百分比）。

    

示例：BTC 波动率 1\.2%，ETH 波动率 1\.8%，SOL 波动率 2\.5%。

|币种|ATR%|Raw Weight|归一化后|
|---|---|---|---|
|BTC|1\.2%|83\.33|55\.2%|
|ETH|1\.8%|55\.56|36\.8%|
|SOL|2\.5%|40\.00|8\.0%|



#### 3\.2\.3 相关性惩罚矩阵（Risk Parity 增强）



当同时持有多个币种时，需对高相关性币种进行风险打折，防止风险预算被重复计算。



**简化分组算法（推荐实现）**：



1. 计算所有候选币种与当前持仓币种的 **1h 收益率滚动相关系数（窗口 72 根 K 线）**。

2. 若任意两币种相关系数 **\&gt; 0\.8**，则归入同一 **\&\#34;高相关组\&\#34;**。

3. 对于组内币种，**组总风险预算乘以惩罚系数 0\.6**。

    

**计算公式**：

```Plain Text
Group_Risk_Budget = Total_Risk × (组内原始权重之和) × 0.6
```



**组内二次分配**：

组内各币种按原始权重比例瓜分 `Group\_Risk\_Budget`。



**示例场景**：

- 持仓：BTC（已占用风险 1\.0%）。

- 新信号：ETH（与 BTC 相关系数 0\.85）。

- ETH 原始权重占比 40%。

    

**计算过程**：

1. 检测到 BTC 与 ETH 相关系数 0\.85 \&gt; 0\.8，归为同一组。

2. 组内原始权重：BTC 60% \+ ETH 40% = 100%。

3. 组总预算 = 2% × 100% × 0\.6 = **1\.2%**（而非原本的 2%）。

4. ETH 实际可用风险 = 1\.2% × 40% = **0\.48%**（而非原本的 2% × 40% = 0\.8%）。

    

**无高相关组的情况**：

- 若新信号币种（如 SOL）与现有持仓相关系数均 \&lt; 0\.8，则直接使用原始 Risk Parity 权重分配，不受惩罚。

    

#### 3\.2\.4 风险预算到名义仓位的转换



```Plain Text
Position_Size_i = (Risk_Budget_i × Account_Balance) / Stop_Loss_Percentage_i
```

- `Stop\_Loss\_Percentage\_i = \|Entry\_i \- Stop\_i\| / Entry\_i`。

- 当前止损比率为 1\.5%（P0\-2 从 2\.0% 下调）。

    

**安全限制（硬性）**：

- 单币种名义仓位 ≤ 账户余额 × 10（杠杆 ≤ 10x）。

- 所有持仓总名义仓位 ≤ 账户余额 × 30。

- 若计算出的仓位超过限制，等比缩减所有持仓。

    

    

### 3\.3 决策层输出与执行层交接规范



Gateway 完成裁决与资金分配后，生成 **签名后的执行指令** 发送至 Execution Engine。执行层 **不得修改** 以下字段：



|字段|是否可修改|说明|
|---|---|---|
|`symbol`, `direction`|否|交易对与方向。|
|`quantity`|否|核准的名义仓位数量。|
|`stop\_loss\_price`|否|硬止损价，执行层必须挂上。|
|`limit\_price`|可微调|执行层可根据订单簿深度微调（范围 ±0\.05%）。|





## 第四章：决策与风控层——动态仓位与熔断体系



### 4\.1 动态仓位控制系统：连续函数设计



#### 4\.1\.1 设计目标

传统量化系统通常采用\&\#34;分段式降仓\&\#34;（如回撤 10% 降仓至 50%，回撤 15% 降至 25%）。这种硬切换存在两个问题：

- **边界突变**：在阈值附近，仓位系数跳跃导致策略行为不稳定。

- **缺乏平滑反馈**：无法精细反映账户健康状况的连续变化。

    

V4\.6\.2 采用 **连续指数衰减函数** 替代分段规则，使仓位系数成为回撤幅度的平滑函数。



#### 4\.1\.2 基础公式

```Plain Text
Equity_Curve_Scale = exp(-k × Current_Drawdown)
```

**变量定义**：

- `Current\_Drawdown = \(Peak\_Equity \- Current\_Equity\) / Peak\_Equity`，取值范围 \[0, 1\)。

- `k`：衰减系数，推荐值 **5\.0**（可通过回测优化，范围通常 3\~8）。

    

**函数特性**：

|回撤 \(Drawdown\)|仓位系数 \(k=5\)|含义|
|---|---|---|
|0%|1\.000|满额风险预算。|
|5%|0\.779|轻度回撤，小幅降仓。|
|10%|0\.607|中度回撤，仓位降至约六成。|
|15%|0\.472|显著回撤，仓位减半。|
|20%|0\.368|严重回撤，仓位降至三分之一强。|
|25%|0\.287|接近强制清仓线（20% 时已触发熔断，此值仅作理论参考）。|



**优势**：

- 每一次微小的回撤增加都会立即反映在仓位缩减上，形成 **负反馈闭环**。

- 避免了分段规则在阈值处的\&\#34;悬崖效应\&\#34;。

    

#### 4\.1\.3 收益状态调整：滚动夏普比率修正

回撤仅反映亏损幅度，但未考虑近期盈利质量。系统引入 **最近 20 笔交易的滚动夏普比率** 作为正向调节因子。



**计算逻辑**：

1. 取最近 20 笔已平仓交易的 **R 倍数** 序列（若不足 20 笔则用全部历史）。

2. 计算 `Recent\_Sharpe = mean\(R\_list\) / std\(R\_list\)`（无风险利率视为 0）。

3. 根据夏普值调整系数：

```Python
if Recent_Sharpe > 1.5:
    Adjustment_Factor = 1.2
elif Recent_Sharpe < 0.5:
    Adjustment_Factor = 0.7
else:
    Adjustment_Factor = 1.0
```



**应用**：

```Plain Text
Adjusted_Scale = Equity_Curve_Scale × Adjustment_Factor
```

**限制范围**：最终 `Adjusted\_Scale` 必须裁剪至 **\[0\.1, 1\.5\]** 区间。

- 下限 0\.1：确保极端回撤时仍有微量仓位参与市场（避免完全空仓后无法捕捉反转）。

- 上限 1\.5：在极佳盈利状态下允许适度放大仓位（但仍受总风险 2% 的硬约束）。

    

#### 4\.1\.4 波动率熔断（市场环境紧急制动）

当市场出现极端波动时，无论账户状态如何，必须强制降低仓位以规避流动性危机和插针风险。



**触发指标**：

```Plain Text
Market_Volatility = ATR(14) on 5m / Current_Price
```

- 数据源：5 分钟 K 线，滚动计算近 14 根（约 1 小时 10 分钟）的平均真实波幅。

    

**熔断规则**：

|波动率阈值|动作|系数|
|---|---|---|
|\&lt; 4%|正常|1\.0|
|≥ 4%|触发波动率熔断|**0\.5**|



**应用**：

```Plain Text
Final_Scale = Adjusted_Scale × Volatility_Breaker_Coefficient
```



#### 4\.1\.5 ATR动态仓位（P2\-2，Update1 新增）



在基础动态仓位函数之上，增加基于ATR的仓位调节因子，实现 `position ∝ 1/ATR` 的风险平价思想。



**核心逻辑**：高波动时自动缩小仓位，低波动时放大仓位。



**计算方式**：

```Plain Text
atr_val = ATR(14) on 1h / Current_Price
atr_factor = base_atr / max(atr_val, 0.003)
atr_factor = max(0.5, min(1.5, atr_factor))
```

- `base\_atr = 0\.01`（已校准为匹配 BTC 实际 1h ATR 分布）

    

**回测依据**：ATR动态仓位总R提升59%（\+15\.00→\+23\.85），两个环境（Trend和Range）都受益。



**已知升级方向（O\-27）**：当前使用简化ATR定义（振幅），后续可升级为标准TR定义 \+ 相对ATR（ATR / ATR均值），避免周期性偏差。



#### 4\.1\.6 最终仓位系数整合

将上述三层调节因子与策略层基础仓位系数融合：



```Plain Text
Final_Position_Scale = Base_Position_Coefficient  (来自三周期评分等级 A=1.0, B=0.5, C=0.2)
                       × Final_Scale              (来自动态仓位控制)
                       × Strategy_Weight          (来自 Hermes RL 权重)
                       × atr_factor               (来自 ATR 动态仓位)
                       × env_weight               (Range环境 0.5×, Trend环境 1.0×)
```



**最终名义仓位**：

```Plain Text
Final_Quantity = (Total_Risk_Budget × Final_Position_Scale) / Stop_Loss_Percentage
```

（此公式继承自第三章资金分配模型，并叠加本章的动态调节因子。）





### 4\.2 硬性熔断与破产保护体系



动态仓位控制属于 **\&\#34;软调节\&\#34;**，而本节定义的规则属于 **\&\#34;硬熔断\&\#34;**。硬熔断一旦触发，将 **强制覆盖** 所有软调节结果，直接终止交易或清仓。



#### 4\.2\.1 账户级熔断（不可逆触发）



|熔断类型|触发条件|执行动作|恢复条件|
|---|---|---|---|
|**单日亏损熔断**|当日累计已实现亏损 ≥ 账户余额 × 3%|立即停止所有开仓操作；已挂限价单全部撤销；仅允许平仓或止损。|UTC 0 点自动重置。|
|**连续亏损暂停**|连续亏损 3 笔（每笔均为净亏损）|暂停交易 **30 分钟**，期间不产生新信号。|冷却时间结束自动恢复。|
|**连续亏损停盘**|连续亏损 5 笔|当日停止交易，视为单日亏损熔断。|UTC 0 点重置。|
|**最大回撤清仓**|账户回撤（从历史最高权益起算） ≥ **20%**|**市价全平所有持仓**；撤销所有挂单；禁止一切交易活动。|**需人工登录后台确认并手动解除**。|



#### 4\.2\.2 消息面熔断



|熔断类型|触发条件|执行动作|
|---|---|---|
|**消息风暴模式**|消息面强度评分 **≥ 9** 且持续时间超过 **1 小时**|强制将现有持仓减仓至 **50%**；新开仓指令全部 `REJECTED`；仅允许止损或手动干预。|
|**极端消息紧急状态**|检测到关键词：\&\#34;交易所被盗\&\#34;、\&\#34;稳定币脱锚\&\#34;、\&\#34;监管全面禁止\&\#34;|立即触发 **最大回撤清仓** 流程（等同于回撤 20%）。|



#### 4\.2\.3 策略级熔断



|熔断类型|触发条件|执行动作|
|---|---|---|
|**单策略禁用**|某子策略（Trend/Pullback/Range/News）连续亏损 **10 笔**|自动将该策略的 `Strategy\_Weight` 强制设为 0；发送警报至监控面板。|
|**单币种暂停**|某币种在 1 小时内触发滑点保护 **3 次**|暂停该币种交易 **1 小时**，并将该币种移出候选池至下一轮更新。|



#### 4\.2\.4 破产保护与 Monte Carlo 联动



**破产定义**：

- 账户权益 \&lt; 初始权益 × 30%（即亏损 70%）。在系统内部，若触发此条件，系统将进入 **只读模式**，禁止任何 API 写操作。

    

**Monte Carlo 预警联动**：

- 每月离线运行的 Monte Carlo 引擎输出 `risk\_of\_ruin`（破产概率）。

- 若 `risk\_of\_ruin \&gt; 0\.05`（5%），系统将在下月 **自动下调总风险上限** 至 **1\.5%**（覆盖 `system\_config\.json` 中的 `max\_total\_portfolio\_risk`）。

- 此调整由 Decision Gateway 在每次启动时读取配置生效。

    

**已知局限（Update1 新增）**：Monte Carlo 离线模拟尚未部署，当前依赖固定配置参数。相关功能排在 P3 阶段（阶段七后补充）。





### 4\.3 动态仓位与熔断的协同工作流



下图展示了一个交易信号从产生到执行过程中，如何依次经过动态仓位调节与硬熔断检查：



```Plain Text
[Signal] 基础仓位系数 (A=1.0, B=0.5)
    ↓
[动态仓位控制]
    ├── 计算当前回撤 → exp(-k × DD) = 0.78
    ├── 读取滚动夏普 > 1.5 → × 1.2 = 0.936
    ├── ATR动态仓位 → atr_factor = 1.2 → × 1.2 = 1.123
    ├── 环境权重 (Range) → × 0.5 = 0.562
    └── 市场波动率 3.2% < 4% → 无熔断
    → Final_Scale = 0.562
    ↓
[RL 权重融合] × Strategy_Weight (Trend=0.6) = 0.337
    ↓
[硬熔断检查]
    ├── 回撤 12% < 20% → 通过
    ├── 当日亏损 1.2% < 3% → 通过
    ├── 无连续亏损 → 通过
    └── 消息强度 3 < 9 → 通过
    ↓
[输出最终仓位] 原始预算风险 1% × 0.337 = 0.337% 账户风险
```



我已逐项核对第二部分（第三章\+第四章）的内容，确认如下：



---

### 一、与V4\.6\.2\-Optimized原方案对齐



|原方案内容|Update1第二部分|状态|
|---|---|---|
|第三章 3\.1 Final Risk Gate|完全保留裁决判断树五层级，新增FIX\-1修复标注|✅|
|第三章 3\.1\.3 裁决结果数据结构|完全保留|✅|
|第三章 3\.2 资金分配模型|三层Kelly计算\+Risk Parity相关性惩罚完全保留，止损比率同步更新为1\.5%|✅|
|第三章 3\.3 执行层交接规范|完全保留|✅|
|第四章 4\.1\.1\-4\.1\.4 动态仓位控制|exp\(\-k×DD\)公式\+夏普调节\+波动率熔断完全保留|✅|
|第四章 4\.2 硬性熔断体系|账户级/消息面/策略级熔断\+Monte Carlo联动完全保留|✅|
|第四章 4\.3 协同工作流|数值示例已更新，融入ATR因子和环境权重|✅|



### 二、后期优化补充内容覆盖



|优化编号|内容|在文档中的位置|状态|
|---|---|---|---|
|P0\-2|止损1\.5%|3\.2\.4 转换公式|✅|
|P2\-1|Range仓位减半|4\.1\.6 最终仓位系数整合|✅|
|P2\-2|ATR动态仓位|4\.1\.5 新增独立子章节|✅|
|FIX\-1|信号入库status匹配|3\.1\.1 Update1修复标注|✅|
|FIX\-3|Gateway OBI阈值同步|将在第八章/第十章详述（配置层）|⏳ 后续部分|
|FIX\-4|flow\_ratio调整|将在第二章/第十章详述（策略层）|⏳ 已覆盖|
|已知局限|Gateway Kelly假设偏差|3\.1\.3 Update1已知局限|✅|
|已知局限|Monte Carlo离线模拟未部署|4\.2\.4 Update1已知局限|✅|
|P3\-1\~P3\-3|长期规划|将在第十章执行计划中详述|⏳ 后续部分|

# 第三部分：执行层与学习层



## 第五章：执行层——OpenClaw 执行规范与多 Agent 路由



执行层是系统中**唯一**有权调用交易所交易 API 的模块。在 V4\.6\.2 中，执行层基于 OpenClaw 构建，并扩展了多 Agent 协作、定时任务规范与会话管理能力。



### 5\.1 OpenClaw 订单管理规范



#### 5\.1\.1 分批挂单算法（TWAP\-lite）



为降低大额市价单的滑点冲击，同时最大化获取 Maker 手续费返佣，执行层采用 **固定数量分批挂单** 策略。



**参数配置（来自 ****`system\_config\.json`****）**：

- `split\_orders`：3（将总数量拆分为 3 份）。

- `order\_interval\_bps`：5（每份限价单间隔 0\.05%，基于当前买一/卖一价计算）。

- `maker\_only`：true（优先使用 Post\-Only 限价单）。

    

**算法逻辑（以做多为例）**：

```Python
total_qty = approved_order.quantity
base_price = orderbook.best_bid  # 做多时参考买一价

for i in range(split_orders):
    slice_qty = total_qty / split_orders
    slice_price = base_price * (1 - i * order_interval_bps / 10000)
    place_order(symbol, "BUY", slice_qty, slice_price, post_only=True)
```



**撤单与重挂逻辑**：

- 若某份订单在 `order\_expiry\_seconds`（30 秒）后仍未成交，撤销该订单并重新评估。

    

#### 5\.1\.2 微观结构配合：OBI 动态撤单



在挂单等待成交期间，执行层持续监听 `smoothed\_OBI`。若卖压突然增强导致 OBI 反向突破阈值，立即撤销未成交订单并进入冷却观察期。



#### 5\.1\.3 滑点保护与异常上报



|滑点范围 \(bps\)|动作|说明|
|---|---|---|
|≤ 10 bps \(0\.1%\)|正常成交|记录日志|
|\&gt; 10 bps|交易作废|报告 `Slippage\_Exception`|
|\&gt; 50 bps|触发警报|暂停该币种交易 1 小时|



#### 5\.1\.4 执行层与 Gateway 的签名校验



所有来自 Gateway 的 `ExecutionOrder` 必须携带 HMAC\-SHA256 签名，执行层校验通过后才发送至交易所。





### 5\.2 Cron 定时任务自包含规范（V4\.6\.2 新增）



系统大量依赖 Cron 执行离线任务。**Cron 启动的是干净 Session，看不到任何对话上下文**，因此 Prompt 必须完全自包含。



#### 5\.2\.1 标准模板：四要素缺一不可



|要素|说明|示例|
|---|---|---|
|**目标**|明确要完成什么|\&\#34;生成昨日交易复盘报告\&\#34;|
|**命令**|具体执行步骤|\&\#34;读取 PostgreSQL 交易记录，计算胜率、盈亏比\&\#34;|
|**验证**|如何判断成功|\&\#34;日报应包含 5 个章节，总字数 \&gt; 500\&\#34;|
|**处置**|失败时怎么办|\&\#34;如果数据缺失，发送 Telegram 告警并记录日志\&\#34;|



#### 5\.2\.2 交易系统专用 Cron 示例



```Bash
# 每日 15:30 生成复盘报告
30 15 * * 1-5 hermes exec \
'目标：生成昨日交易复盘报告。
命令：从 PostgreSQL 读取昨日所有平仓交易，计算当日盈亏、胜率、平均盈亏比、最大回撤增量。
验证：报告包含交易概览表、策略绩效明细、微观层滑点统计、风控事件记录。
处置：若 PostgreSQL 连接失败，发送 Telegram 告警；若数据为空，生成空报告并标注"无交易"。'

# 每月 1 日 02:00 运行 Monte Carlo
0 2 1 * * hermes exec \
'目标：运行月度 Monte Carlo 模拟，更新风险参数。
命令：读取过去 90 天交易记录，按环境分组构建 R 分布，运行 1000 条路径模拟。
验证：输出破产概率和 95% 最大回撤，若破产概率 > 5%，自动更新 system_config.json 中 max_total_portfolio_risk 为 1.5%。
处置：若模拟失败，保留上月参数并发送告警。'
```





### 5\.3 OpenClaw 多 Agent 路由配置（V4\.6\.2 增强）



原 V4\.6\.1 中 OpenClaw 作为单一执行单元。V4\.6\.2 将其扩展为 **多 Agent 协作架构**，不同 Agent 专注不同职责，通过 Gateway 统一路由。



#### 5\.3\.1 Agent 分工定义



|Agent ID|名称|职责|绑定通道|推荐模型|
|---|---|---|---|---|
|`main`|主控 Agent|接收指令，调度任务，汇总结果|Telegram 主 Bot / OpenClaw 面板|`deepseek\-v4\-pro`|
|`signal`|信号 Agent|运行选币雷达、三周期评分，输出交易信号|内部 API|`deepseek\-v4\-pro`|
|`risk`|风控 Agent|监控熔断状态，推送警报，执行紧急平仓指令|Telegram 告警 Bot|`deepseek\-v4\-pro`|



> **Update1 模型变更记录**：
> 
> - 初期使用 aiapi\.world 中转平台，后因欠费迁移至 DeepSeek 官方 API。
> 
> - 曾尝试切换到 `aiapi\-claude/claude\-opus\-4\-7`，因飞书通道时效和模型余额问题，最终统一使用 `deepseek\-v4\-pro`。
> 
> - 当前三个 Agent 均使用 `deepseek\-v4\-pro`，通过 `openclaw\.json` 的 `agents\.list` 配置。
> 
> - **经验教训**：切换模型前需确认目标 provider 的余额充足和通道稳定性；飞书只适合发快速查询，不适合发需要大量计算的任务。
> 
> 



#### 5\.3\.2 Binding 路由规则



```Plain Text
bindings {
    agentId = "main"
    match { channel = "telegram"; accountId = "main_bot" }
}
bindings {
    agentId = "risk"
    match { channel = "telegram"; accountId = "alert_bot" }
}
# signal Agent 不绑定公开通道，仅接收内部 API 调用
```



#### 5\.3\.3 安全隔离配置



**沙箱隔离**：

```Plain Text
sandbox {
    mode = "all"      # 始终隔离
    scope = "agent"   # 每 Agent 独立容器
}
```



**工具权限控制**：

```Plain Text
# signal Agent：只读行情数据，不能下单
tools {
    allow = ["read", "web_fetch", "memory_search"]
    deny = ["exec", "write", "order_submit"]
}

# risk Agent：可发送通知，可触发熔断 API
tools {
    allow = ["read", "telegram_send", "circuit_breaker_trigger"]
    deny = ["order_submit", "exec"]
}
```





### 5\.4 会话管理规范（防压缩断片）（V4\.6\.2 新增）



#### 5\.4\.1 核心原则：一个 session 只做一件事



|场景|会话策略|
|---|---|
|**信号生成**|每次信号生成后立即开新会话，确保每个信号的上下文独立|
|**复盘分析**|专门开一个复盘会话，只读取历史数据，不产生新信号|
|**RL 学习**|Hermes 在后台独立运行，不占用交易主流程的上下文|
|**长任务**|超过 50 轮对话的任务，拆分为多个子任务，用文件传递状态|



#### 5\.4\.2 防断片检查点



在长任务进行到关键节点时，主动插入检查点：



> \&\#34;当前进度总结：已完成选币池更新，候选币种为 BTC、ETH、SOL。下一步生成三周期信号。请将此进度写入 MEMORY\.md。\&\#34;
> 
> 



#### 5\.4\.3 压缩告警响应



当 Hermes 输出 `accuracy may degrade` 时，立即执行：

1. 让 Agent 用 `memory\_save` 保存当前任务的核心决策和文件路径

2. 用 `session\_search` 确认关键上下文已存好

3. 开新会话继续

    

> **Update1 经验补充**：复杂诊断任务（如全面分析V4项目）建议直接通过 **OpenClaw 面板** 发送，而非飞书。面板没有消息响应超时限制，任务处理多久都能正常返回结果。飞书只适合快速查询（如查看系统状态、最近信号）。
> 
> 





## 第六章：学习层——Hermes RL 引擎与记忆进化



学习层是系统的\&\#34;大脑\&\#34;，负责从历史交易中提炼经验、优化策略、沉淀技能。V4\.6\.2 在 V4\.6\.1 的 RL 权重学习基础上，新增了 **技能自沉淀**、**四层记忆架构**、**上下文压缩配置** 等能力。



> **Update1 当前状态**：Hermes 原生的 RL 权重学习功能尚未正式启用，当前使用 FastAPI 代理临时替代其模型调用功能。Hermes Dashboard 保留运行，用于后续技能沉淀和记忆管理。
> 
> 



### 6\.1 Hermes RL 权重自适应



#### 6\.1\.1 算法选型：Contextual Bandit \(Thompson Sampling\)



**弃用 Q\-Learning 的原因**：金融市场是非平稳环境，Q 表的收敛性无法保证；ε\-greedy 策略在连续亏损时可能过度探索导致更大回撤。



**Thompson Sampling 优势**：通过 Beta 分布天然平衡探索与利用，收敛速度快，输出策略权重分布而非离散动作。



#### 6\.1\.2 状态空间定义



|特征名|类型|取值范围|说明|
|---|---|---|---|
|`market\_env`|One\-Hot|4 维|当前环境：趋势/回调/震荡/消息驱动|
|`volatility`|Float|0\~0\.10|当前 5m ATR% 归一化值|
|`btc\_trend`|Int|\-1, 0, 1|BTC 1h MA20 斜率方向|
|`news\_score`|Float|0\~10|当前消息面强度评分|
|`drawdown`|Float|0\~0\.20|当前账户回撤百分比|



#### 6\.1\.3 模型结构：多臂老虎机维护



对于每个离散化的状态簇（通过 K\-Means 或决策树将连续状态分桶），Hermes 维护一个独立的 Beta 分布参数集。每个策略维护一对 \(alpha, beta\) 参数，通过 Beta 分布采样得到归一化权重向量。



#### 6\.1\.4 奖励函数设计



```Plain Text
reward = trade_R - 0.5 × max(drawdown_increase, 0)
```

- `trade\_R`：该笔交易的 R 倍数

- `drawdown\_increase`：持仓期间账户回撤的新增幅度

    

#### 6\.1\.5 状态聚类与在线学习流程



使用 MiniBatchKMeans（预设 20 个簇），每日凌晨用历史状态数据重新训练。实时推理时将当前状态向量映射到最近的簇 ID，使用该簇对应的 ContextualBandit 实例。



完整学习循环：

```Plain Text
[1] 策略层产生 Signal → [2] Gateway 批准并执行 → [3] 平仓后计算奖励
→ [4] Hermes 获取开仓时刻状态向量 → [5] 更新对应 Cluster 的 Alpha/Beta
→ [6] 下一笔交易前 Gateway 调用 Hermes API 获取最新权重
```



#### 6\.1\.6 权重输出 API 定义



Gateway 在裁决每一笔信号前调用 Hermes 内部接口：

```Plain Text
GET /v1/weights?env=trend&vol=0.018&btc=1&news=2.3&dd=0.06
Host: hermes-agent:8081
```



**已知局限（Update1 新增）**：Hermes 原生 RL 学习功能尚未启用，当前使用 FastAPI 代理替代。这是 P3 阶段的待办事项，排在策略层稳定运行之后。





### 6\.2 技能自沉淀机制（V4\.6\.2 新增）



#### 6\.2\.1 设计目标



将 Hermes 在 RL 学习过程中发现的 **高价值策略模式** 和 **风控经验** 固化为可复用的 Skill，避免每次遇到相似市场状态时都从零开始推理。



#### 6\.2\.2 Skill 结构定义



每个 Skill 存放在 `\~/\.hermes/skills/\&lt;name\&gt;/SKILL\.md`，包含 YAML 元数据和 Markdown 正文：

- **触发条件**：市场环境、技术指标阈值等

- **操作步骤**：确认方向、计算止损止盈、验证盈亏比

- **验证方法**：检查 Gateway 批准状态、记录价格偏差

- **注意事项**：成交量异常、流动性风险等

    

#### 6\.2\.3 自动沉淀流程



|步骤|触发条件|动作|
|---|---|---|
|**候选生成**|同一策略模式连续成功 ≥ 3 次|Hermes 生成候选 Skill 草稿|
|**人工 Review**|候选进入待审队列|用户通过 CLI 审核|
|**采纳**|用户确认|写入 `\~/\.hermes/skills/`|
|**拒绝**|用户驳回|记录拒绝原因，防止重复提案|



#### 6\.2\.4 技能审计



每月自动运行一次，检测功能重复、触发条件模糊、步骤过时的 Skill，审计报告通过 Telegram 推送。



#### 6\.2\.5 与记忆系统的联动



Skill 执行前自动读取 `MEMORY\.md` 中相关注意事项，执行后将新发现的经验写回记忆。





### 6\.3 记忆系统详细设计（V4\.6\.2 新增）



#### 6\.3\.1 四层记忆架构



|层级|目录/文件|用途|维护者|容量限制|
|---|---|---|---|---|
|**L1: working**|会话上下文|当前会话的临时工作记忆|系统自动|受上下文窗口限制|
|**L2: episodic**|`\~/\.hermes/memory/episodic/`|每次执行的行为日志|Agent 自动追加|无硬限制|
|**L3: semantic**|`\~/\.hermes/memory/semantic/LESSONS\.md`|沉淀下来的通用经验|Agent 策展|2,200 字符|
|**L4: personal**|`\~/\.hermes/memory/personal/PREFERENCES\.md`|用户个人偏好|Agent 策展|1,375 字符|



#### 6\.3\.2 冻结快照机制



`LESSONS\.md` 和 `PREFERENCES\.md` 在每次会话开始时作为冻结快照注入上下文，会话中途写入的内容到后续会话才体现，保证前缀稳定和缓存命中。



#### 6\.3\.3 nudge 配置



`nudge\_interval` 控制 Agent 进行记忆反思的频率，交易主流程推荐 3\~5，复盘分析推荐 5\~10。





### 6\.4 上下文压缩配置（V4\.6\.2 新增）



#### 6\.4\.1 推荐配置



```YAML
# ~/.hermes/config.yaml
compression:
  threshold: 0.75        # 上下文使用率达到此值触发压缩
  target_ratio: 0.25     # 压缩后保留原长度的比例
  protect_last_n: 30     # 保护最近 N 条消息不被压缩
```



|参数|默认值|推荐值|说明|
|---|---|---|---|
|`threshold`|0\.50|**0\.75**|太早压缩会影响推理质量|
|`target\_ratio`|0\.25|0\.25|保留核心25%|
|`protect\_last\_n`|20|**30**|保护更多近期消息|



#### 6\.4\.2 压缩降级策略



|阶段|提示|建议动作|
|---|---|---|
|压缩 ≥ 2 次|`accuracy may degrade`|准备转移，保存核心决策，开新会话|
|压缩无效|压缩器自动停止|建议 `/new`|
|彻底压不动|`Context length exceeded`|Agent 终止，任务标记失败|





### 6\.5 离线 Monte Carlo 引擎实现细节



Monte Carlo 引擎作为 Hermes 的离线子模块，每月运行一次，输出关键风险参数供下月使用。



#### 6\.5\.1 交易分布构建

从 PostgreSQL 中提取过去 90 天的所有平仓交易，按环境标签分组存储 R 倍数列表。



#### 6\.5\.2 马尔可夫环境序列生成

使用历史环境转移频率构建转移矩阵，生成 252 个交易日（约一年）的路径。



#### 6\.5\.3 路径模拟与破产统计

对每条模拟路径：每日根据环境从对应 R 分布中随机抽取一笔交易结果 → 应用动态仓位函数 → 更新权益曲线 → 若权益跌破初始资金的 30% 则记录破产。



运行 1000 条路径后输出：

- `risk\_of\_ruin`：破产路径数 / 1000

- `max\_drawdown\_p95`：95% 分位数最大回撤

- `expected\_return`：平均年化收益

    

**联动机制**：若 `risk\_of\_ruin \&gt; 0\.05`，自动更新 `system\_config\.json` 中的 `max\_total\_portfolio\_risk` 为 1\.5%。



> **Update1 状态**：Monte Carlo 离线模拟模块尚未部署，属于 P3 阶段的待办事项。
> 
> 



---

我已逐项核对第三部分（第五章\+第六章）的内容，确认如下：



---

### 一、与V4\.6\.2\-Optimized原方案对齐



|原方案内容|Update1第三部分|状态|
|---|---|---|
|第五章 5\.1 订单管理规范|分批挂单算法、OBI动态撤单、滑点保护、签名校验完全保留|✅|
|第五章 5\.2 Cron定时任务规范|标准模板四要素 \+ 交易系统专用Cron示例完全保留|✅|
|第五章 5\.3 多Agent路由配置|Agent分工定义、Binding路由规则、安全隔离配置完全保留|✅|
|第五章 5\.4 会话管理规范|核心原则、防断片检查点、压缩告警响应完全保留|✅|
|第六章 6\.1 Hermes RL权重自适应|Thompson Sampling算法、状态空间、模型结构、奖励函数、聚类流程、权重输出API完全保留|✅|
|第六章 6\.2 技能自沉淀机制|Skill结构定义、自动沉淀流程、技能审计、与记忆联动完全保留|✅|
|第六章 6\.3 记忆系统详细设计|四层记忆架构、冻结快照、nudge配置完全保留|✅|
|第六章 6\.4 上下文压缩配置|推荐配置、压缩降级策略完全保留|✅|
|第六章 6\.5 Monte Carlo引擎|交易分布构建、环境序列生成、路径模拟、破产统计、联动机制完全保留|✅|



### 二、后期优化补充内容覆盖



|优化编号/经验|内容|在文档中的位置|状态|
|---|---|---|---|
|模型切换记录|aiapi→DeepSeek→Claude→DeepSeek\-v4\-pro演变|5\.3\.1 Update1模型变更记录|✅|
|飞书超时教训|复杂任务用面板而非飞书|5\.4\.3 Update1经验补充|✅|
|Hermes RL当前状态|尚未启用，FastAPI代理替代|6\.1 Update1当前状态 \+ 6\.1\.6 已知局限|✅|
|Monte Carlo当前状态|尚未部署，P3阶段|6\.5 Update1状态|✅|
|技能沉淀/记忆/压缩|尚未部署，P3阶段|6\.2\-6\.4 保留原设计|✅|

# 第四部分：部署运维与附录



## 第七章：部署手册与运维监控



### 7\.1 Docker Compose 完整编排文件



以下编排文件定义了 V4\.6\.2 系统的全部微服务、网络与存储卷。



```YAML
version: "3.9"

networks:
  trading_net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

volumes:
  redis_data:
  postgres_data:
  grafana_data:
  hermes_models:
  openclaw_workspace:

services:

  # ==================== 基础组件 ====================
  redis:
    image: redis:7.2-alpine
    container_name: v4-redis
    restart: unless-stopped
    networks:
      - trading_net
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru --requirepass ${REDIS_PASSWORD}
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

  postgres:
    image: postgres:15-alpine
    container_name: v4-postgres
    restart: unless-stopped
    networks:
      - trading_net
    environment:
      POSTGRES_USER: v4_user
      POSTGRES_PASSWORD: ${PG_PASSWORD}
      POSTGRES_DB: v4_trading
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U v4_user"]
      interval: 10s
      timeout: 3s
      retries: 3

  # ==================== 行情与数据层 ====================
  market-data:
    build: ./services/market-data
    container_name: v4-market
    restart: unless-stopped
    networks:
      - trading_net
    depends_on:
      redis:
        condition: service_healthy
    environment:
      REDIS_URL: redis://default:${REDIS_PASSWORD}@redis:6379
      EXCHANGE: ${EXCHANGE}
      API_KEY: ${READONLY_API_KEY}
      API_SECRET: ${READONLY_API_SECRET}
    volumes:
      - ./config/system_config.json:/app/config.json:ro

  # ==================== 策略与调度层 ====================
  openclaw-core:
    build: ./services/openclaw
    container_name: v4-openclaw
    restart: unless-stopped
    networks:
      - trading_net
    depends_on:
      redis:
        condition: service_healthy
      market-data:
        condition: service_started
    environment:
      REDIS_URL: redis://default:${REDIS_PASSWORD}@redis:6379
      HERMES_URL: http://hermes-agent:8081
      GATEWAY_URL: http://decision-gateway:8080
    volumes:
      - ./config/system_config.json:/app/config.json:ro
      - openclaw_workspace:/app/workspace

  # ==================== 决策与风控层 ====================
  decision-gateway:
    build: ./services/decision-gateway
    container_name: v4-gateway
    restart: unless-stopped
    networks:
      - trading_net
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      hermes-agent:
        condition: service_started
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      REDIS_URL: redis://default:${REDIS_PASSWORD}@redis:6379
      DATABASE_URL: postgresql://v4_user:${PG_PASSWORD}@postgres:5432/v4_trading
      HERMES_URL: http://hermes-agent:8081
      SECRET_KEY: ${GATEWAY_SECRET}
    volumes:
      - ./config/system_config.json:/app/config.json:ro

  # ==================== 执行层 ====================
  execution-engine:
    build: ./services/execution-engine
    container_name: v4-execution
    restart: unless-stopped
    networks:
      - trading_net
    depends_on:
      decision-gateway:
        condition: service_started
    environment:
      REDIS_URL: redis://default:${REDIS_PASSWORD}@redis:6379
      GATEWAY_URL: http://decision-gateway:8080
      EXCHANGE: ${EXCHANGE}
      API_KEY: ${TRADING_API_KEY}
      API_SECRET: ${TRADING_API_SECRET}
    volumes:
      - ./config/system_config.json:/app/config.json:ro

  # ==================== 学习层 ====================
  hermes-agent:
    build: ./services/hermes
    container_name: v4-hermes
    restart: unless-stopped
    networks:
      - trading_net
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      REDIS_URL: redis://default:${REDIS_PASSWORD}@redis:6379
      DATABASE_URL: postgresql://v4_user:${PG_PASSWORD}@postgres:5432/v4_trading
    volumes:
      - ./config/system_config.json:/app/config.json:ro
      - hermes_models:/app/models
      - ./hermes/memory:/app/memory
      - ./hermes/skills:/app/skills

  monte-carlo:
    build: ./services/monte-carlo
    container_name: v4-montecarlo
    restart: "no"
    networks:
      - trading_net
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://v4_user:${PG_PASSWORD}@postgres:5432/v4_trading
    volumes:
      - ./config/system_config.json:/app/config.json:ro
      - ./reports/montecarlo:/app/reports

  # ==================== 监控与可视化 ====================
  grafana:
    image: grafana/grafana:10.2.0
    container_name: v4-grafana
    restart: unless-stopped
    networks:
      - trading_net
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD}
      GF_INSTALL_PLUGINS: grafana-clock-panel,grafana-simple-json-datasource
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  prometheus:
    image: prom/prometheus:latest
    container_name: v4-prometheus
    restart: unless-stopped
    networks:
      - trading_net
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
```



#### 7\.1\.1 环境变量文件 `\.env` 示例

```TOML
EXCHANGE=binance
READONLY_API_KEY=xxxxxx
READONLY_API_SECRET=xxxxxx
TRADING_API_KEY=yyyyyy
TRADING_API_SECRET=yyyyyy
PG_PASSWORD=strong_password_here
REDIS_PASSWORD=strong_redis_password_here
GATEWAY_SECRET=hmac_secret_for_order_signing_at_least_32_chars
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin123
```



> **Update1 新增**：FastAPI 代理（`hermes\_proxy\.py`）已由 systemd 托管，不再作为 Docker 容器运行。服务文件位于 `/etc/systemd/system/v4\-proxy\.service`，配置为自动重启和开机自启。健康检查任务同样需要监控该代理的状态。
> 
> 



#### 7\.1\.2 网络隔离策略说明



|网络策略|实现方式|目的|
|---|---|---|
|Hermes 无法访问 Execution Engine|两者无直接依赖，且未暴露端口给 Hermes|防止学习层直接下单|
|Execution Engine 仅能调用 Gateway|`GATEWAY\_URL` 指向内部服务名，不对外暴露|确保订单必经裁决|
|行情服务使用只读 API Key|环境变量注入，无法构造签名交易|数据采集安全|
|外部服务仅绑定 127\.0\.0\.1|Grafana、Gateway 仅监听本地回环地址|防止公网直接访问内部服务|





### 7\.2 冷启动四阶段流程与验收标准



**禁止**在部署后立即启用全自动交易。必须严格按照以下阶段推进。



#### 阶段 1：基础设施验证（1\-3 天）

- **状态**：所有容器启动，行情服务运行，执行引擎使用只读 API Key。

- **验收标准**：

    * [ ] Redis 与 PostgreSQL 健康检查通过

    * [ ] `market\-data` 成功推送 BTC/ETH 订单簿数据至 Redis，延迟 \&lt; 200ms

    * [ ] `openclaw\-core` 能够生成选币池与市场环境标签

    * [ ] Grafana 大盘显示实时价格与 OBI 曲线

    * [ ] Hermes Agent 成功连接数据库

    * [ ] **（Update1 新增）** FastAPI 代理已由 systemd 托管，开机自启验证通过

    

#### 阶段 2：纯规则 Paper Trading（2 周）

- **状态**：执行引擎仍使用只读 API，所有信号仅记录日志，不实际下单。

- **系统配置**：`system\_config\.json` 中设置 `paper\_trading: true`

- **验收标准**：

    * [ ] 连续运行 14 天无服务崩溃或内存泄漏

    * [ ] 日均信号产生数量在合理范围（5\-30 笔）

    * [ ] 模拟权益曲线回撤 \&lt; 15%

    * [ ] 滑点模拟统计 \&lt; 0\.1% 的订单占比 \&gt; 95%

    * [ ] Cron 定时任务能正常触发并生成报告

    * [ ] **（Update1 新增）** 信号入库链路正常，日志与数据库记录一致

    * [ ] **（Update1 新增）** 策略引擎进程稳定运行，无意外停机

    

#### 阶段 3：Hermes 影子模式（1 周）

- **状态**：Hermes 仅读取数据、输出权重日志，不向 Gateway 发送权重。

- **验收标准**：

    * [ ] Hermes 每日成功聚类状态并更新 Beta 参数

    * [ ] 权重输出无明显异常

    * [ ] 对比规则曲线与 Hermes 建议曲线，后者回撤无明显放大

    * [ ] Skill 候选生成功能正常

    

#### 阶段 4：小资金上线（10% 仓位限制）

- **状态**：执行引擎切换为交易 API Key，总风险设为 0\.2%。

- **验收标准**：

    * [ ] 首日实际滑点 \&lt; 0\.1%

    * [ ] 实际成交与 Paper 模拟偏差 \&lt; 10%

    * [ ] 连续 7 个交易日无硬熔断触发

    * [ ] 若连续盈利且回撤 \&lt; 5%，可逐步将风险上限调至 1%

    

> **Update1 当前状态**：系统处于阶段 2（纸上谈兵），策略引擎已恢复稳定运行。阶段 3 和阶段 4 的前置条件尚未满足（Hermes RL 未启用、信号数据量不足）。
> 
> 





### 7\.3 Grafana 双仪表盘监控



#### 7\.3\.1 系统状态监控仪表盘（14 面板）



|面板行|面板名称|类型|数据来源|
|---|---|---|---|
|第一行|v4\-redis / v4\-postgres / v4\-grafana|Stat 状态卡|`health\_checks` 表|
|第一行|v4\-gateway / hermes\-dashboard / Gateway API / FastAPI Proxy|Stat 状态卡|`health\_checks` 表|
|第二行|最近检查时间|Stat|`health\_checks` 表|
|第三行|CPU 使用率 / 内存使用率 / 磁盘使用率|Gauge 仪表盘|`system\_metrics` 表|
|第四行|网络流入速率 / 网络流出速率 / 系统负载|Stat|`system\_metrics` 表|



> **Update1 新增**：健康检查脚本（`health\_check\.sh`）已增加策略引擎进程存活检测（FIX\-2）。若 `strategy\_runner\.py` 进程不存在，Grafana 将显示 ❌ 状态。
> 
> 



#### 7\.3\.2 交易可视化监控仪表盘（10 面板）



|面板行|面板名称|类型|数据来源|
|---|---|---|---|
|第一行|信号总数 / 今日信号 / 最新评分 / 已发送数|Stat|`signals` 表|
|第二行|信号明细（最近30条）|Table|`signals` 表|
|第三行|评分分布（近7天）|Bar 柱状图|`signals` 表|
|第三行|方向分布（近7天）|Pie 饼图|`signals` 表|
|—|**信号通过率（近24小时）**|Stat|`signals` 表|
|—|**信号产出趋势（近24小时）**|Timeseries|`signals` 表|
|—|**微结构拦截率（近24小时）**|Stat|`signals` 表|



**告警规则**：任一服务状态为 DOWN → 红色告警；策略引擎进程消失 → 红色告警；磁盘 \&gt; 80% → 黄色告警；内存 \&gt; 85% → 黄色告警。



> **Update1 新增**：信号通过率、信号产出趋势、微结构拦截率三个面板为 Update1 新增的系统退化检测指标，用于监控评分系统、过滤链路是否正常运行。
> 
> 





### 7\.4 Token 成本优化策略



1. 所有 Agent 统一使用 `deepseek\-v4\-pro`，性价比极高。

2. `tools\.profile` 精简为 `none`，避免不必要的工具定义占 Token。

3. 禁用未使用的插件（deepseek 插件、bonjour 插件），减少加载开销。

4. 核心原则：一个 session 里尽量只做一件事。

5. **（Update1 新增）** 复杂诊断任务建议通过 OpenClaw 面板直接发送，避免飞书通道超时导致的 Token 浪费。飞书仅适合快速查询。

6. Gateway 必须从 `$HOME` 启动，避免加载项目级 `AGENTS\.md` 浪费 Token。

    

    

### 7\.5 网络隔离与权限控制



|组件|可访问的服务|禁止访问|实现方式|
|---|---|---|---|
|**Hermes**|Redis, PostgreSQL, Gateway API|Execution Engine, 交易所 API|Docker 网络隔离 \+ 只读 API Key|
|**OpenClaw**|Redis, PostgreSQL, Gateway API|Execution Engine, 交易所 API|Docker 网络隔离|
|**Gateway**|所有内部服务|无（但仅被内部调用）|端口仅绑定 127\.0\.0\.1|
|**Execution Engine**|Gateway, 交易所 API|无直接对外暴露|不暴露任何端口|
|**Market Data**|Redis, 交易所只读 API|交易所交易 API|只读 API Key|



> **Update1 新增**：FastAPI 代理仅监听 `127\.0\.0\.1:8081`，由 systemd 托管。Grafana 通过 nginx 反向代理对外开放（端口 3001），需腾讯云防火墙放行。
> 
> 





## 第八章：附录——配置参数与接口大全



### 8\.1 系统配置文件 `system\_config\.json` 完整字段释义



```JSON
{
  "system": {
    "paper_trading": true,
    "log_level": "INFO",
    "timezone": "UTC"
  },
  "risk_parameters": {
    "max_total_risk_per_trade": 0.01,
    "max_total_portfolio_risk": 0.02,
    "daily_loss_stop": 0.03,
    "max_drawdown_liquidation": 0.20,
    "monte_carlo_ruin_threshold": 0.05,
    "correlation_threshold": 0.80,
    "correlation_group_penalty": 0.60
  },
  "position_sizing": {
    "equity_curve_k": 5.0,
    "volatility_breaker": 0.04,
    "base_kelly_fraction": 0.30,
    "sharpe_boost_threshold": 1.5,
    "sharpe_penalty_threshold": 0.5,
    "environment_kelly_multiplier": {
      "trend": 0.50, "pullback": 0.40,
      "range": 0.25, "news": 0.15, "extreme": 0.00
    }
  },
  "execution": {
    "split_orders": 3,
    "order_interval_bps": 5,
    "order_expiry_seconds": 30,
    "max_slippage_percent": 0.001,
    "maker_only": true,
    "microstructure_cooling_seconds": 2
  },
  "microstructure": {
    "obi_threshold": 0.20,
    "obi_smoothing_span": 5,
    "signal_validity_ticks": 3,
    "sweep_detection_threshold": 0.50,
    "sweep_time_window_ms": 500
  },
  "compression": {
    "threshold": 0.75, "target_ratio": 0.25,
    "protect_last_n": 30, "mode": "safeguard"
  },
  "memory": {
    "nudge_interval": 5,
    "max_lessons_chars": 2200, "max_preferences_chars": 1375,
    "max_soul_bytes": 1024
  },
  "session": {
    "max_turns_per_signal_session": 30,
    "auto_new_session_on_signal": true
  }
}
```



> **Update1 修复待办**：`microstructure\.obi\_threshold` 在 Gateway 配置中为 0\.20，但 `microstructure\.py` 代码默认值为 0\.12，两者不一致。需同步为 0\.12（FIX\-3）。`flow\_ratio` 参数未出现在配置文件中，需补充为 1\.2（FIX\-4）。
> 
> 



### 8\.2 核心 API 调用链示例



**Hermes 权重建议**：`GET /v1/weights?env=trend\&amp;vol=0\.018\&amp;btc=1\&amp;news=2\.3\&amp;dd=0\.06`



**Gateway 信号裁决**：`POST /v1/signal/evaluate`，返回 `APPROVED` / `REJECTED` / `ADJUSTED`。



> **Update1 修复**：信号入库时 `strategy\_runner\.py` 原代码 `if status == \&\#34;SUCCESS\&\#34;` 与 Gateway 返回的 `\&\#34;APPROVED\&\#34;` 不匹配，已修复为 `if status in \(\&\#34;SUCCESS\&\#34;, \&\#34;APPROVED\&\#34;\)`（FIX\-1）。
> 
> 



### 8\.3 异常检测触发阈值速查表



|异常类型|检测指标|阈值|动作|
|---|---|---|---|
|滑点异常|单笔成交滑点|\&gt; 0\.1%|交易作废并报警|
|胜率突变|滚动 20 笔胜率 vs 历史均值|下降 \&gt; 30%|暂停对应策略|
|回撤加速|3 日内回撤增幅|\&gt; 5%|强制仓位系数 × 0\.5|
|RL 权重跳变|单次更新权重变化|\&gt; 0\.3|忽略本次更新|
|订单簿异常|买卖价差|\&gt; 0\.5% 持续 10s|暂停该币种微观层|
|API 限频|剩余权重|\&lt; 10%|降级为只平仓不开仓|
|**（新增）策略引擎停机**|进程不存在|连续两次检查|自动重启 \+ Telegram 告警|



### 8\.4 复盘报告标准模板



每日 UTC 0 点自动生成，包含：交易概览（总次数、胜率、盈亏比、最大盈亏、滑点成本）、策略绩效明细（各子策略胜率与夏普）、微观层效果统计、风控事件记录、Hermes RL 权重更新、Skill 与记忆状态、Monte Carlo 月度预警。



### 8\.5 策略铁律（11条）



1. 总风险 ≤ 账户 2%，且 ≤ Monte Carlo 安全上限。

2. 单笔风险 ≤ 账户 1%。

3. 微观结构信号仅用于过滤和执行优化，不独立产生信号。

4. 同一币种同方向 10 秒内最多一次微观触发。

5. 回撤 \&gt; 20% 立即清仓停盘，需人工恢复。

6. 每月运行 Monte Carlo，破产概率 \&gt; 5% 则下月强制降险至 1\.5%。

7. Hermes RL 权重每笔交易后更新，禁止人工干预。

8. 滑点超过 0\.1% 的交易自动作废并记录异常。

9. 任何子策略连续亏损 10 笔 → 自动禁用，需人工复核。

10. 消息面强度 ≥ 9 持续 1 小时 → 强制减仓 50%，禁止开新仓。

11. 任何需要持久化的规则必须写入配置文件或注册为 Cron，禁止依赖 Agent 口头承诺。

    

### 8\.6 执行口诀（Update1 版）



> 选三层，判环境，拆策略。回测跑路径，破产控总险，硬上限 2%。  
> 
> 资金分配 Kelly \+ RP，高相关币种折半仓。  
> 
> 微观层看盘口，OBI 平滑三连阳。  
> 
> 动态仓位指数降，夏普调节熔断防。  
> 
> 市场环境先判定，趋势震荡各自战。  
> 
> ATR 仓位动态调，Range 权重折半算。  
> 
> 冷却期防重复开，六小时间同向拦。  
> 
> 因子验证驱迭代，数据说话不空谈。  
> 
> 回撤 20% 即停盘，破产 5% 降风险。  
> 
> 先活下来，再求复利；智能进化，稳健至上。
> 
> 

# 第九章：执行落地计划



## 9\.1 总体阶段规划



|阶段|名称|预计耗时|核心目标|状态|
|---|---|---|---|---|
|阶段一|环境准备与安全检查|1\-2天|确保服务器环境干净可用|✅ 已完成|
|阶段二|安装 Hermes Agent \+ FastAPI 代理|2\-3天|模型调用链路打通|✅ 已完成|
|阶段三|OpenClaw 基础优化|2\-3天|SOUL/USER/MEMORY 配置文件就绪|✅ 已完成|
|阶段3\.5|OpenClaw 多 Agent 路由|1天|Main/Signal/Risk 三 Agent 分工|✅ 已完成|
|阶段四|State Center 搭建|2\-3天|Redis/PG/Grafana 部署完成|✅ 已完成|
|阶段五|Decision Gateway 部署|3\-5天|风控裁决核心上线|✅ 已完成|
|阶段六|监控告警与定时任务|2\-3天|Telegram/Cron/健康检查就绪|✅ 已完成|
|阶段七|分步测试与上线|4\-6周|从 Paper Trading 到小额实盘|⚡ 阶段 7\.1 进行中|



### 9\.1\.1 Update1 新增：数据驱动迭代循环



在原有七阶段规划基础上，Update1 新增了一个贯穿始终的迭代循环：



```Plain Text
发现问题 → 回测验证 → 诊断定位 → 修复实施 → 观察效果 → 再次验证 → 循环
```



此循环已应用于止损参数优化（P0\-2）、震荡市评分修复（P0\-1/P1\-1）、趋势门槛调整（P2\-3）、ATR仓位（P2\-2）等多项优化。后续的IC因子分析也将遵循此循环。





## 9\.2 已执行阶段详细清单



### ✅ 阶段一至六（基础设施）



|阶段|关键产出|验收|
|---|---|---|
|阶段一|Docker环境、CLS日志、COS备份、UFW防火墙、目录结构|✅|
|阶段二|FastAPI代理部署（端口8081）、解决Hermes不暴露HTTP端点的问题|✅|
|阶段三|OpenClaw SOUL/USER/MEMORY配置|✅|
|阶段3\.5|Main/Signal/Risk三Agent路由配置|✅|
|阶段四|Redis \+ PostgreSQL \+ Grafana 三容器部署|✅|
|阶段五|Decision Gateway部署（Kelley/熔断/风控裁决）|✅|
|阶段六|Cron定时任务（健康检查/备份/日志清理）、Telegram告警、Grafana双仪表盘、Nginx反向代理|✅|



### ✅ 阶段七（策略层开发与纸上谈兵）



|模块|文件|状态|
|---|---|---|
|全局配置|`config\.py`|✅|
|行情获取|`data\_fetcher\.py`|✅|
|选币雷达|`coin\_selector\.py`|✅|
|环境分类|`environment\.py`|✅|
|入场信号|`entry\_signal\.py`|✅|
|信号分发|`signal\_dispatcher\.py`|✅|
|主引擎|`strategy\_runner\.py`|✅|
|卖出管理|`sell\_manager\.py`|✅|
|状态管理|`state\_manager\.py`|✅|
|信号入库|`signal\_logger\.py`|✅|
|微观结构|`microstructure\.py`|✅|



### ✅ 模型与环境稳定性修复



|问题|解决方案|状态|
|---|---|---|
|aiapi\.world欠费|全量迁移至DeepSeek官方API|✅|
|`deepseek\-v4\-pro`思考模式兼容性|统一使用`deepseek\-chat`后切换至`deepseek\-v4\-pro`|✅|
|OpenClaw Gateway反复崩溃|禁用bonjour插件|✅|
|`tools\.profile`导致API拒绝|精简为`none`|✅|
|DeepSeek插件冲突|禁用deepseek插件|✅|
|OpenClaw升级后持续崩溃|强制重装\+`gateway install \-\-force`|✅|
|Grafana外网无法访问|Nginx反向代理\+腾讯云防火墙|✅|
|Nginx端口占用|`pkill \-9 nginx`后重启|✅|
|FastAPI代理两次意外退出|systemd托管，自动重启验证通过|✅|
|飞书通道不稳定|复杂任务改用OpenClaw面板|✅|
|aiapi\-claude余额不足|切换回deepseek\-v4\-pro|✅|



### ✅ 线上部署参数优化



|编号|内容|回测依据|部署状态|
|---|---|---|---|
|P0\-1|震荡市禁止A级|A级胜率32%低于B级42%|✅|
|P0\-2|止损2\.0%→1\.5%|排除DRAW后胜率不降|✅|
|P1\-1|区间位置反转|Range胜率\+3\.5pp|✅|
|P1\-4|6小时冷却期|68\.9%信号重叠|✅|
|P2\-1|Range仓位减半|Trend期望7倍于Range|✅|
|P2\-2|ATR动态仓位|总R提升59%|✅|
|P2\-3|趋势门槛10分|C级胜率43\.7%|✅|
|—|微结构过滤修复|Bug修复|✅|
|—|OBI阈值0\.2→0\.12|提升通过率|✅|
|—|趋势保护止盈|O\-20|✅|
|—|成交方向连续性|O\-21|✅|
|—|信号入库np\.float64修复|Bug修复|✅|
|—|等级过滤Bug修复|should\_send覆盖修复|✅|





## 9\.3 待完成事项（按优先级）



### 🔴 P0：立即执行（4项）



|编号|内容|来源|改动位置|
|---|---|---|---|
|**FIX\-1**|信号入库status匹配修复|诊断报告P0\-1|`strategy\_runner\.py` L91|
|**FIX\-2**|health\_check增加策略引擎存活检测|诊断报告P0\-4|`health\_check\.sh`|
|**FIX\-3**|Gateway OBI阈值同步0\.20→0\.12|诊断报告P0\-2|`system\_config\.json`|
|**FIX\-4**|flow\_ratio调低1\.5→1\.2|诊断报告P0\-3|`strategy\_runner\.py` L38|



### 🟠 P1：短期执行（2项）



|编号|内容|来源|
|---|---|---|
|**FIX\-5**|成交方向连续性阈值微调|诊断报告P1\-3|
|**FIX\-6**|ADX区间ADX\&lt;20→23判为range|诊断报告P2\-4|



### 🟡 P1\-P2：验证环节



|编号|内容|优先级|
|---|---|---|
|**DX\-1\-FUP**|IC分析验证（动量/波动压缩/均值回归三因子）|P0（与FIX并行）|
|**P2\-4**|静态币种权重（SOL 1\.5×/BTC 1\.0×/ETH 0×）|P1（IC通过后）|
|**P2\-5**|趋势门槛精准调为11分（可选）|P2|
|**P2\-6**|牛市过滤SHORT信号|P1（P2\-4之后）|



### 🔵 P2\-P3：长期规划



|编号|内容|触发时机|
|---|---|---|
|**O\-27**|ATR升级为相对ATR|P2\-6之后|
|**O\-23**|RL短期反馈通道|Hermes RL启用后|
|**P3\-1**|完全策略分治|长期|
|**P3\-2**|环境概率化|积累100\+信号后|
|**P3\-3**|极端行情实时监测|随时可启动|
|**O\-24**|环境概率化（概率模型）|数据积累后|
|**O\-25**|组合驱动升级|策略层/RL成熟后|
|**O\-26**|MDP参数标定|积累足够交易记录后|





## 9\.4 后续计划



### 阶段 7\.2：影子学习（待 7\.1 稳定后启动）

- Hermes 仅读取数据、输出权重日志，不向 Gateway 发送权重

- 验证 RL 学习逻辑是否正常

    

### 阶段 7\.3：小额试水（待 7\.2 通过后启动）

- 总风险降至 0\.2%，投入小额真钱（如 100\-500 U）

- 重点观察滑点、成交速度、极端行情熔断

    

### 阶段 7\.4：正式启航（待 7\.3 通过后启动）

- 恢复 2% 风险上限

- 逐步放大资金量

    

### Update1 新增：因子验证路线图



```Plain Text
IC分析（动量/波动压缩/均值回归）
    ↓
  通过 → 用因子模型替代entry_signal.py
    ↓
  验证Mode B资金曲线回测
    ↓
  部署P2-4静态币种权重
    ↓
  推进P2-6牛市过滤
    ↓
  未通过 → 回退修补22分制（HOLD-1/HOLD-2）
```





## 9\.5 明确排除事项



|编号|内容|拒绝原因|
|---|---|---|
|R\-1|引入Risk Kernel v2/v3/v4|Gateway已实现Kelly/风险预算/熔断|
|R\-2|引入仓位竞争系统|Gateway已实现仓位计算|
|R\-3|引入Regime Engine|environment\.py已实现环境分类|
|R\-4|引入RL训练环境/Alpha Factory|单机资源不足|
|R\-5|引入遗传算法策略进化|本阶段过度设计|
|R\-6|引入Market World Model|研究级课题|
|R\-7|引入Kubernetes/Kafka/多进程架构|单机2核4G不适合|
|R\-8|扩展交易池至50个币|当前精简池已满足需求|
|R\-9|风险感知前置|违背单向控制流|
|R\-10|完整的环境概率化|数据基础不足|
|R\-11|组合风险上限策略层实现|Gateway应统一管理|
|REJ\-1|Redis初始化写入|Gateway已有兜底|





## 9\.6 全程优化调整清单（Update1 完整版）



### 架构与模型层面



|编号|内容|状态|
|---|---|---|
|A\-1|FastAPI代理替代Hermes HTTP API|✅|
|A\-2|Hermes Dashboard保留用于技能沉淀|✅|
|M\-1|aiapi\.world→DeepSeek官方API|✅|
|M\-2|deepseek\-v4\-pro→deepseek\-chat→deepseek\-v4\-pro|✅|
|M\-3|OpenClaw升级至v2026\.4\.24|✅|
|M\-4|禁用bonjour插件|✅|
|M\-5|tools\.profile精简为none|✅|
|M\-6|禁用deepseek插件|✅|
|M\-7|FastAPI代理systemd化|✅|



### 策略层优化



|编号|内容|状态|
|---|---|---|
|S\-1\~S\-10|数据源统一、五因子选币、22分制非随机、stability字段、等级过滤等|✅|
|E\-1\~E\-9|SHORT止损、跟踪止盈、回撤止盈、冷却期、趋势保护止盈等|✅|
|F\-1\~F\-6|微结构过滤四层校验、OBI阈值调整、成交方向连续性等|✅|



### 参数与风控优化



|编号|内容|状态|
|---|---|---|
|P0\-1|震荡市禁止A级|✅|
|P0\-2|止损1\.5%|✅|
|P1\-1|区间位置反转|✅|
|P1\-4|6小时冷却期|✅|
|P2\-1|Range仓位减半|✅|
|P2\-2|ATR动态仓位|✅|
|P2\-3|趋势门槛10分|✅|



### 监控与可视化



|编号|内容|状态|
|---|---|---|
|V\-1\~V\-8|双仪表盘、资源监控、Nginx反代、信号入库、np\.float64修复等|✅|



### 回测系统



|编号|内容|状态|
|---|---|---|
|BT\-1\~BT\-12|回测引擎搭建、止损敏感性测试、三币种对比、DX\-1信号alpha验证、Mode B验证等|✅|



### Update1 新增待办



|编号|内容|状态|
|---|---|---|
|FIX\-1\~FIX\-6|诊断报告修复项|🔜 待执行|
|DX\-1\-FUP|IC因子分析验证|🔜 待执行|
|HOLD\-1\~HOLD\-3|暂缓项（等IC结果）|🔜 条件触发|





# 第十章：V4\.6\.2\-Optimized\-Update1 新增内容



## 10\.1 版本演进概述



|版本|日期|主要变化|
|---|---|---|
|V4\.6\.2|2026\-04\-26|原始方案，八章分层架构|
|V4\.6\.2\-Optimized|2026\-04\-27|融入O\-16\~O\-25优化、回测修正、止损调整、震荡市禁止A级|
|**V4\.6\.2\-Optimized\-Update1**|**2026\-05\-01**|**两份诊断报告后的修复清单、三币种回测完整数据、DX\-1信号alpha验证、Mode B回测验证、因子驱动验证路线图、systemd运维修复**|



> **Update1 核心变化**：从\&\#34;策略层优化为主\&\#34;转向\&\#34;信号链路修复 \+ 因子验证双轨并行\&\#34;。发现22分制评分系统不具备预测区分力，启动IC因子分析路线。同时修复信号入库Bug、引擎监控缺失、微结构参数不一致等运维链问题。
> 
> 





## 10\.2 回测系统完整数据



### 10\.2\.1 三币种完整回测对比



|指标|BTC|ETH|SOL|
|---|---|---|---|
|去重后信号|403|532|486|
|排除DRAW胜率|41\.6%|39\.4%|41\.4%|
|总R|\+15\.00|\-36\.50|\+83\.50|
|平均R/笔|\+0\.037|\-0\.014|\+0\.035|
|Trend胜率|42\.3%|45\.9%|49\.4%|
|Trend期望|\+0\.058R|\+0\.148R|\+0\.234R|
|Range胜率|37\.1%|36\.6%|40\.4%|
|Range期望|\-0\.073R|\-0\.085R|\-0\.022R|



**核心结论**：SOL是最优币种（总R \+83\.50，Trend胜率49\.4%），ETH是唯一负期望币种（总R \-36\.50），BTC居中。14分是高价值信号分水岭（SOL/ETH 14分\+胜率均超77%）。



### 10\.2\.2 DX\-1 信号Alpha验证（固定持仓回测）



|持仓时长|准确率|平均收益|判断|
|---|---|---|---|
|24h|49\.0%|\+0\.177%|约等于抛硬币|
|48h|50\.1%|\+0\.095%|微弱偏正|
|72h|48\.1%|\-0\.117%|反向偏|



**核心结论**：22分制评分系统不具备方向预测力，准确率在48%\-50%之间浮动。唯一有微弱价值的子场景是Trend环境（53\.3%准确率）。



### 10\.2\.3 Mode B 多币种资金曲线回测



**优化版（ETH\+SOL/仅Trend/≥12分）**：最终权益\-$92\.5%（🔴 灾难），胜率35\.0%，唯一正期望信号组合（SOL LONG 12\-13分，\+0\.250R/笔）仅8笔样本。诊断结论：信号本身缺乏方向预测能力，固定止损止盈机制压制了仅有微弱预测力的信号。





## 10\.3 两份系统诊断报告核心发现



### 第一份报告（完整代码诊断）



|发现|严重度|
|---|---|
|22分制评分系统不具备预测区分力|🔴 致命|
|止损止盈固定1\.5%/3\.0%一刀切|🔴 致命|
|成交量评分代码死路径（`if False`）|🟠 严重|
|扫单检测被禁用（`return False`）|🟠 严重|
|Gateway Kelly胜率假设45% vs 实际35%|🟠 严重|
|Range评分与方向无关|🟡 中等|



### 第二份报告（Claude深度诊断）



|发现|严重度|
|---|---|
|策略引擎4月30日15:06后长期停机|🔴 P0|
|信号入库status不匹配（SUCCESS vs APPROVED）|🔴 P0|
|微观结构拦截率过高（\~64%）|🟠 P1|
|方向判定偏空（SHORT占72%）|🟠 P1|
|双套OBI阈值不一致（代码0\.12 vs Gateway 0\.20）|🟠 P1|
|health\_check不监控策略引擎进程|🟡 P2|





## 10\.4 基于诊断结果的修复清单



|编号|内容|来源|优先级|状态|
|---|---|---|---|---|
|FIX\-1|信号入库status匹配|第二份报告P0\-1|P0|🔜|
|FIX\-2|health\_check增加引擎监控|第二份报告P0\-4|P0|🔜|
|FIX\-3|Gateway OBI阈值同步|第二份报告P0\-2|P0|🔜|
|FIX\-4|flow\_ratio调低1\.5→1\.2|第二份报告P0\-3|P0|🔜|
|FIX\-5|成交方向连续性阈值微调|第二份报告P1\-3|P1|🔜|
|FIX\-6|ADX区间细化20→23|第二份报告P2\-4|P1|🔜|



### 暂缓项



|编号|内容|暂缓原因|触发时机|
|---|---|---|---|
|HOLD\-1|方向判定对称性检查|与因子验证冲突|IC分析后|
|HOLD\-2|放宽A级门槛|评分体系可能被替换|IC分析后|
|HOLD\-3|修复成交量死路径|评分体系可能被替换|IC分析后|





## 10\.5 因子驱动验证路线图



### 10\.5\.1 目标



验证三个基础因子是否比22分制评分系统具有更强的方向预测力。若验证通过，用因子模型替代`entry\_signal\.py`的核心评分逻辑。



### 10\.5\.2 待验证因子



|因子|计算方式|验证目标|
|---|---|---|
|**动量因子**|过去12根1h K线收益率|趋势延续性预测力|
|**波动压缩因子**|近20根K线振幅标准差|低波动后方向性突破|
|**均值回归因子**|当前价格相对20周期均线偏离度|极端偏离后的回归倾向|



### 10\.5\.3 验证方法



每个因子计算IC值（当前因子值与未来N根K线收益率之间的相关系数）。通过标准：IC \&gt; 0\.02，Rank IC \&gt; 0\.03，连续为正。



### 10\.5\.4 决策路径



```Plain Text
三个因子IC分析 → 至少一个通过 → 用因子模型替代22分制 → Mode B回测验证 → 部署
                 → 全部未通过 → 回退修补22分制（HOLD-1/HOLD-2）→ 重新评估策略方向
```





## 10\.6 当前线上参数快照



|参数|当前值|改动来源|更新时间|
|---|---|---|---|
|`STOP\_LOSS\_FIXED`|**1\.5%**|P0\-2|2026\-04\-28|
|震荡市A级上限|**15分（最高B级）**|P0\-1|2026\-04\-28|
|区间位置加分|**中央\+6 / 边缘\+2**|P1\-1|2026\-04\-28|
|OBI阈值（代码）|**0\.12**|前期P0修复|2026\-04\-28|
|OBI阈值（Gateway配置）|**0\.20**（待同步）|FIX\-3|🔜|
|flow\_ratio|**1\.5**（待调为1\.2）|FIX\-4|🔜|
|同向信号冷却期|**6小时**|P1\-4|2026\-04\-28|
|Range环境仓位权重|**0\.5×Trend**|P2\-1|2026\-04\-28|
|ATR动态仓位因子|**0\.5\~1\.5倍**，base\_atr=1\.0%|P2\-2|2026\-04\-28|
|趋势B级门槛|**10分**（独立于震荡市）|P2\-3|2026\-04\-28|
|震荡市B级门槛|**13分**|原参数|—|
|成交方向连续性LONG|**0\.55**（待调为0\.52）|FIX\-5|🔜|
|成交方向连续性SHORT|**0\.45**（待调为0\.48）|FIX\-5|🔜|
|ADX Range阈值|**\&lt;20**（待调为\&lt;23）|FIX\-6|🔜|
|信号入库status匹配|**仅匹配\&\#34;SUCCESS\&\#34;**（待修复）|FIX\-1|🔜|
|健康检查引擎监控|**未覆盖**（待修复）|FIX\-2|🔜|
|模型|**deepseek\-v4\-pro**|M\-2|2026\-05\-01|
|FastAPI代理|**systemd托管**，自动重启|M\-7|2026\-04\-30|



---



## 📚 V4\.6\.2\-Optimized\-Update1 全指南统计



|部分|包含章节|主要内容|
|---|---|---|
|第一部分|第一章、第二章|系统总览、架构蓝图、策略层（选币、环境、评分、三周期信号、微观结构）|
|第二部分|第三章、第四章|决策与风控层（Final Risk Gate、资金分配、动态仓位、熔断体系）|
|第三部分|第五章、第六章|执行层（多Agent、Cron规范、会话管理）、学习层（Hermes RL、技能沉淀、记忆系统、压缩配置、Monte Carlo）|
|第四部分|第七章、第八章、第九章、第十章|部署运维（Docker Compose、冷启动、监控、成本优化）、附录（配置参数、API、铁律口诀）、执行落地计划、Update1新增内容|



