Reading additional input from stdin...
2026-06-12T07:07:48.608345Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
OpenAI Codex v0.139.0
--------
workdir: /Users/yaomingyu/Documents/AI_QUANT_COMPANY
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR] (network access enabled)
reasoning effort: high
reasoning summaries: none
session id: 019ebaa8-8c03-7423-8bfc-e36d6db5291c
--------
user
执行任务书 04_AI_TEAM/CODEX_TASKS/TASK_UNIVERSE_PIT.md。先读任务书与项目根 AGENTS.md。只建数据资产，严守禁止项。
2026-06-12T07:08:05.218881Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
codex
我会先读取根目录 `AGENTS.md` 和任务书，确认验收标准、禁止项和允许产出范围，再决定是否可以直接执行。
exec
/bin/bash -lc "sed -n '1,260p' 04_AI_TEAM/CODEX_TASKS/TASK_UNIVERSE_PIT.md" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "sed -n '1,240p' AGENTS.md" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc 'pwd && ls' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
/Users/yaomingyu/Documents/AI_QUANT_COMPANY
00_PROJECT_MANAGEMENT
01_MEMORY_CORE
02_KNOWLEDGE_BASE
03_RAW_INBOX
04_AI_TEAM
05_ARCHITECTURE
06_RESEARCH
07_TRADING_SYSTEM
08_DEPLOYMENT
09_OPERATIONS
10_COST_TRACKING
99_TEMP
AGENTS.md
BTC_USDT_4H.csv
CLAUDE.md
ETH_USDT_4H.csv
SOL_USDT_4H.csv

 succeeded in 0ms:


# AGENTS.md — AI Quant Company（Codex 实现会话指令）

## 角色
你是本项目的实现工程师（Codex），对交付代码与报告的**正确性、可复现性、可审计性**负责。判断与方向由 Claude（主理人/CTO）负责，但你有**专业异议义务**：发现任务书的研究方向/方法有问题，先暂停并书面提异议，不机械执行（DEC-057）。

## 任务执行前强制自查（七问精简版）
①任务在验证什么机制？②任务书的验收标准是否可量化？③有无更便宜的等效实现？④是否会触碰禁止项（见下）？任一答不出 → 先问 Claude，不要猜。

## 研究铁律（Research Protocol v1.2 + v1.3 增补件，冲突以 v1.3 为准）
1. 单变量原则；预登记后禁改假设；结论诚实（禁"有一定效果"类模糊话）。
2. 成本完整：手续费 0.1%/边 + 滑点 0.1%（保守）+ 真实 funding。**事件类策略必须加报滑点 0.3/0.5/1.0% 压力档**（v1.3 §二）。
3. 验收四件套（v1.3 §一）：E[R]>0 / 赢亏比≥1.5 / 分档爆仓概率（块bootstrap，seed预登记）/ 年化log增长>0 且分年正期望占多数。旧门槛（Sharpe>1、MaxDD<25%、Expectancy>1.0）作废。
4. WF 硬门槛；Holdout 物理封存、**任何情况下不得读取**；事件研究按 v1.3 §四（episode<300 用池化+单调性，禁60/20/20三分）。
5. 阈值/分位数一律滚动或扩张窗口计算，**禁全样本分位（前视）**。

## 工程纪律
- 库纪律（DEC-061）：Triple Barrier/Purged CV/统计校验等一律**自实现为可审计小函数**；MLFinPy 等仅作参考，不引硬依赖。
- 持仓/状态以 DB 或文件为权威，禁内存字典（V4 事故根因）。
- 输出三件套：CODE（可复跑脚本+固定seed）/ RESULTS（结论md）/ CODEX_TASKS 执行报告（含验收标准逐条自检）。
- 数据：只读 06_RESEARCH/DATA/；产出写 06_RESEARCH/CODE/output/；UTC 时间戳。
- git：每任务完成 commit，message 带任务号。

## 禁止项
碰 Holdout｜改预登记文档｜简化成本模型｜全样本分位｜引入不可审计黑箱依赖｜把失败结果写成"部分成功"｜超任务书范围"顺手优化"。

## 交接
完成后报告写入 04_AI_TEAM/CODEX_TASKS/REPORT_[任务号].md，等 Claude 验收；验收不过按退回意见修，不抗辩成本与工期以外的验收标准。

 succeeded in 0ms:
# TASK_UNIVERSE_PIT —— 无幸存者偏差 universe 数据资产（DEC-069③前置，双审计收敛项2）

**发包：** Claude 2026-06-12 ｜ **用途：** TSMOM 扩样本与 A-4 普查共用（一鱼两吃）。**只建数据资产，不跑任何策略/统计。**

## 交付物
1. `06_RESEARCH/DATA/UNIVERSE_PIT.csv`：Binance USDT-M 永续**全量历史合约**（含已退市/已下架），列=symbol, onboard_date, delist_date(空=仍在市), 数据来源标注。来源用官方（exchangeInfo API + data.binance.vision 归档目录枚举交叉验证）；网络经 `export HTTPS_PROXY=http://127.0.0.1:7897`。
2. `06_RESEARCH/CODE/build_universe_pit.py`（可复跑）+ 简短 pytest（point-in-time 查询函数：给定日期返回当日可交易集合，验证不含未来上市/已退市合约——无前视）。
3. 报告 `04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md`：总数/退市数/各年度上市数表、与现有 9 品种覆盖对照、数据缺口与来源可信度说明、复算命令。

## 验收
退市合约必须出现在清单中（抽查 ≥3 个已知退市永续）；PIT 查询测试过；UTC；不改既有文件；不碰 HOLDOUT；不 git commit。

2026-06-12T07:08:15.211602Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
codex
任务边界清楚：这是 PIT universe 数据资产，不验证策略机制；验收项可量化；最低成本实现是官方 `exchangeInfo` 加 `data.binance.vision` 目录枚举；会避开 HOLDOUT、策略统计、全样本分位和 git commit。接下来我会先看现有目录结构和数据命名，避免改动既有文件。
exec
/bin/bash -lc "find 06_RESEARCH/DATA -maxdepth 3 -type f | sort | sed -n '1,240p'" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/DATA/DATA_HASHES_20260610.txt
06_RESEARCH/DATA/DATA_HASHES_20260611.txt
06_RESEARCH/DATA/FUTURES/ADAUSDT_FUNDING_8H.csv
06_RESEARCH/DATA/FUTURES/ADAUSDT_MARK_4H.csv
06_RESEARCH/DATA/FUTURES/BNBUSDT_FUNDING_8H.csv
06_RESEARCH/DATA/FUTURES/BNBUSDT_MARK_4H.csv
06_RESEARCH/DATA/FUTURES/BTCUSDT_FUNDING_8H.csv
06_RESEARCH/DATA/FUTURES/BTCUSDT_FUNDING_8H_2026H1.csv
06_RESEARCH/DATA/FUTURES/BTCUSDT_MARK_1H.csv
06_RESEARCH/DATA/FUTURES/BTCUSDT_MARK_1H_2026H1.csv
06_RESEARCH/DATA/FUTURES/BTCUSDT_MARK_4H.csv
06_RESEARCH/DATA/FUTURES/BTCUSDT_MARK_4H_2026H1.csv
06_RESEARCH/DATA/FUTURES/BTCUSDT_METRICS_5M.csv
06_RESEARCH/DATA/FUTURES/DOGEUSDT_FUNDING_8H.csv
06_RESEARCH/DATA/FUTURES/DOGEUSDT_MARK_4H.csv
06_RESEARCH/DATA/FUTURES/ETHUSDT_FUNDING_8H.csv
06_RESEARCH/DATA/FUTURES/ETHUSDT_FUNDING_8H_2026H1.csv
06_RESEARCH/DATA/FUTURES/ETHUSDT_MARK_1H.csv
06_RESEARCH/DATA/FUTURES/ETHUSDT_MARK_1H_2026H1.csv
06_RESEARCH/DATA/FUTURES/ETHUSDT_MARK_4H.csv
06_RESEARCH/DATA/FUTURES/ETHUSDT_MARK_4H_2026H1.csv
06_RESEARCH/DATA/FUTURES/ETHUSDT_METRICS_5M.csv
06_RESEARCH/DATA/FUTURES/LTCUSDT_FUNDING_8H.csv
06_RESEARCH/DATA/FUTURES/LTCUSDT_MARK_4H.csv
06_RESEARCH/DATA/FUTURES/SOLUSDT_FUNDING_8H.csv
06_RESEARCH/DATA/FUTURES/SOLUSDT_FUNDING_8H_2026H1.csv
06_RESEARCH/DATA/FUTURES/SOLUSDT_MARK_1H.csv
06_RESEARCH/DATA/FUTURES/SOLUSDT_MARK_1H_2026H1.csv
06_RESEARCH/DATA/FUTURES/SOLUSDT_MARK_4H.csv
06_RESEARCH/DATA/FUTURES/SOLUSDT_MARK_4H_2026H1.csv
06_RESEARCH/DATA/FUTURES/SOLUSDT_METRICS_5M.csv
06_RESEARCH/DATA/FUTURES/XRPUSDT_FUNDING_8H.csv
06_RESEARCH/DATA/FUTURES/XRPUSDT_MARK_4H.csv
06_RESEARCH/DATA/HOLDOUT/a2_events_holdout.csv

exec
/bin/bash -lc 'find 06_RESEARCH -maxdepth 3 -type d | sort' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH
06_RESEARCH/CODE
06_RESEARCH/CODE/.pytest_cache
06_RESEARCH/CODE/.pytest_cache/v
06_RESEARCH/CODE/__pycache__
06_RESEARCH/CODE/features
06_RESEARCH/CODE/features/__pycache__
06_RESEARCH/CODE/output
06_RESEARCH/CODE/tests
06_RESEARCH/CODE/tests/__pycache__
06_RESEARCH/DATA
06_RESEARCH/DATA/FUTURES
06_RESEARCH/DATA/HOLDOUT
06_RESEARCH/DATA/LIQUIDATIONS
06_RESEARCH/HYPOTHESES
06_RESEARCH/RESULTS

exec
/bin/bash -lc "find 06_RESEARCH/CODE -maxdepth 3 -type f | sort | sed -n '1,240p'" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/CODE
btc_eth_trades.csv
06_RESEARCH/CODE/output/v6b_strategy_baseline_equity_curve.csv
06_RESEARCH/CODE/output/v6b_strategy_baseline_equity_curve.png
06_RESEARCH/CODE/output/v6b_strategy_baseline_trades.csv
06_RESEARCH/CODE/output/v6b_strategy_btc_eth_trades.csv
06_RESEARCH/CODE/output/v6b_strategy_equity_curve.csv
06_RESEARCH/CODE/output/v6b_strategy_equity_curve.png
06_RESEARCH/CODE/output/v6b_strategy_metrics.json
06_RESEARCH/CODE/output/v6b_strategy_stop_tightening_distribution.png
06_RESEARCH/CODE/output/v6b_strategy_trades.csv
06_RESEARCH/CODE/output/vol_compression_charts.png
06_RESEARCH/CODE/output/vol_compression_event_groups.csv
06_RESEARCH/CODE/output/vol_compression_metrics.json
06_RESEARCH/CODE/p1_01_tsmom.py
06_RESEARCH/CODE/p1_02_tsmom_voltarget.py
06_RESEARCH/CODE/p1_03_data_recon_oi.py
06_RESEARCH/CODE/p1_04_tsmom_regime_long.py
06_RESEARCH/CODE/p1_05_regime_attribution.py
06_RESEARCH/CODE/p1_06_tsmom_macro_bull.py
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py
06_RESEARCH/CODE/signal_detector.py
06_RESEARCH/CODE/sweep_dual_regime.py
06_RESEARCH/CODE/sweep_event_study.py
06_RESEARCH/CODE/test_backtest_rules.py
06_RESEARCH/CODE/tests/README_TESTS.md
06_RESEARCH/CODE/tests/__pycache__/test_a2_event_study.cpython-313-pytest-9.0.3.pyc
06_RESEARCH/CODE/tests/__pycache__/test_backtest_rules.cpython-313-pytest-9.0.3.pyc
06_RESEARCH/CODE/tests/__pycache__/test_backtest_rules.cpython-313.pyc
06_RESEARCH/CODE/tests/__pycache__/test_p1_05_regime_attribution.cpython-313-pytest-9.0.3.pyc
06_RESEARCH/CODE/tests/__pycache__/test_p1_06_tsmom_macro_bull.cpython-313-pytest-9.0.3.pyc
06_RESEARCH/CODE/tests/__pycache__/test_tsmom_regime_long.cpython-313-pytest-9.0.3.pyc
06_RESEARCH/CODE/tests/__pycache__/test_tsmom_regime_long.cpython-313.pyc
06_RESEARCH/CODE/tests/__pycache__/test_tsmom_rules.cpython-313-pytest-9.0.3.pyc
06_RESEARCH/CODE/tests/__pycache__/test_tsmom_rules.cpython-313.pyc
06_RESEARCH/CODE/tests/__pycache__/test_tsmom_voltarget.cpython-313-pytest-9.0.3.pyc
06_RESEARCH/CODE/tests/__pycache__/test_tsmom_voltarget.cpython-313.pyc
06_RESEARCH/CODE/tests/test_a2_event_study.py
06_RESEARCH/CODE/tests/test_backtest_rules.py
06_RESEARCH/CODE/tests/test_p1_05_regime_attribution.py
06_RESEARCH/CODE/tests/test_p1_06_tsmom_macro_bull.py
06_RESEARCH/CODE/tests/test_tsmom_regime_long.py
06_RESEARCH/CODE/tests/test_tsmom_rules.py
06_RESEARCH/CODE/tests/test_tsmom_voltarget.py
06_RESEARCH/CODE/v10_wider_stop_backtest.py
06_RESEARCH/CODE/v11_regime_v3_event_study.py
06_RESEARCH/CODE/v12_no_stop_diagnostic.py
06_RESEARCH/CODE/v14_mae_analysis.py
06_RESEARCH/CODE/v15_vol_compression.py
06_RESEARCH/CODE/v16_atr_stop_diagnostic.py
06_RESEARCH/CODE/v17_atr_strategy.py
06_RESEARCH/CODE/v18_volpos_strategy.py
06_RESEARCH/CODE/v4_bootstrap_validation.py
06_RESEARCH/CODE/v4_strategy_backtest.py
06_RESEARCH/CODE/v8_time_sensitivity.py
06_RESEARCH/CODE/v9_stop_diagnostic.py

exec
/bin/bash -lc 'git status --short' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
 M 99_TEMP/codex_carry.log
 M 99_TEMP/codex_universe.log

codex
现有工作区已有无关改动在 `99_TEMP`，我不会碰。刚才只做了目录层级和文件名级别盘点；后续不会读取 `06_RESEARCH/DATA/HOLDOUT` 下任何内容。
exec
/bin/bash -lc "sed -n '1,180p' 06_RESEARCH/CODE/tests/test_tsmom_rules.py" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "sed -n '1,220p' 06_RESEARCH/CODE/prepare_tsmom_expansion_data.py" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "sed -n '1,240p' 06_RESEARCH/CODE/download_futures_data.py" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
"""Prepare data-only TSMOM expansion files and QC report.

This task intentionally performs no backtest, signal generation, or
statistical test. It only downloads Binance official archives, writes new
CSV files, and validates the resulting data files.
"""

from __future__ import annotations

import argparse
import hashlib
import http.client
import json
import time
from concurrent.futures import ThreadPoolExecutor, as_completed
from dataclasses import dataclass
from datetime import date, datetime, timezone
from io import BytesIO
from pathlib import Path
from typing import Iterable
from urllib.error import HTTPError, URLError
from urllib.request import urlopen
from urllib.parse import urlencode
from zipfile import ZipFile

import pandas as pd


PROJECT_ROOT = Path(__file__).resolve().parents[2]
FUTURES_DIR = PROJECT_ROOT / "06_RESEARCH/DATA/FUTURES"
REPORT_PATH = PROJECT_ROOT / "04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md"
OLD_HASH_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/DATA_HASHES_20260610.txt"
NEW_HASH_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/DATA_HASHES_20260611.txt"

BASE_URL = "https://data.binance.vision/data/futures/um"
FUNDING_API_URL = "https://fapi.binance.com/fapi/v1/fundingRate"
NEW_SYMBOLS = ["BNBUSDT", "XRPUSDT", "DOGEUSDT", "ADAUSDT", "LTCUSDT"]
EXISTING_SYMBOLS = ["BTCUSDT", "ETHUSDT", "SOLUSDT"]
MONTH_SCAN_START = "2019-09"
MONTH_SCAN_END = "2026-05"
DAILY_START = date(2026, 6, 1)
DAILY_END = date(2026, 6, 5)

MARK_COLUMNS = ["datetime", "open", "high", "low", "close", "volume"]
FUNDING_COLUMNS = ["datetime", "funding_interval_hours", "last_funding_rate"]


@dataclass(frozen=True)
class ArchiveJob:
    symbol: str
    dataset: str
    timeframe: str
    period: str
    frequency: str
    required: bool


@dataclass(frozen=True)
class FileSpec:
    path: Path
    dataset: str
    timeframe: str
    expected_hours: int
    reference_path: Path


def archive_url(job: ArchiveJob) -> str:
    if job.dataset == "mark":
        return (
            f"{BASE_URL}/{job.frequency}/markPriceKlines/{job.symbol}/"
            f"{job.timeframe}/{job.symbol}-{job.timeframe}-{job.period}.zip"
        )
    return (
        f"{BASE_URL}/{job.frequency}/fundingRate/{job.symbol}/"
        f"{job.symbol}-fundingRate-{job.period}.zip"
    )


def download_archive(job: ArchiveJob, retries: int = 3) -> tuple[ArchiveJob, bytes | None]:
    url = archive_url(job)
    for attempt in range(1, retries + 1):
        try:
            with urlopen(url, timeout=40) as response:
                return job, response.read()
        except HTTPError as exc:
            if exc.code == 404 and not job.required:
                return job, None
            if attempt == retries:
                raise RuntimeError(f"HTTP {exc.code}: {url}") from exc
        except (TimeoutError, URLError, http.client.RemoteDisconnected, OSError) as exc:
            if attempt == retries:
                raise RuntimeError(f"download failed: {url}") from exc
        time.sleep(1.5 * attempt)
    raise AssertionError("unreachable")


def parse_archive(payload: bytes, dataset: str) -> pd.DataFrame:
    with ZipFile(BytesIO(payload)) as archive:
        member = archive.namelist()[0]
        raw_bytes = archive.read(member)

    frame = pd.read_csv(BytesIO(raw_bytes))
    if dataset == "mark":
        if "open_time" not in frame.columns:
            frame = pd.read_csv(
                BytesIO(raw_bytes),
                header=None,
                names=[
                    "open_time",
                    "open",
                    "high",
                    "low",
                    "close",
                    "volume",
                    "close_time",
                    "quote_volume",
                    "count",
                    "taker_buy_volume",
                    "taker_buy_quote_volume",
                    "ignore",
                ],
            )
        return frame[["open_time", "open", "high", "low", "close", "volume"]]

    if "calc_time" not in frame.columns:
        frame = pd.read_csv(
            BytesIO(raw_bytes),
            header=None,
            names=["calc_time", "funding_interval_hours", "last_funding_rate"],
        )
    return frame[["calc_time", "funding_interval_hours", "last_funding_rate"]]


def parse_mixed_epoch(values: pd.Series) -> pd.Series:
    numeric = pd.to_numeric(values, errors="raise")
    milliseconds = numeric < 10**15
    parsed = pd.Series(pd.NaT, index=numeric.index, dtype="datetime64[ns]")
    parsed.loc[milliseconds] = pd.to_datetime(
        numeric.loc[milliseconds], unit="ms", utc=True
    ).dt.tz_localize(None)
    parsed.loc[~milliseconds] = pd.to_datetime(
        numeric.loc[~milliseconds], unit="us", utc=True
    ).dt.tz_localize(None)
    return parsed


def month_periods() -> list[str]:
    return [str(period) for period in pd.period_range(MONTH_SCAN_START, MONTH_SCAN_END, freq="M")]


def day_periods() -> list[str]:
    return [
        day.strftime("%Y-%m-%d")
        for day in pd.date_range(DAILY_START, DAILY_END, freq="D").date
    ]


def download_jobs(jobs: Iterable[ArchiveJob]) -> dict[tuple[str, str, str], list[pd.DataFrame]]:
    jobs = list(jobs)
    collected: dict[tuple[str, str, str], list[pd.DataFrame]] = {}
    seen_periods: dict[tuple[str, str, str, str], list[str]] = {}
    with ThreadPoolExecutor(max_workers=12) as executor:
        futures = {executor.submit(download_archive, job): job for job in jobs}
        for count, future in enumerate(as_completed(futures), start=1):
            job, payload = future.result()
            if payload is not None:
                key = (job.symbol, job.dataset, job.timeframe)
                collected.setdefault(key, []).append(parse_archive(payload, job.dataset))
                seen_key = (job.symbol, job.dataset, job.timeframe, job.frequency)
                seen_periods.setdefault(seen_key, []).append(job.period)
            if count % 25 == 0 or count == len(jobs):
                print(f"Downloaded/checked {count}/{len(jobs)} archives", flush=True)

    missing_after_start = []
    for symbol in NEW_SYMBOLS:
        for dataset in ["mark", "funding"]:
            key = (symbol, dataset, "4h", "monthly")
            seen = set(seen_periods.get(key, []))
            ordered = month_periods()
            first_index = next((idx for idx, month in enumerate(ordered) if month in seen), None)
            if first_index is None:
                missing_after_start.append(f"{symbol} {dataset} monthly: no archives found")
                continue
            missing = [month for month in ordered[first_index:] if month not in seen]
            if missing:
                missing_after_start.append(
                    f"{symbol} {dataset} monthly missing after first archive: {', '.join(missing)}"
                )
    if missing_after_start:
        raise RuntimeError("; ".join(missing_after_start))
    return collected


def utc_ms(day: date, end_of_day: bool = False) -> int:
    if end_of_day:
        stamp = datetime(day.year, day.month, day.day, 23, 59, 59, 999000, tzinfo=timezone.utc)
    else:
        stamp = datetime(day.year, day.month, day.day, 0, 0, 0, tzinfo=timezone.utc)
    return int(stamp.timestamp() * 1000)


def fetch_funding_api(symbol: str, retries: int = 3) -> pd.DataFrame:
    params = urlencode(
        {
            "symbol": symbol,
            "startTime": utc_ms(DAILY_START),
            "endTime": utc_ms(DAILY_END, end_of_day=True),
            "limit": 1000,
        }
    )
    url = f"{FUNDING_API_URL}?{params}"
    for attempt in range(1, retries + 1):
        try:
            with urlopen(url, timeout=40) as response:
                payload = response.read()
            rows = json.loads(payload.decode("utf-8"))
            frame = pd.DataFrame(rows)
            if frame.empty:
                return pd.DataFrame(columns=FUNDING_COLUMNS)
            frame["calc_time"] = pd.to_numeric(frame["fundingTime"], errors="raise")

 succeeded in 0ms:
"""Download Binance USD-M mark-price klines and funding-rate history."""

import argparse
from concurrent.futures import ThreadPoolExecutor, as_completed
from io import BytesIO
from pathlib import Path
from urllib.error import HTTPError
from urllib.request import urlopen
from zipfile import ZipFile

import pandas as pd


SYMBOL_START = {
    "BTCUSDT": "2020-01",
    "ETHUSDT": "2020-01",
    "SOLUSDT": "2020-09",
}
END_MONTH = "2026-05"
DEFAULT_TIMEFRAME = "4h"
BASE_URL = "https://data.binance.vision/data/futures/um/monthly"
PROJECT_ROOT = Path(__file__).resolve().parents[2]
OUTPUT_DIR = PROJECT_ROOT / "06_RESEARCH/DATA/FUTURES"


def archive_url(
    symbol: str, month: str, dataset: str, timeframe: str
) -> str:
    if dataset == "mark":
        return (
            f"{BASE_URL}/markPriceKlines/{symbol}/{timeframe}/"
            f"{symbol}-{timeframe}-{month}.zip"
        )
    return (
        f"{BASE_URL}/fundingRate/{symbol}/"
        f"{symbol}-fundingRate-{month}.zip"
    )


def download_archive(
    symbol: str, month: str, dataset: str, timeframe: str
) -> tuple[str, str, str, bytes]:
    url = archive_url(symbol, month, dataset, timeframe)
    try:
        payload = urlopen(url, timeout=30).read()
    except HTTPError as exc:
        raise RuntimeError(f"HTTP {exc.code}: {url}") from exc
    return symbol, month, dataset, payload


def parse_archive(payload: bytes, dataset: str) -> pd.DataFrame:
    with ZipFile(BytesIO(payload)) as archive:
        member = archive.namelist()[0]
        raw_bytes = archive.read(member)
    frame = pd.read_csv(BytesIO(raw_bytes))
    if dataset == "mark":
        if "open_time" not in frame.columns:
            frame = pd.read_csv(
                BytesIO(raw_bytes),
                header=None,
                names=[
                    "open_time",
                    "open",
                    "high",
                    "low",
                    "close",
                    "volume",
                    "close_time",
                    "quote_volume",
                    "count",
                    "taker_buy_volume",
                    "taker_buy_quote_volume",
                    "ignore",
                ],
            )
        return frame[
            ["open_time", "open", "high", "low", "close", "volume"]
        ]
    if "calc_time" not in frame.columns:
        frame = pd.read_csv(
            BytesIO(raw_bytes),
            header=None,
            names=[
                "calc_time",
                "funding_interval_hours",
                "last_funding_rate",
            ],
        )
    return frame[
        ["calc_time", "funding_interval_hours", "last_funding_rate"]
    ]


def parse_mixed_epoch(values: pd.Series) -> pd.Series:
    milliseconds = values < 10**15
    parsed = pd.Series(pd.NaT, index=values.index, dtype="datetime64[ns]")
    parsed.loc[milliseconds] = pd.to_datetime(
        values.loc[milliseconds], unit="ms"
    ).to_numpy()
    parsed.loc[~milliseconds] = pd.to_datetime(
        values.loc[~milliseconds], unit="us"
    ).to_numpy()
    return parsed


def parse_args() -> argparse.Namespace:
    parser = argparse.ArgumentParser()
    parser.add_argument("--timeframe", default=DEFAULT_TIMEFRAME)
    parser.add_argument(
        "--mark-only",
        action="store_true",
        help="Download mark-price klines without re-downloading funding.",
    )
    return parser.parse_args()


def main() -> None:
    args = parse_args()
    timeframe = args.timeframe.lower()
    timeframe_hours = int(timeframe.removesuffix("h"))
    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    jobs: list[tuple[str, str, str, str]] = []
    for symbol, start_month in SYMBOL_START.items():
        for month in pd.period_range(start_month, END_MONTH, freq="M"):
            month_text = str(month)
            jobs.append((symbol, month_text, "mark", timeframe))
            if not args.mark_only:
                jobs.append((symbol, month_text, "funding", timeframe))

    collected: dict[tuple[str, str], list[pd.DataFrame]] = {}
    with ThreadPoolExecutor(max_workers=10) as executor:
        futures = {
            executor.submit(download_archive, *job): job for job in jobs
        }
        for count, future in enumerate(as_completed(futures), start=1):
            symbol, month, dataset, payload = future.result()
            collected.setdefault((symbol, dataset), []).append(
                parse_archive(payload, dataset)
            )
            if count % 50 == 0 or count == len(jobs):
                print(f"Downloaded {count}/{len(jobs)} archives", flush=True)

    for symbol in SYMBOL_START:
        mark = pd.concat(collected[(symbol, "mark")], ignore_index=True)
        mark["datetime"] = parse_mixed_epoch(mark["open_time"])
        mark = (
            mark[["datetime", "open", "high", "low", "close", "volume"]]
            .sort_values("datetime")
            .drop_duplicates("datetime")
            .reset_index(drop=True)
        )
        mark_path = OUTPUT_DIR / f"{symbol}_MARK_{timeframe.upper()}.csv"
        mark.to_csv(mark_path, index=False, date_format="%Y-%m-%d %H:%M:%S")

        mark_gaps = (
            mark["datetime"].diff().dropna()
            != pd.Timedelta(hours=timeframe_hours)
        ).sum()
        if args.mark_only:
            print(
                f"{symbol}: mark={len(mark)} "
                f"({mark.datetime.iloc[0]} ~ {mark.datetime.iloc[-1]}), "
                f"mark_gaps={mark_gaps}"
            )
            continue

        funding = pd.concat(collected[(symbol, "funding")], ignore_index=True)
        funding["datetime"] = parse_mixed_epoch(funding["calc_time"])
        funding["datetime"] = funding["datetime"].dt.floor("s")
        funding = (
            funding[
                ["datetime", "funding_interval_hours", "last_funding_rate"]
            ]
            .sort_values("datetime")
            .drop_duplicates("datetime")
            .reset_index(drop=True)
        )
        funding_path = OUTPUT_DIR / f"{symbol}_FUNDING_8H.csv"
        funding.to_csv(
            funding_path, index=False, date_format="%Y-%m-%d %H:%M:%S"
        )

        funding_gaps = (
            funding["datetime"].diff().dropna() != pd.Timedelta(hours=8)
        ).sum()
        print(
            f"{symbol}: mark={len(mark)} "
            f"({mark.datetime.iloc[0]} ~ {mark.datetime.iloc[-1]}), "
            f"mark_gaps={mark_gaps}; funding={len(funding)} "
            f"({funding.datetime.iloc[0]} ~ {funding.datetime.iloc[-1]}), "
            f"funding_gaps={funding_gaps}"
        )


if __name__ == "__main__":
    main()

 succeeded in 0ms:
"""Synthetic tests for P1-01 TSMOM execution and funding rules."""

from pathlib import Path
import sys

import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from p1_01_tsmom import (
    execution_price,
    funding_payment,
    signal_from_prices,
)


def test_tsmom_signal_direction() -> None:
    assert signal_from_prices(110, 100) == 1
    assert signal_from_prices(90, 100) == -1
    assert signal_from_prices(100, 100) == 0


def test_long_and_short_execution_slippage() -> None:
    assert execution_price(100, 1, True) == pytest.approx(100.1)
    assert execution_price(100, 1, False) == pytest.approx(99.9)
    assert execution_price(100, -1, True) == pytest.approx(99.9)
    assert execution_price(100, -1, False) == pytest.approx(100.1)


def test_funding_direction_for_positive_rate() -> None:
    assert funding_payment(1, 2, 10_000, 0.0001) == pytest.approx(2.0)
    assert funding_payment(-1, 2, 10_000, 0.0001) == pytest.approx(-2.0)


def test_funding_direction_for_negative_rate() -> None:
    assert funding_payment(1, 2, 10_000, -0.0001) == pytest.approx(-2.0)
    assert funding_payment(-1, 2, 10_000, -0.0001) == pytest.approx(2.0)

exec
/bin/bash -lc "python3 --version && python3 - <<'PY'
import pandas as pd
print(pd.__version__)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 925ms:
3.0.3

codex
我在复用项目里已有的数据脚本风格：标准库下载、pandas 解析、测试放在 `06_RESEARCH/CODE/tests`，并保持“只生成新资产/报告”的边界。现在开始确认 Binance 官方 API 与归档目录的可枚举性。
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
import json
url='https://fapi.binance.com/fapi/v1/exchangeInfo'
with urlopen(url, timeout=30) as r:
    data=json.load(r)
print(len(data.get('symbols', [])))
for s in data.get('symbols', [])[:3]:
    print({k:s.get(k) for k in ['symbol','contractType','status','onboardDate','deliveryDate','quoteAsset','marginAsset']})
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
url='https://data.binance.vision/?delimiter=/&prefix=data/futures/um/monthly/klines/'
with urlopen(url, timeout=30) as r:
    b=r.read(1000)
print(b.decode('utf-8', errors='replace')[:1000])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
url='https://data.binance.vision/data/futures/um/monthly/klines/'
with urlopen(url, timeout=30) as r:
    b=r.read(1000)
print(b.decode('utf-8', errors='replace')[:1000])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
url='https://data.binance.vision/?delimiter=/&prefix=data/futures/um/monthly/markPriceKlines/'
with urlopen(url, timeout=30) as r:
    b=r.read(1000)
print(b.decode('utf-8', errors='replace')[:1000])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 2650ms:
<!DOCTYPE html>
<html>
<head>
  <title>Binance Data Collection</title>
  <link href="bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-dark bg-dark">
    <div class="container-md">
      <div class="row justify-content-between" style="width: 100%">
        <div class="col-sm">
          <a href="/"><img src="logo.png" height="30" class="d-inline-block align-top" alt=""></a>
          <span class="navbar-brand" style="padding-left: 10px;">Market Data</span>
        </div>
        <div class="col-sm" style="text-align: right" >
          <a href="https://github.com/binance/binance-public-data/" data-toggle="tooltip" title data-original-title="Public data document">
            <svg class="octicon octicon-mark-github v-align-middle" height="25" viewBox="0 0 16 16" version="1.1" width="25" aria-hidden="true" style="fill: #fff">
              <path fill-rule="evenodd" d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49

 succeeded in 3587ms:
<!DOCTYPE html>
<html>
<head>
  <title>Binance Data Collection</title>
  <link href="bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-dark bg-dark">
    <div class="container-md">
      <div class="row justify-content-between" style="width: 100%">
        <div class="col-sm">
          <a href="/"><img src="logo.png" height="30" class="d-inline-block align-top" alt=""></a>
          <span class="navbar-brand" style="padding-left: 10px;">Market Data</span>
        </div>
        <div class="col-sm" style="text-align: right" >
          <a href="https://github.com/binance/binance-public-data/" data-toggle="tooltip" title data-original-title="Public data document">
            <svg class="octicon octicon-mark-github v-align-middle" height="25" viewBox="0 0 16 16" version="1.1" width="25" aria-hidden="true" style="fill: #fff">
              <path fill-rule="evenodd" d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49

 exited 1 in 3897ms:
Traceback (most recent call last):
  File "<stdin>", line 3, in <module>
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 189, in urlopen
    return opener.open(url, data, timeout)
           ~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 495, in open
    response = meth(req, response)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 604, in http_response
    response = self.parent.error(
        'http', request, response, code, msg, hdrs)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 533, in error
    return self._call_chain(*args)
           ~~~~~~~~~~~~~~~~^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 466, in _call_chain
    result = func(*args)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 613, in http_error_default
    raise HTTPError(req.full_url, code, msg, hdrs, fp)
urllib.error.HTTPError: HTTP Error 404: Not Found

codex
`data.binance.vision` 的网页目录可访问，但顶层目录不是 S3 XML，而是 Binance 的 HTML 索引页；脚本需要解析页面链接而不是假设 bucket list。`exchangeInfo` 调用还在等待，我会单独确认是否是 API 侧超时或代理问题。
 succeeded in 37025ms:
792
{'symbol': 'BTCUSDT', 'contractType': 'PERPETUAL', 'status': 'TRADING', 'onboardDate': 1567965300000, 'deliveryDate': 4133404800000, 'quoteAsset': 'USDT', 'marginAsset': 'USDT'}
{'symbol': 'ETHUSDT', 'contractType': 'PERPETUAL', 'status': 'TRADING', 'onboardDate': 1574840700000, 'deliveryDate': 4133404800000, 'quoteAsset': 'USDT', 'marginAsset': 'USDT'}
{'symbol': 'BCHUSDT', 'contractType': 'PERPETUAL', 'status': 'TRADING', 'onboardDate': 1576745700000, 'deliveryDate': 4133404800000, 'quoteAsset': 'USDT', 'marginAsset': 'USDT'}

exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from html.parser import HTMLParser
from urllib.request import urlopen
class P(HTMLParser):
    def __init__(self): super().__init__(); self.links=[]
    def handle_starttag(self, tag, attrs):
        if tag=='a':
            d=dict(attrs); href=d.get('href')
            if href: self.links.append(href)
for path in ['klines','markPriceKlines','fundingRate']:
    url=f'https://data.binance.vision/data/futures/um/monthly/{path}/'
    p=P(); p.feed(urlopen(url,timeout=40).read().decode('utf-8','replace'))
    print(path, len(p.links), p.links[:20])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
import json, collections
with urlopen('https://fapi.binance.com/fapi/v1/exchangeInfo', timeout=40) as r:
    data=json.load(r)
rows=data['symbols']
print('total',len(rows))
print('contractType',collections.Counter(x.get('contractType') for x in rows))
print('status',collections.Counter(x.get('status') for x in rows))
print('quote',collections.Counter(x.get('quoteAsset') for x in rows).most_common())
for status in sorted(set(x.get('status') for x in rows)):
    sample=[x['symbol'] for x in rows if x.get('status')==status][:10]
    print(status, sample)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
for url in ['https://data.binance.vision/data/futures/um/daily/klines/','https://data.binance.vision/data/futures/um/monthly/klines/BTCUSDT/','https://data.binance.vision/data/futures/um/monthly/klines/1000LUNCBUSD/']:
    try:
        with urlopen(url,timeout=30) as r:
            print(url, r.status, r.geturl(), r.read(200).decode('utf-8','replace').replace('\\n',' ')[:160])
    except Exception as e:
        print(url, type(e).__name__, e)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 exited 1 in 2602ms:
Traceback (most recent call last):
  File "<stdin>", line 11, in <module>
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 189, in urlopen
    return opener.open(url, data, timeout)
           ~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 495, in open
    response = meth(req, response)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 604, in http_response
    response = self.parent.error(
        'http', request, response, code, msg, hdrs)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 533, in error
    return self._call_chain(*args)
           ~~~~~~~~~~~~~~~~^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 466, in _call_chain
    result = func(*args)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 613, in http_error_default
    raise HTTPError(req.full_url, code, msg, hdrs, fp)
urllib.error.HTTPError: HTTP Error 404: Not Found

 succeeded in 5393ms:
https://data.binance.vision/data/futures/um/daily/klines/ HTTPError HTTP Error 404: Not Found
https://data.binance.vision/data/futures/um/monthly/klines/BTCUSDT/ HTTPError HTTP Error 404: Not Found
https://data.binance.vision/data/futures/um/monthly/klines/1000LUNCBUSD/ HTTPError HTTP Error 404: Not Found

 succeeded in 32954ms:
total 792
contractType Counter({'PERPETUAL': 688, 'TRADIFI_PERPETUAL': 100, 'CURRENT_QUARTER': 2, 'NEXT_QUARTER': 2})
status Counter({'TRADING': 671, 'SETTLING': 119, 'PENDING_TRADING': 2})
quote [('USDT', 752), ('USDC', 38), ('BTC', 1), ('USD1', 1)]
PENDING_TRADING ['BTCSTUSDT', 'GAIBUSDT']
SETTLING ['OMGUSDT', 'WAVESUSDT', 'MKRUSDT', 'DEFIUSDT', 'BALUSDT', 'BLZUSDT', 'FTMUSDT', 'FLMUSDT', 'RENUSDT', 'LRCUSDT']
TRADING ['BTCUSDT', 'ETHUSDT', 'BCHUSDT', 'XRPUSDT', 'LTCUSDT', 'TRXUSDT', 'ETCUSDT', 'LINKUSDT', 'XLMUSDT', 'ADAUSDT']

codex
官方归档的可浏览入口存在，但具体层级可能需要用页面参数或固定文件路径探测；我会让脚本优先使用网页索引中能稳定解析出的官方链接，并用逐月文件探测作为兜底。`exchangeInfo` 返回的 792 个 symbol 还需要按 `PERPETUAL` 和 USDT 过滤。
codex
`exchangeInfo` 本身已经包含退市/结算中的 USDT 永续，字段里有 `onboardDate` 和 `deliveryDate`，这能提供 PIT 日期主数据。归档侧主要做交叉验证：确认这些 symbol 确实在 Binance 官方历史文件中出现过。
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
for url in [
'https://data.binance.vision/data/futures/um/monthly/klines/BTCUSDT/1h/BTCUSDT-1h-2020-01.zip',
'https://data.binance.vision/data/futures/um/monthly/klines/OMGUSDT/1h/OMGUSDT-1h-2021-01.zip',
'https://data.binance.vision/data/futures/um/monthly/klines/FTMUSDT/1h/FTMUSDT-1h-2022-01.zip',
'https://data.binance.vision/data/futures/um/monthly/markPriceKlines/OMGUSDT/1h/OMGUSDT-1h-2021-01.zip',
]:
    try:
        with urlopen(url, timeout=20) as r:
            print('OK',url, r.status, r.headers.get('Content-Length'))
            r.read(1)
    except Exception as e:
        print('ERR',url,type(e).__name__,e)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
url='https://data.binance.vision/data/futures/um/monthly/klines/'
html=urlopen(url, timeout=40).read().decode('utf-8','replace')
print('len',len(html))
for needle in ['BTCUSDT','data-symbol','prefix','klines']:
    print(needle, html.find(needle))
print(html[-2000:])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
import json, collections
with urlopen('https://fapi.binance.com/fapi/v1/exchangeInfo', timeout=40) as r:
    data=json.load(r)
rows=[s for s in data['symbols'] if s.get('contractType')=='PERPETUAL' and s.get('quoteAsset')=='USDT']
print('usdt perp', len(rows))
print(collections.Counter(s['status'] for s in rows))
for s in [x for x in rows if x['status']"'!='"'TRADING'][:10]:
    print(s['symbol'], s['status'], s['onboardDate'], s['deliveryDate'])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 exited 1 in 1956ms:
Traceback (most recent call last):
  File "<stdin>", line 3, in <module>
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 189, in urlopen
    return opener.open(url, data, timeout)
           ~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 495, in open
    response = meth(req, response)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 604, in http_response
    response = self.parent.error(
        'http', request, response, code, msg, hdrs)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 533, in error
    return self._call_chain(*args)
           ~~~~~~~~~~~~~~~~^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 466, in _call_chain
    result = func(*args)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 613, in http_error_default
    raise HTTPError(req.full_url, code, msg, hdrs, fp)
urllib.error.HTTPError: HTTP Error 404: Not Found

 succeeded in 8287ms:
OK https://data.binance.vision/data/futures/um/monthly/klines/BTCUSDT/1h/BTCUSDT-1h-2020-01.zip 200 37271
OK https://data.binance.vision/data/futures/um/monthly/klines/OMGUSDT/1h/OMGUSDT-1h-2021-01.zip 200 34476
OK https://data.binance.vision/data/futures/um/monthly/klines/FTMUSDT/1h/FTMUSDT-1h-2022-01.zip 200 37418
OK https://data.binance.vision/data/futures/um/monthly/markPriceKlines/OMGUSDT/1h/OMGUSDT-1h-2021-01.zip 200 18608

 succeeded in 39534ms:
usdt perp 648
Counter({'TRADING': 527, 'SETTLING': 119, 'PENDING_TRADING': 2})
OMGUSDT SETTLING 1593680400000 1738314000000
WAVESUSDT SETTLING 1597215600000 1718096400000
MKRUSDT SETTLING 1597302000000 1757322000000
DEFIUSDT SETTLING 1598598000000 1754902800000
BALUSDT SETTLING 1598943600000 1744621200000
BLZUSDT SETTLING 1600326000000 1734944400000
FTMUSDT SETTLING 1600930800000 1736154000000
FLMUSDT SETTLING 1601362800000 1763715600000
RENUSDT SETTLING 1602572400000 1733216400000
LRCUSDT SETTLING 1603091100000 1774342800000

exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
url='https://data.binance.vision/?prefix=data/futures/um/monthly/klines/&delimiter=/'
html=urlopen(url, timeout=40).read().decode('utf-8','replace')
print('len',len(html))
for needle in ['BTCUSDT','path','data/futures','href','S3','list']:
    print(needle, html.find(needle))
print(html[:2000])
print('---tail---')
print(html[-3000:])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 2432ms:
len 2591
BTCUSDT -1
path 881
data/futures -1
href 79
S3 2114
list 1896
<!DOCTYPE html>
<html>
<head>
  <title>Binance Data Collection</title>
  <link href="bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-dark bg-dark">
    <div class="container-md">
      <div class="row justify-content-between" style="width: 100%">
        <div class="col-sm">
          <a href="/"><img src="logo.png" height="30" class="d-inline-block align-top" alt=""></a>
          <span class="navbar-brand" style="padding-left: 10px;">Market Data</span>
        </div>
        <div class="col-sm" style="text-align: right" >
          <a href="https://github.com/binance/binance-public-data/" data-toggle="tooltip" title data-original-title="Public data document">
            <svg class="octicon octicon-mark-github v-align-middle" height="25" viewBox="0 0 16 16" version="1.1" width="25" aria-hidden="true" style="fill: #fff">
              <path fill-rule="evenodd" d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </nav>
  <div class="container-md">
    <div id="navigation" class="h6" style="margin-top: .8rem"></div>

    <table class="table table-hover">
      <thead>
        <tr>
          <th scope="col">Item</th>
          <th scope="col">Size</th>
          <th scope="col">Last Modified</th>
        </tr>
      </thead>
      <tbody id="listing"></tbody>
    </table>
  </div>
  <script type="text/javascript" src="jquery.min.js"></script>
 
---tail---
<!DOCTYPE html>
<html>
<head>
  <title>Binance Data Collection</title>
  <link href="bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-dark bg-dark">
    <div class="container-md">
      <div class="row justify-content-between" style="width: 100%">
        <div class="col-sm">
          <a href="/"><img src="logo.png" height="30" class="d-inline-block align-top" alt=""></a>
          <span class="navbar-brand" style="padding-left: 10px;">Market Data</span>
        </div>
        <div class="col-sm" style="text-align: right" >
          <a href="https://github.com/binance/binance-public-data/" data-toggle="tooltip" title data-original-title="Public data document">
            <svg class="octicon octicon-mark-github v-align-middle" height="25" viewBox="0 0 16 16" version="1.1" width="25" aria-hidden="true" style="fill: #fff">
              <path fill-rule="evenodd" d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </nav>
  <div class="container-md">
    <div id="navigation" class="h6" style="margin-top: .8rem"></div>

    <table class="table table-hover">
      <thead>
        <tr>
          <th scope="col">Item</th>
          <th scope="col">Size</th>
          <th scope="col">Last Modified</th>
        </tr>
      </thead>
      <tbody id="listing"></tbody>
    </table>
  </div>
  <script type="text/javascript" src="jquery.min.js"></script>
  <script type="text/javascript" src="bootstrap.bundle.min.js"></script>
  <script type="text/javascript">
    var S3BL_IGNORE_PATH = true;
    var BUCKET_URL = 'https://s3-ap-northeast-1.amazonaws.com/data.binance.vision';
    var BUCKET_WEBSITE_URL = 'https://data.binance.vision'
    var S3B_SORT = 'A2Z';
    var EXCLUDE_FILE = 'index.html'; 
    var S3B_ROOT_DIR = 'data/';
    $(function () {
      $('[data-toggle="tooltip"]').tooltip()
    })
  </script>
  <script type="text/javascript" src="xss.js"></script>
  <script type="text/javascript" src="list.js"></script>
</body>
</html>


codex
找到关键点了：`data.binance.vision` 页面用 `list.js` 从官方 S3 bucket 拉目录，所以可审计的“目录枚举”应直接请求 `https://s3-ap-northeast-1.amazonaws.com/data.binance.vision` 的 ListBucket XML，而不是解析空 HTML 表。
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
url='https://s3-ap-northeast-1.amazonaws.com/data.binance.vision?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/'
with urlopen(url, timeout=40) as r:
    text=r.read(2000).decode('utf-8','replace')
print(text)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 exited 1 in 452ms:
Traceback (most recent call last):
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 1319, in do_open
    h.request(req.get_method(), req.selector, req.data, headers,
    ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
              encode_chunked=req.has_header('Transfer-encoding'))
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1338, in request
    self._send_request(method, url, body, headers, encode_chunked)
    ~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1384, in _send_request
    self.endheaders(body, encode_chunked=encode_chunked)
    ~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1333, in endheaders
    self._send_output(message_body, encode_chunked=encode_chunked)
    ~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1093, in _send_output
    self.send(msg)
    ~~~~~~~~~^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1037, in send
    self.connect()
    ~~~~~~~~~~~~^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1479, in connect
    self.sock = self._context.wrap_socket(self.sock,
                ~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^
                                          server_hostname=server_hostname)
                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/ssl.py", line 455, in wrap_socket
    return self.sslsocket_class._create(
           ~~~~~~~~~~~~~~~~~~~~~~~~~~~~^
        sock=sock,
        ^^^^^^^^^^
    ...<5 lines>...
        session=session
        ^^^^^^^^^^^^^^^
    )
    ^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/ssl.py", line 1076, in _create
    self.do_handshake()
    ~~~~~~~~~~~~~~~~~^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/ssl.py", line 1372, in do_handshake
    self._sslobj.do_handshake()
    ~~~~~~~~~~~~~~~~~~~~~~~~~^^
ssl.SSLEOFError: [SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol (_ssl.c:1028)

During handling of the above exception, another exception occurred:

Traceback (most recent call last):
  File "<stdin>", line 3, in <module>
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 189, in urlopen
    return opener.open(url, data, timeout)
           ~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 489, in open
    response = self._open(req, data)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 506, in _open
    result = self._call_chain(self.handle_open, protocol, protocol +
                              '_open', req)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 466, in _call_chain
    result = func(*args)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 1367, in https_open
    return self.do_open(http.client.HTTPSConnection, req,
           ~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                        context=self._context)
                        ^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 1322, in do_open
    raise URLError(err)
urllib.error.URLError: <urlopen error [SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol (_ssl.c:1028)>

codex
S3 path-style HTTPS 连接在当前代理下有 SSL EOF，我会在脚本里保留重试，并改用官方页面声明的 bucket URL/virtual-host 组合测试；如果目录枚举不稳定，再用确定性 URL 探测交叉验证并在报告里如实标注。
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import Request, urlopen
urls=[
'https://data.binance.vision.s3.ap-northeast-1.amazonaws.com/?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/',
'https://data.binance.vision.s3.amazonaws.com/?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/',
'http://s3-ap-northeast-1.amazonaws.com/data.binance.vision?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/',
]
for url in urls:
    try:
        req=Request(url,headers={'User-Agent':'Mozilla/5.0'})
        with urlopen(req, timeout=30) as r:
            print('OK', url, r.status, r.read(500).decode('utf-8','replace'))
    except Exception as e:
        print('ERR', url, type(e).__name__, e)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import Request, urlopen
url='https://data.binance.vision/list.js'
with urlopen(Request(url,headers={'User-Agent':'Mozilla/5.0'}), timeout=30) as r:
    js=r.read().decode('utf-8','replace')
print(js[:4000])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 3488ms:
if (typeof AUTO_TITLE != 'undefined' && AUTO_TITLE == true) {
  document.title = location.hostname;
}

if (typeof S3_REGION != 'undefined') {
  var BUCKET_URL = location.protocol + '//' + location.hostname + '.' + S3_REGION + '.amazonaws.com'; // e.g. just 's3' for us-east-1 region
  var BUCKET_WEBSITE_URL = location.protocol + '//' + location.hostname;
}

if (typeof S3BL_IGNORE_PATH == 'undefined' || S3BL_IGNORE_PATH != true) {
  var S3BL_IGNORE_PATH = false;
}

if (typeof BUCKET_URL == 'undefined') {
  var BUCKET_URL = location.protocol + '//' + location.hostname;
}

if (typeof BUCKET_NAME != 'undefined') {
  // if bucket_url does not start with bucket_name,
  // assume path-style url
  if (!~BUCKET_URL.indexOf(location.protocol + '//' + BUCKET_NAME)) {
    BUCKET_URL += '/' + BUCKET_NAME;
  }
}

if (typeof BUCKET_WEBSITE_URL == 'undefined') {
  var BUCKET_WEBSITE_URL = BUCKET_URL;
}

if (typeof S3B_ROOT_DIR == 'undefined') {
  var S3B_ROOT_DIR = '';
}

if (typeof S3B_SORT == 'undefined') {
  var S3B_SORT = 'DEFAULT';
}

if (typeof EXCLUDE_FILE == 'undefined') {
  var EXCLUDE_FILE = [];
} else if (typeof EXCLUDE_FILE == 'string' || EXCLUDE_FILE instanceof RegExp) {
  var EXCLUDE_FILE = [EXCLUDE_FILE];
}

// https://tc39.github.io/ecma262/#sec-array.prototype.includes
if (!Array.prototype.includes) {
  Object.defineProperty(Array.prototype, 'includes', {
    value: function(searchElement, fromIndex) {

      if (this == null) {
        throw new TypeError('"this" is null or not defined');
      }

      // 1. Let O be ? ToObject(this value).
      var o = Object(this);

      // 2. Let len be ? ToLength(? Get(O, "length")).
      var len = o.length >>> 0;

      // 3. If len is 0, return false.
      if (len === 0) {
        return false;
      }

      // 4. Let n be ? ToInteger(fromIndex).
      //    (If fromIndex is undefined, this step produces the value 0.)
      var n = fromIndex | 0;

      // 5. If n ≥ 0, then
      //  a. Let k be n.
      // 6. Else n < 0,
      //  a. Let k be len + n.
      //  b. If k < 0, let k be 0.
      var k = Math.max(n >= 0 ? n : len - Math.abs(n), 0);

      function sameValueZero(x, y) {
        return x === y || (typeof x === 'number' && typeof y === 'number' && isNaN(x) && isNaN(y));
      }

      // 7. Repeat, while k < len
      while (k < len) {
        // a. Let elementK be the result of ? Get(O, ! ToString(k)).
        // b. If SameValueZero(searchElement, elementK) is true, return true.
        if (sameValueZero(o[k], searchElement)) {
          return true;
        }
        // c. Increase k by 1.
        k++;
      }

      // 8. Return false
      return false;
    }
  });
}

jQuery(function($) { getS3Data(); });

// This will sort your file listing by most recently modified.
// Flip the comparator to '>' if you want oldest files first.
function sortFunction(a, b) {
  switch (S3B_SORT) {
    case "OLD2NEW":
      return a.LastModified > b.LastModified ? 1 : -1;
    case "NEW2OLD":
      return a.LastModified < b.LastModified ? 1 : -1;
    case "A2Z":
      return a.Key < b.Key ? 1 : -1;
    case "Z2A":
      return a.Key > b.Key ? 1 : -1;
    case "BIG2SMALL":
      return a.Size < b.Size ? 1 : -1;
    case "SMALL2BIG":
      return a.Size > b.Size ? 1 : -1;
  }
}

function getS3Data(marker, html, page) {
  var s3_rest_url = createS3QueryUrl(marker);
  // set loading notice
  $('#listing')
      .html('<img src="ajaxload-circle.gif" />');
  $.get(s3_rest_url)
      .done(function(data) {
        // clear loading notice
        $('#listing').html('');
        var xml = $(data);
        var info = getInfoFromS3Data(xml);

        // Slight modification by FuzzBall03
        // This will sort your file listing based on var S3B_SORT
        // See url for example:
        // http://esp-link.s3-website-us-east-1.amazonaws.com/
        if (S3B_SORT != 'DEFAULT') {
          var sortedFiles = info.files;
          sortedFiles.sort(sortFunction);
          info.files = sortedFiles;


 succeeded in 4182ms:
ERR https://data.binance.vision.s3.ap-northeast-1.amazonaws.com/?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/ URLError <urlopen error [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: Hostname mismatch, certificate is not valid for 'data.binance.vision.s3.ap-northeast-1.amazonaws.com'. (_ssl.c:1028)>
ERR https://data.binance.vision.s3.amazonaws.com/?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/ URLError <urlopen error [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: Hostname mismatch, certificate is not valid for 'data.binance.vision.s3.amazonaws.com'. (_ssl.c:1028)>
ERR http://s3-ap-northeast-1.amazonaws.com/data.binance.vision?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/ HTTPError HTTP Error 502: Bad Gateway

codex
当前代理对 S3 TLS 主机名校验不兼容，但 Binance 页面确认目录来自该 S3 ListBucket 接口。为保证可复跑，我会实现两层：先尝试 S3 XML 枚举，失败时退回到官方归档 URL 的月度 `HEAD` 探测，并把实际采用的验证模式写进 CSV 来源字段和报告。
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
for url in [
'https://data.binance.vision?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/',
'https://data.binance.vision/?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/',
'https://data.binance.vision.s3-ap-northeast-1.amazonaws.com/?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/',
]:
    try:
        with urlopen(url,timeout=30) as r:
            print('OK',url,r.status,r.headers.get('content-type'),r.read(200).decode('utf-8','replace'))
    except Exception as e:
        print('ERR',url,type(e).__name__,e)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 6827ms:
OK https://data.binance.vision?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/ 200 text/html <!DOCTYPE html>
<html>
<head>
  <title>Binance Data Collection</title>
  <link href="bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-dark bg-dark">
    <div class="cont
OK https://data.binance.vision/?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/ 200 text/html <!DOCTYPE html>
<html>
<head>
  <title>Binance Data Collection</title>
  <link href="bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-dark bg-dark">
    <div class="cont
ERR https://data.binance.vision.s3-ap-northeast-1.amazonaws.com/?list-type=2&delimiter=/&prefix=data/futures/um/monthly/klines/ URLError <urlopen error [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: Hostname mismatch, certificate is not valid for 'data.binance.vision.s3-ap-northeast-1.amazonaws.com'. (_ssl.c:1028)>

codex
我会把 `UNIVERSE_PIT.csv` 的日期统一写成 UTC 日粒度 ISO 日期，PIT 函数按闭区间规则处理：`onboard_date <= date` 且 `delist_date` 为空或 `date < delist_date`，这样不会把未来上市或已退市合约带进当日集合。
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
js=urlopen('https://data.binance.vision/list.js',timeout=30).read().decode()
start=js.find('function createS3QueryUrl')
print(js[start:start+2500])
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 3540ms:
function createS3QueryUrl(marker) {
  var s3_rest_url = BUCKET_URL;
  s3_rest_url += '?delimiter=/';

  //
  // Handling paths and prefixes:
  //
  // 1. S3BL_IGNORE_PATH = false
  // Uses the pathname
  // {bucket}/{path} => prefix = {path}
  //
  // 2. S3BL_IGNORE_PATH = true
  // Uses ?prefix={prefix}
  //
  // Why both? Because we want classic directory style listing in normal
  // buckets but also allow deploying to non-buckets
  //

  var rx = '.*[?&]prefix=' + S3B_ROOT_DIR + '([^&]+)(&.*)?$';
  var prefix = '';
  if (S3BL_IGNORE_PATH == false) {
    var prefix = location.pathname.replace(/^\//, S3B_ROOT_DIR);
  }
  var match = location.search.match(rx);
  if (match) {
    prefix = S3B_ROOT_DIR + match[1];
  } else {
    if (S3BL_IGNORE_PATH) {
      var prefix = S3B_ROOT_DIR;
    }
  }
  if (prefix) {
    // make sure we end in /
    var prefix = prefix.replace(/\/$/, '') + '/';
    s3_rest_url += '&prefix=' + prefix;
  }
  if (marker) {
    s3_rest_url += '&marker=' + marker;
  }
  return s3_rest_url;
}

function getInfoFromS3Data(xml) {
  var prefix = $(xml.find('Prefix')[0]).text();
  var files = $.map(xml.find('Contents'), function(item) {
    item = $(item);
    // clang-format off
    return {
      Key: item.find('Key').text(),
          LastModified: item.find('LastModified').text(),
          Size: bytesToHumanReadable(item.find('Size').text()),
          Type: 'file'
    }
    // clang-format on
  });
  if (prefix && files[0] && files[0].Key == prefix) {
    files.shift();
  }
  var directories = $.map(xml.find('CommonPrefixes'), function(item) {
    item = $(item);
    // clang-format off
    return {
      Key: item.find('Prefix').text(),
        LastModified: '',
        Size: '',
        Type: 'directory'
    }
    // clang-format on
  });
  if ($(xml.find('IsTruncated')[0]).text() == 'true') {
    var nextMarker = $(xml.find('NextMarker')[0]).text();
  } else {
    var nextMarker = null;
  }
  // clang-format off
  return {
    files: files,
    directories: directories,
    prefix: prefix,
    nextMarker: encodeURIComponent(nextMarker)
  }
  // clang-format on
}

// info is object like:
// {
//    files: ..
//    directories: ..
//    prefix: ...
// }

function prepareTopLevelSymbol(info, page) {
  var files = info.directories.concat(info.files), prefix = info.prefix;

  if (prefix && prefix !== S3B_ROOT_DIR) {
    var up = prefix.replace(/\/$/, '').replace(/"/g, '&quot;').split('/').slice(0, -1).concat('').join(
            '/'),  /

exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; curl -I -L --max-time 30 'https://s3-ap-northeast-1.amazonaws.com/data.binance.vision?delimiter=/&prefix=data/futures/um/monthly/klines/' || true
curl -sS -L --max-time 30 'https://s3-ap-northeast-1.amazonaws.com/data.binance.vision?delimiter=/&prefix=data/futures/um/monthly/klines/' | head -c 500 || true" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 240ms:
  0     0    0     0    0     0      0      0 --:--:-- --:--:-- --:--:--     0
curl: (35) LibreSSL SSL_connect: SSL_ERROR_SYSCALL in connection to s3-ap-northeast-1.amazonaws.com:443 
HTTP/1.1 200 Connection established

curl: (35) LibreSSL SSL_connect: SSL_ERROR_SYSCALL in connection to s3-ap-northeast-1.amazonaws.com:443 

exec
/bin/bash -lc "unset HTTPS_PROXY; unset https_proxy; python3 - <<'PY'
from urllib.request import urlopen
for url in ['https://s3-ap-northeast-1.amazonaws.com/data.binance.vision?delimiter=/&prefix=data/futures/um/monthly/klines/','https://fapi.binance.com/fapi/v1/exchangeInfo']:
    try:
        with urlopen(url,timeout=20) as r:
            print('OK',url,r.status,r.read(200).decode('utf-8','replace'))
    except Exception as e:
        print('ERR',url,type(e).__name__,e)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import Request,urlopen
from urllib.error import HTTPError
for sym in ['OMGUSDT','WAVESUSDT','FTMUSDT','LUNA2USDT','1000LUNCUSDT','BTSUSDT','SRMUSDT']:
    for month in ['2020-01','2021-01','2022-01','2023-01','2024-01','2025-01','2026-01']:
        url=f'https://data.binance.vision/data/futures/um/monthly/klines/{sym}/1h/{sym}-1h-{month}.zip'
        req=Request(url, method='HEAD')
        try:
            with urlopen(req, timeout=10) as r:
                print(sym, month, 'OK', r.headers.get('content-length'))
                break
        except HTTPError as e:
            pass
        except Exception as e:
            print(sym, month, type(e).__name__, e)
            break
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 10207ms:
ERR https://s3-ap-northeast-1.amazonaws.com/data.binance.vision?delimiter=/&prefix=data/futures/um/monthly/klines/ URLError <urlopen error [SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol (_ssl.c:1028)>
ERR https://fapi.binance.com/fapi/v1/exchangeInfo URLError <urlopen error [SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol (_ssl.c:1028)>

codex
未走代理时官方接口也失败，所以任务书给的代理是必要条件。归档文件本身可通过 `data.binance.vision` 直接访问，S3 XML 枚举在该代理路径失败，这会作为数据缺口/复算环境说明写入报告。
 succeeded in 38558ms:
OMGUSDT 2021-01 OK 34476
WAVESUSDT 2021-01 OK 34321
FTMUSDT 2021-01 OK 35861
LUNA2USDT 2023-01 OK 33405
1000LUNCUSDT 2023-01 OK 32892
BTSUSDT 2022-01 OK 32351
SRMUSDT 2021-01 OK 31841

codex
接下来做文件编辑：新增一个可复跑构建脚本、一个短 pytest，以及最终执行报告。脚本会默认写 `06_RESEARCH/DATA/UNIVERSE_PIT.csv`，不会改动已有数据文件。
exec
/bin/bash -lc "ls 04_AI_TEAM/CODEX_TASKS | sort | sed -n '1,220p'" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
COLLABORATION_RULES.md
REPORT_0A6_SIGNAL_DETECTOR.md
REPORT_0B10_WIDER_STOP.md
REPORT_0B11_REGIME_V3.md
REPORT_0B12_NO_STOP_DIAGNOSTIC.md
REPORT_0B14_MAE_ANALYSIS.md
REPORT_0B15_VOL_COMPRESSION.md
REPORT_0B16_ATR_STOP_DIAGNOSTIC.md
REPORT_0B17_ATR_STRATEGY.md
REPORT_0B18_VOLPOS_STRATEGY.md
REPORT_0B1_SWEEP_EVENT_STUDY.md
REPORT_0B2_SWEEP_DUAL_REGIME.md
REPORT_0B3_BEARISH_SWEEP.md
REPORT_0B4_V4_STRATEGY_BACKTEST.md
REPORT_0B5_V4_BOOTSTRAP.md
REPORT_0B6_BACKTEST_UNIT_TESTS.md
REPORT_0B8_TIME_SENSITIVITY.md
REPORT_0B9_STOP_DIAGNOSTIC.md
REPORT_A1_OI_FEATURES.md
REPORT_P1_01_TSMOM.md
REPORT_P1_02_TSMOM_VOLTARGET.md
REPORT_P1_03_DATA_RECON_OI.md
REPORT_P1_04_TSMOM_REGIME_LONG.md
REPORT_P1_05_REGIME_ATTRIBUTION.md
REPORT_P1_06_TSMOM_MACRO_BULL.md
REPORT_R1_A2_FEATURES.md
REPORT_R2_A2_EVENT_STUDY.md
REPORT_TSMOM_EXPANSION_DATA.md
SPEC_0A6_SIGNAL_DEFINITIONS.md
TASK_0A6_SIGNAL_DETECTOR.md
TASK_0B10_WIDER_STOP.md
TASK_0B11_REGIME_V3.md
TASK_0B12_MAE_ANALYSIS.md
TASK_0B12_NO_STOP_DIAGNOSTIC.md
TASK_0B13_T24_NO_STOP.md
TASK_0B13_T24_REFLOWSTOP.md
TASK_0B14_MAE_ANALYSIS.md
TASK_0B14_VOL_COMPRESSION.md
TASK_0B15_ATR_STOP_DIAGNOSTIC.md
TASK_0B15_VOL_COMPRESSION.md
TASK_0B16_ATR_STOP_DIAGNOSTIC.md
TASK_0B17_ATR_STRATEGY.md
TASK_0B18_VOLPOS_STRATEGY.md
TASK_0B1_SWEEP_SINGLE_EVENT.md
TASK_0B2_SWEEP_DUAL_REGIME.md
TASK_0B3_BEARISH_SWEEP.md
TASK_0B4_V4_STRATEGY_BACKTEST.md
TASK_0B5_V4_BOOTSTRAP_VALIDATION.md
TASK_0B6_BACKTEST_UNIT_TESTS.md
TASK_0B7_V6_TIME_EXIT.md
TASK_0B8_TIME_SENSITIVITY.md
TASK_0B9_STOP_DIAGNOSTIC.md
TASK_A1_OI_FEATURES.md
TASK_CARRY_FEASIBILITY.md
TASK_EXT_AUDIT_CODEX.md
TASK_INF01_1H_DATA.md
TASK_INF02_RESULTS_DASHBOARD.md
TASK_P1_01_TSMOM.md
TASK_P1_02_TSMOM_VOLTARGET.md
TASK_P1_03_DATA_RECON_OI.md
TASK_P1_04_TSMOM_REGIME_LONG.md
TASK_P1_05_REGIME_ATTRIBUTION.md
TASK_P1_06_TSMOM_MACRO_BULL.md
TASK_R1_A2_FEATURES.md
TASK_R2_A2_EVENT_STUDY.md
TASK_TSMOM_EXPANSION_DATA.md
TASK_UNIVERSE_PIT.md

exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
from datetime import datetime, timezone
import json
with urlopen('https://fapi.binance.com/fapi/v1/exchangeInfo', timeout=40) as r:
    data=json.load(r)
rows=[s for s in data['symbols'] if s.get('contractType')=='PERPETUAL' and s.get('quoteAsset')=='USDT' and s.get('status')"'!='"'PENDING_TRADING']
print(len(rows))
sett=[s for s in rows if s.get('status')"'!='"'TRADING']
print('settling',len(sett))
for s in sorted(sett, key=lambda x:x['deliveryDate'])[:5]+sorted(sett, key=lambda x:x['deliveryDate'])[-5:]:
    print(s['symbol'], s['status'], datetime.fromtimestamp(s['onboardDate']/1000, tz=timezone.utc).isoformat(), datetime.fromtimestamp(s['deliveryDate']/1000, tz=timezone.utc).isoformat())
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc 'test -e 06_RESEARCH/DATA/UNIVERSE_PIT.csv; echo DATA_EXISTS=$?
test -e 06_RESEARCH/CODE/build_universe_pit.py; echo SCRIPT_EXISTS=$?
test -e 06_RESEARCH/CODE/tests/test_universe_pit.py; echo TEST_EXISTS=$?
test -e 04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md; echo REPORT_EXISTS=$?' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
DATA_EXISTS=1
SCRIPT_EXISTS=1
TEST_EXISTS=1
REPORT_EXISTS=1

codex
目标文件都还不存在，可以新增而不覆盖。现有报告目录也没有 `REPORT_UNIVERSE_PIT.md`，符合“不改既有文件”。
 succeeded in 38951ms:
646
settling 119
SCUSDT SETTLING 2021-04-12T07:00:00+00:00 2022-06-17T09:00:00+00:00
FTTUSDT SETTLING 2022-04-14T07:00:00+00:00 2022-11-14T04:00:00+00:00
RAYUSDT SETTLING 2021-08-18T07:00:00+00:00 2022-11-15T04:00:00+00:00
STRAXUSDT SETTLING 2023-10-11T02:30:00+00:00 2024-03-15T09:00:00+00:00
DGBUSDT SETTLING 2021-04-19T07:00:00+00:00 2024-04-01T09:00:00+00:00
MLNUSDT SETTLING 2025-03-31T08:45:00+00:00 2026-05-19T09:00:00+00:00
HIGHUSDT SETTLING 2023-02-03T07:00:00+00:00 2026-06-11T09:00:00+00:00
MBOXUSDT SETTLING 2024-08-28T09:30:00+00:00 2026-06-11T09:00:00+00:00
COSUSDT SETTLING 2024-09-30T13:00:00+00:00 2026-06-11T09:00:00+00:00
DUSDT SETTLING 2025-01-09T10:00:00+00:00 2026-06-11T09:00:00+00:00

codex
实现细节我会保持简单可审计：`exchangeInfo` 是主数据源；`data.binance.vision` 用官方归档路径做逐 symbol 历史文件存在性校验；PIT 查询函数只依赖生成 CSV，不依赖网络或内存状态。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ed7206b57ff8dcd70ad9210e93e025fd8718183
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month.date())
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int(frame["source"].str.contains("missing", regex=False).rsub(1).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()

diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ed7206b57ff8dcd70ad9210e93e025fd8718183
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month.date())
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int(frame["source"].str.contains("missing", regex=False).rsub(1).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()

codex
脚本主体已新增。我注意到末尾统计打印有一个 pandas 写法不够直接，会先修掉，再加 PIT 单元测试。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b8321125ea0241b86a00695cfa084429878b4e8
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month.date())
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()

diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b8321125ea0241b86a00695cfa084429878b4e8
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month.date())
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()

codex
现在新增 pytest，只测 PIT 日期边界和真实 CSV 结构，不触网、不读取 HOLDOUT。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/tests/test_universe_pit.py
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b8321125ea0241b86a00695cfa084429878b4e8
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month.date())
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b8321125ea0241b86a00695cfa084429878b4e8
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month.date())
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
测试文件已加好。下一步先跑目标测试和脚本的语法检查，再执行构建生成 CSV。
exec
/bin/bash -lc 'python3 -m py_compile 06_RESEARCH/CODE/build_universe_pit.py 06_RESEARCH/CODE/tests/test_universe_pit.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
exec
/bin/bash -lc 'python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b8321125ea0241b86a00695cfa084429878b4e8
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month.date())
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

 succeeded in 942ms:
..                                                                       [100%]
2 passed in 0.52s

codex
目标 pytest 通过。现在按任务书代理环境运行构建脚本；它会先尝试 S3 目录枚举，失败才进入官方归档文件探测。
exec
/bin/bash -lc 'export HTTPS_PROXY=http://127.0.0.1:7897; python3 06_RESEARCH/CODE/build_universe_pit.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b8321125ea0241b86a00695cfa084429878b4e8
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month.date())
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

 exited 1 in 11922ms:
S3 directory enumeration unavailable: failed to list archive prefix data/futures/um/monthly/klines/
Traceback (most recent call last):
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 299, in <module>
    main()
    ~~~~^^
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 287, in main
    found = archive_presence_by_probe(rows, args.workers)
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 188, in archive_presence_by_probe
    symbol, url = future.result()
                  ~~~~~~~~~~~~~^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/concurrent/futures/_base.py", line 449, in result
    return self.__get_result()
           ~~~~~~~~~~~~~~~~~^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/concurrent/futures/_base.py", line 401, in __get_result
    raise self._exception
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/concurrent/futures/thread.py", line 59, in run
    result = self.fn(*self.args, **self.kwargs)
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 179, in check
    monthly_end = min(end, last_complete_month.date())
                           ^^^^^^^^^^^^^^^^^^^^^^^^
AttributeError: 'datetime.date' object has no attribute 'date'

codex
第一次构建在归档探测前暴露了一个日期类型 bug：`date - Timedelta` 已经返回 `date`，不应再 `.date()`。我会修正后重跑。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..c98703a24244235c4c11eb36118bcc2666224781
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..c98703a24244235c4c11eb36118bcc2666224781
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
修正已完成，我会重新跑目标测试和构建，确认输出资产。
exec
/bin/bash -lc 'python3 -m py_compile 06_RESEARCH/CODE/build_universe_pit.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
exec
/bin/bash -lc 'python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..c98703a24244235c4c11eb36118bcc2666224781
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

 succeeded in 878ms:
..                                                                       [100%]
2 passed in 0.49s

codex
目标测试仍通过。现在重跑构建脚本，预计会进入官方归档 URL 探测并打印每 100 个 symbol 的进度。
exec
/bin/bash -lc 'export HTTPS_PROXY=http://127.0.0.1:7897; python3 06_RESEARCH/CODE/build_universe_pit.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..c98703a24244235c4c11eb36118bcc2666224781
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
构建正在跑，S3 XML 枚举仍受代理/TLS 限制失败，脚本已按设计切换到官方归档 `HEAD` 探测。
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..c98703a24244235c4c11eb36118bcc2666224781
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
归档交叉验证已完成约一半，目前没有中断。完成后我会做 CSV 行数、退市样例、年度上市表和现有 9 品种覆盖检查。
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..c98703a24244235c4c11eb36118bcc2666224781
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

 exited 1 in 99955ms:
S3 directory enumeration unavailable: failed to list archive prefix data/futures/um/monthly/klines/
Archive cross-check 100/646
Archive cross-check 200/646
Archive cross-check 300/646
Archive cross-check 400/646
Archive cross-check 500/646
Traceback (most recent call last):
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 299, in <module>
    main()
    ~~~~^^
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 287, in main
    found = archive_presence_by_probe(rows, args.workers)
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 188, in archive_presence_by_probe
    symbol, url = future.result()
                  ~~~~~~~~~~~~~^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/concurrent/futures/_base.py", line 449, in result
    return self.__get_result()
           ~~~~~~~~~~~~~~~~~^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/concurrent/futures/_base.py", line 401, in __get_result
    raise self._exception
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/concurrent/futures/thread.py", line 59, in run
    result = self.fn(*self.args, **self.kwargs)
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 182, in check
    return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
                           ~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 154, in first_existing_archive
    if url_exists(url):
       ~~~~~~~~~~^^^^^
  File "/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py", line 136, in url_exists
    with urlopen(request, timeout=20) as response:
         ~~~~~~~^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 189, in urlopen
    return opener.open(url, data, timeout)
           ~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 489, in open
    response = self._open(req, data)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 506, in _open
    result = self._call_chain(self.handle_open, protocol, protocol +
                              '_open', req)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 466, in _call_chain
    result = func(*args)
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 1367, in https_open
    return self.do_open(http.client.HTTPSConnection, req,
           ~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                        context=self._context)
                        ^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/urllib/request.py", line 1319, in do_open
    h.request(req.get_method(), req.selector, req.data, headers,
    ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
              encode_chunked=req.has_header('Transfer-encoding'))
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1338, in request
    self._send_request(method, url, body, headers, encode_chunked)
    ~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1349, in _send_request
    self.putrequest(method, url, **skips)
    ~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1187, in putrequest
    self._output(self._encode_request(request))
                 ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^
  File "/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/http/client.py", line 1267, in _encode_request
    return request.encode('ascii')
           ~~~~~~~~~~~~~~^^^^^^^^^
UnicodeEncodeError: 'ascii' codec can't encode characters in position 37-40: ordinal not in range(128)

codex
第二次构建在约 500 个 symbol 后遇到一个非 ASCII symbol URL 编码问题。需要对归档 URL 的 path segment 做安全编码，而不是直接拼接原 symbol。
exec
/bin/bash -lc "export HTTPS_PROXY=http://127.0.0.1:7897; python3 - <<'PY'
from urllib.request import urlopen
import json
with urlopen('https://fapi.binance.com/fapi/v1/exchangeInfo', timeout=45) as r:
    data=json.load(r)
rows=[s for s in data['symbols'] if s.get('contractType')=='PERPETUAL' and s.get('quoteAsset')=='USDT' and s.get('marginAsset')=='USDT' and s.get('status')"'!='"'PENDING_TRADING']
for s in rows:
    sym=s['symbol']
    if not sym.isascii():
        print(repr(sym), s.get('status'), s.get('onboardDate'), s.get('deliveryDate'))
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..c98703a24244235c4c11eb36118bcc2666224781
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,299 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{symbol}/{interval}/"
+        f"{symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

 succeeded in 30090ms:
'币安人生USDT' TRADING 1760958000000 4133404800000
'我踏马来了USDT' TRADING 1769005800000 4133404800000
'龙虾USDT' TRADING 1773228600000 4133404800000

codex
`exchangeInfo` 当前确实返回 3 个中文命名 USDT 永续 symbol，这属于官方数据本身，不是脚本文字。CSV 会保留原 symbol，归档 URL 查询会做 percent-encoding。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
编码修正完成。我会再跑一次测试，然后重新构建；这次应能跳过/标记无归档命中的特殊 symbol，而不是中断。
exec
/bin/bash -lc 'python3 -m py_compile 06_RESEARCH/CODE/build_universe_pit.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
exec
/bin/bash -lc 'python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

 succeeded in 895ms:
..                                                                       [100%]
2 passed in 0.49s

codex
测试通过，开始第三次构建。前两次没有写出 CSV，这次完成后会立即做数据完整性审计。
exec
/bin/bash -lc 'export HTTPS_PROXY=http://127.0.0.1:7897; python3 06_RESEARCH/CODE/build_universe_pit.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
构建正在继续，已经完成 100/646 个归档探测。
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
归档探测已过 500/646，剩余主要是后段 symbol 和可能缺归档的新品种。
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

 succeeded in 78846ms:
S3 directory enumeration unavailable: failed to list archive prefix data/futures/um/monthly/klines/
Archive cross-check 100/646
Archive cross-check 200/646
Archive cross-check 300/646
Archive cross-check 400/646
Archive cross-check 500/646
Archive cross-check 600/646
Archive cross-check 646/646
Wrote /Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/DATA/UNIVERSE_PIT.csv
rows=646 delisted=119 archive_verified=646

codex
CSV 已生成：646 行、119 个退市/结算合约，归档探测全部命中。现在做报告所需的审计统计：年度上市、已知退市抽查、现有品种覆盖和 PIT 函数真实数据检查。
exec
/bin/bash -lc "python3 - <<'PY'
from pathlib import Path
import re
symbols=set()
for path in Path('06_RESEARCH/DATA/FUTURES').glob('*_MARK_4H.csv'):
    symbols.add(path.name.split('_MARK_4H.csv')[0])
for path in Path('.').glob('*_USDT_4H.csv'):
    symbols.add(path.name.replace('_USDT_4H.csv','USDT'))
print(len(symbols), sorted(symbols))
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
8 ['ADAUSDT', 'BNBUSDT', 'BTCUSDT', 'DOGEUSDT', 'ETHUSDT', 'LTCUSDT', 'SOLUSDT', 'XRPUSDT']

exec
/bin/bash -lc "python3 - <<'PY'
import pandas as pd
from pathlib import Path
p=Path('06_RESEARCH/DATA/UNIVERSE_PIT.csv')
df=pd.read_csv(p,dtype=str).fillna('')
print('rows',len(df))
print('delisted',df.delist_date.ne('').sum())
print('columns',list(df.columns))
print('head')
print(df.head(10).to_string(index=False))
print('known delisted')
for sym in ['OMGUSDT','FTTUSDT','SCUSDT','WAVESUSDT','FTMUSDT','LUNA2USDT','BTSUSDT']:
    row=df[df.symbol==sym]
    print(sym, 'FOUND' if not row.empty else 'MISSING', row.to_dict('records')[:1])
print('non-ascii')
print(df[~df.symbol.map(str.isascii)].to_string(index=False))
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "python3 - <<'PY'
import pandas as pd
df=pd.read_csv('06_RESEARCH/DATA/UNIVERSE_PIT.csv', dtype=str).fillna('')
df['year']=pd.to_datetime(df['onboard_date']).dt.year
print(df.groupby('year').size().to_string())
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "rg -n \"9 品种|9品种|现有.*品种|BTCUSDT|LTCUSDT|TRXUSDT\" 04_AI_TEAM 06_RESEARCH/CODE 06_RESEARCH/RESULTS 06_RESEARCH/HYPOTHESES | sed -n '1,220p'" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/RESULTS/0A_NEW2_SAMPLE_FEASIBILITY_REPORT.md:6:**输入信号：** `triple_signals_BTCUSDT_4H.csv`  
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:21:| BTCUSDT | 2020-01-01 | 2026-06-05 | 7044 (0 non-8h)   | 0.0110%     | 12.04%     | 14.37%         | 24 (8.0d)            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:31:| BTCUSDT | 2020 | 1098    | 366.0    | 17.24%      | 17.20%     | 14.30%         | 24 (8.0d)            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:32:| BTCUSDT | 2021 | 1095    | 365.0    | 30.61%      | 30.63%     | 7.31%          | 9 (3.0d)             |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:33:| BTCUSDT | 2022 | 1095    | 365.0    | 4.16%       | 4.17%      | 22.10%         | 10 (3.3d)            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:34:| BTCUSDT | 2023 | 1095    | 365.0    | 7.87%       | 7.87%      | 10.14%         | 7 (2.3d)             |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:35:| BTCUSDT | 2024 | 1098    | 366.0    | 11.96%      | 11.93%     | 8.38%          | 18 (6.0d)            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:36:| BTCUSDT | 2025 | 1095    | 365.0    | 5.13%       | 5.13%      | 12.88%         | 10 (3.3d)            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:37:| BTCUSDT | 2026 | 468     | 156.0    | 0.42%       | 0.99%      | 40.38%         | 14 (4.7d)            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:59:| BTCUSDT | VIP0_task_conservative       | 30        | 0.80%           | 9.74%         | 2.30%            | 24.3            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:60:| BTCUSDT | VIP0_task_conservative       | 90        | 0.80%           | 3.25%         | 8.79%            | 24.3            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:61:| BTCUSDT | VIP0_task_conservative       | 365       | 0.80%           | 0.80%         | 11.24%           | 24.3            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:62:| BTCUSDT | BNB_fee_discount_sensitivity | 30        | 0.73%           | 8.89%         | 3.15%            | 22.1            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:63:| BTCUSDT | BNB_fee_discount_sensitivity | 90        | 0.73%           | 2.96%         | 9.08%            | 22.1            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:64:| BTCUSDT | BNB_fee_discount_sensitivity | 365       | 0.73%           | 0.73%         | 11.31%           | 22.1            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:84:| BTCUSDT | VIP0_task_conservative       | 10%         | 15000         | 1500       | 13500       | 5.62%             | 1686            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:85:| BTCUSDT | VIP0_task_conservative       | 20%         | 15000         | 3000       | 12000       | 5.62%             | 1686            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:86:| BTCUSDT | VIP0_task_conservative       | 50%         | 15000         | 7500       | 7500        | 5.62%             | 1686            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:87:| BTCUSDT | BNB_fee_discount_sensitivity | 10%         | 15000         | 1500       | 13500       | 5.65%             | 1696            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:88:| BTCUSDT | BNB_fee_discount_sensitivity | 20%         | 15000         | 3000       | 12000       | 5.65%             | 1696            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:89:| BTCUSDT | BNB_fee_discount_sensitivity | 50%         | 15000         | 7500       | 7500        | 5.65%             | 1696            |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:103:| BTCUSDT | 92              | 3            | 89           | 2.17%              | 7.89%           | 13.24%          | 26.18%        |
06_RESEARCH/RESULTS/20260612_carry_feasibility.md:123:- BTCUSDT: 06_RESEARCH/DATA/FUTURES/BTCUSDT_FUNDING_8H.csv sha256=5803cb5e8ffb rows=7029; 06_RESEARCH/DATA/FUTURES/BTCUSDT_FUNDING_8H_2026H1.csv sha256=d1af8ceff1a5 rows=15; 06_RESEARCH/DATA/FUTURES/BTCUSDT_MARK_4H.csv sha256=7127294528e6 rows=14010; 06_RESEARCH/DATA/FUTURES/BTCUSDT_MARK_4H_2026H1.csv sha256=a20868ae0c91 rows=30
06_RESEARCH/CODE/a2_event_study.py:35:SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
0.0027397260273972603
06_RESEARCH/CODE/output/a2_events_work.csv:526:2024-08-10T00:00:00Z,BTCUSDT,neg,P2.5,-3.942e-05,0.012785388127853882
06_RESEARCH/CODE/output/a2_events_work.csv:527:2024-08-10T00:00:00Z,BTCUSDT,neg,P5,-3.942e-05,0.012785388127853882
06_RESEARCH/CODE/output/a2_events_work.csv:528:2024-08-12T16:00:00Z,BTCUSDT,neg,P5,-2.238e-05,0.0365296803652968
06_RESEARCH/CODE/output/a2_events_work.csv:531:2024-08-13T16:00:00Z,BTCUSDT,neg,P2.5,-3.902e-05,0.014611872146118721
06_RESEARCH/CODE/output/a2_events_work.csv:532:2024-08-14T16:00:00Z,BTCUSDT,neg,P1,-7.811e-05,0.0027397260273972603
06_RESEARCH/CODE/output/a2_events_work.csv:535:2024-08-18T16:00:00Z,BTCUSDT,neg,P5,-3.149e-05,0.028767123287671233
06_RESEARCH/CODE/output/a2_events_work.csv:536:2024-08-19T00:00:00Z,BTCUSDT,neg,P2.5,-3.616e-05,0.0228310502283105
06_RESEARCH/CODE/output/a2_events_work.csv:537:2024-08-19T08:00:00Z,BTCUSDT,neg,P1,-6.75e-05,0.00821917808219178
06_RESEARCH/CODE/output/a2_events_work.csv:538:2024-08-21T00:00:00Z,BTCUSDT,neg,P1,-6.756e-05,0.00821917808219178
06_RESEARCH/CODE/output/a2_events_work.csv:539:2024-08-21T00:00:00Z,BTCUSDT,neg,P2.5,-6.756e-05,0.00821917808219178

 succeeded in 618ms:
year
2019      3
2020     67
2021     46
2022     19
2023     96
2024    129
2025    246
2026     40

 succeeded in 623ms:
rows 646
delisted 119
columns ['symbol', 'onboard_date', 'delist_date', 'source']
head
  symbol onboard_date delist_date                                                                            source
 BTCUSDT   2019-09-08             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
 ETHUSDT   2019-11-27             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
 BCHUSDT   2019-12-19             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
 XRPUSDT   2020-01-06             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
 LTCUSDT   2020-01-09             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
 TRXUSDT   2020-01-15             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
 ETCUSDT   2020-01-16             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
LINKUSDT   2020-01-17             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
 XLMUSDT   2020-01-20             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
 ADAUSDT   2020-01-31             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
known delisted
OMGUSDT FOUND [{'symbol': 'OMGUSDT', 'onboard_date': '2020-07-02', 'delist_date': '2025-01-31', 'source': 'Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe'}]
FTTUSDT FOUND [{'symbol': 'FTTUSDT', 'onboard_date': '2022-04-14', 'delist_date': '2022-11-14', 'source': 'Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe'}]
SCUSDT FOUND [{'symbol': 'SCUSDT', 'onboard_date': '2021-04-12', 'delist_date': '2022-06-17', 'source': 'Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe'}]
WAVESUSDT FOUND [{'symbol': 'WAVESUSDT', 'onboard_date': '2020-08-12', 'delist_date': '2024-06-11', 'source': 'Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe'}]
FTMUSDT FOUND [{'symbol': 'FTMUSDT', 'onboard_date': '2020-09-24', 'delist_date': '2025-01-06', 'source': 'Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe'}]
LUNA2USDT FOUND [{'symbol': 'LUNA2USDT', 'onboard_date': '2022-09-09', 'delist_date': '', 'source': 'Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe'}]
BTSUSDT MISSING []
non-ascii
   symbol onboard_date delist_date                                                                            source
 币安人生USDT   2025-10-20             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
我踏马来了USDT   2026-01-21             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe
   龙虾USDT   2026-03-11             Binance fapi/v1/exchangeInfo; data.binance.vision monthly/daily klines HEAD probe

diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
审计结果显示当前仓库可识别的现有 4H/FUTURES 品种是 8 个，不是任务书写的 9 个；我会在报告里明确标注“仓库内可审计到 8 个”，并逐个对照，避免猜第 9 个。
exec
/bin/bash -lc "rg -n \"SYMBOLS|NEW_SYMBOLS|EXISTING_SYMBOLS|BTCUSDT|ETHUSDT|SOLUSDT|BNBUSDT|XRPUSDT|DOGEUSDT|ADAUSDT|LTCUSDT|TRXUSDT\" 06_RESEARCH/CODE/*.py 04_AI_TEAM/CODEX_TASKS/TASK_*.md 04_AI_TEAM/CODEX_TASKS/REPORT_*.md | sed -n '1,260p'" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/CODE/download_futures_data.py:15:    "BTCUSDT": "2020-01",
06_RESEARCH/CODE/download_futures_data.py:16:    "ETHUSDT": "2020-01",
06_RESEARCH/CODE/download_futures_data.py:17:    "SOLUSDT": "2020-09",
06_RESEARCH/CODE/a2_event_study.py:35:SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
06_RESEARCH/CODE/a2_event_study.py:536:    prices_by_symbol = {symbol: load_mark_prices(symbol, DATA_DIR) for symbol in SYMBOLS}
06_RESEARCH/CODE/a2_event_study.py:554:    regime = regime_breakdown(events_with_returns, prices_by_symbol["BTCUSDT"])
06_RESEARCH/CODE/a2_event_study.py:560:            "symbols": list(SYMBOLS),
06_RESEARCH/CODE/backtest_v5_sweep_choch_fvg_bull_v1.py:16:SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
06_RESEARCH/CODE/backtest_v5_sweep_choch_fvg_bull_v1.py:356:    for symbol in SYMBOLS:
06_RESEARCH/CODE/backtest_v5_sweep_choch_fvg_bull_v1.py:372:    common_bars = datasets["SOLUSDT"][0]
06_RESEARCH/CODE/signal_detector.py:16:DEFAULT_SYMBOL = "BTCUSDT"
06_RESEARCH/CODE/signal_detector.py:212:        help="Output symbol label (default: BTCUSDT)",
06_RESEARCH/CODE/carry_feasibility.py:27:SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
06_RESEARCH/CODE/carry_feasibility.py:28:PRIMARY_SYMBOLS = ("BTCUSDT", "ETHUSDT")
06_RESEARCH/CODE/carry_feasibility.py:29:REFERENCE_SYMBOLS = ("SOLUSDT",)
06_RESEARCH/CODE/carry_feasibility.py:295:        if row["symbol"] not in PRIMARY_SYMBOLS:
06_RESEARCH/CODE/carry_feasibility.py:400:        r for r in costs if r["symbol"] in PRIMARY_SYMBOLS and r["scenario"] == "VIP0_task_conservative" and r["hold_days"] == 365
06_RESEARCH/CODE/carry_feasibility.py:531:    for symbol in SYMBOLS:
06_RESEARCH/CODE/v4_strategy_backtest.py:50:        / "06_RESEARCH/DATA/FUTURES/BTCUSDT_FUNDING_8H.csv",
06_RESEARCH/CODE/v4_strategy_backtest.py:59:        / "06_RESEARCH/DATA/FUTURES/ETHUSDT_FUNDING_8H.csv",
06_RESEARCH/CODE/v4_strategy_backtest.py:68:        / "06_RESEARCH/DATA/FUTURES/SOLUSDT_FUNDING_8H.csv",
06_RESEARCH/CODE/p1_03_data_recon_oi.py:26:    "BTCUSDT": date(2020, 9, 1),
06_RESEARCH/CODE/p1_03_data_recon_oi.py:27:    "ETHUSDT": date(2021, 12, 1),
06_RESEARCH/CODE/p1_03_data_recon_oi.py:28:    "SOLUSDT": date(2021, 12, 1),
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:37:NEW_SYMBOLS = ["BNBUSDT", "XRPUSDT", "DOGEUSDT", "ADAUSDT", "LTCUSDT"]
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:38:EXISTING_SYMBOLS = ["BTCUSDT", "ETHUSDT", "SOLUSDT"]
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:175:    for symbol in NEW_SYMBOLS:
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:278:    for symbol in EXISTING_SYMBOLS:
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:358:    for symbol in NEW_SYMBOLS:
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:365:    for symbol in EXISTING_SYMBOLS:
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:482:            symbol: fetch_funding_api(symbol) for symbol in [*NEW_SYMBOLS, *EXISTING_SYMBOLS]
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:485:        for symbol in NEW_SYMBOLS:
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:497:                    FileSpec(mark_path, "mark", "4h", 4, FUTURES_DIR / "BTCUSDT_MARK_4H.csv"),
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:498:                    FileSpec(funding_path, "funding", "8h", 8, FUTURES_DIR / "BTCUSDT_FUNDING_8H.csv"),
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:502:        for symbol in EXISTING_SYMBOLS:
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:523:        for symbol in NEW_SYMBOLS:
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:531:                        FUTURES_DIR / "BTCUSDT_MARK_4H.csv",
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:538:                        FUTURES_DIR / "BTCUSDT_FUNDING_8H.csv",
06_RESEARCH/CODE/prepare_tsmom_expansion_data.py:542:        for symbol in EXISTING_SYMBOLS:
06_RESEARCH/CODE/v10_wider_stop_backtest.py:50:        / "06_RESEARCH/DATA/FUTURES/BTCUSDT_FUNDING_8H.csv",
06_RESEARCH/CODE/v10_wider_stop_backtest.py:59:        / "06_RESEARCH/DATA/FUTURES/ETHUSDT_FUNDING_8H.csv",
06_RESEARCH/CODE/v10_wider_stop_backtest.py:68:        / "06_RESEARCH/DATA/FUTURES/SOLUSDT_FUNDING_8H.csv",
04_AI_TEAM/CODEX_TASKS/TASK_0B13_T24_NO_STOP.md:33:      06_RESEARCH/DATA/BTCUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0B13_T24_NO_STOP.md:34:      06_RESEARCH/DATA/ETHUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0B13_T24_NO_STOP.md:35:      06_RESEARCH/DATA/SOLUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0A6_SIGNAL_DETECTOR.md:33:      <path>/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/output/triple_signals_BTCUSDT_4H.csv</path>
04_AI_TEAM/CODEX_TASKS/TASK_0B14_VOL_COMPRESSION.md:38:      06_RESEARCH/DATA/BTCUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0B14_VOL_COMPRESSION.md:39:      06_RESEARCH/DATA/ETHUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0B14_VOL_COMPRESSION.md:40:      06_RESEARCH/DATA/SOLUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_INF01_1H_DATA.md:28:      品种：BTCUSDT、ETHUSDT、SOLUSDT
04_AI_TEAM/CODEX_TASKS/TASK_INF01_1H_DATA.md:48:        06_RESEARCH/DATA/BTCUSDT_1H.csv
04_AI_TEAM/CODEX_TASKS/TASK_INF01_1H_DATA.md:49:        06_RESEARCH/DATA/ETHUSDT_1H.csv
04_AI_TEAM/CODEX_TASKS/TASK_INF01_1H_DATA.md:50:        06_RESEARCH/DATA/SOLUSDT_1H.csv
04_AI_TEAM/CODEX_TASKS/TASK_INF01_1H_DATA.md:63:    <file>06_RESEARCH/DATA/BTCUSDT_1H.csv</file>
04_AI_TEAM/CODEX_TASKS/TASK_INF01_1H_DATA.md:64:    <file>06_RESEARCH/DATA/ETHUSDT_1H.csv</file>
04_AI_TEAM/CODEX_TASKS/TASK_INF01_1H_DATA.md:65:    <file>06_RESEARCH/DATA/SOLUSDT_1H.csv</file>
04_AI_TEAM/CODEX_TASKS/TASK_0B15_ATR_STOP_DIAGNOSTIC.md:33:      06_RESEARCH/DATA/BTCUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0B15_ATR_STOP_DIAGNOSTIC.md:34:      06_RESEARCH/DATA/ETHUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0B15_ATR_STOP_DIAGNOSTIC.md:35:      06_RESEARCH/DATA/SOLUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0B12_MAE_ANALYSIS.md:34:        06_RESEARCH/DATA/BTCUSDT_4H.csv 或同目录下的BTC数据
04_AI_TEAM/CODEX_TASKS/TASK_0B12_MAE_ANALYSIS.md:35:        06_RESEARCH/DATA/ETHUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_0B12_MAE_ANALYSIS.md:36:        06_RESEARCH/DATA/SOLUSDT_4H.csv
04_AI_TEAM/CODEX_TASKS/TASK_TSMOM_EXPANSION_DATA.md:10:   - 品种：BNBUSDT、XRPUSDT、DOGEUSDT、ADAUSDT、LTCUSDT（按上市日起至 2026-06-05）
04_AI_TEAM/CODEX_TASKS/REPORT_0A6_SIGNAL_DETECTOR.md:12:- 输出路径：`06_RESEARCH/CODE/output/triple_signals_BTCUSDT_4H.csv`
04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md:9:- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_BTCUSDT.csv`
04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md:10:- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_ETHUSDT.csv`
04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md:11:- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_SOLUSDT.csv`
04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md:26:| BTCUSDT | 2020-09-01T00:00:00Z to 2026-06-06T00:00:00Z | 50497 | 50497 | 4344 | 22 | 2021-01-20T01:00:00Z | 2024-02-16T23:00:00Z |
04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md:27:| ETHUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md:28:| SOLUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
04_AI_TEAM/CODEX_TASKS/REPORT_R2_A2_EVENT_STUDY.md:17:- Price symbols loaded: BTCUSDT, ETHUSDT, SOLUSDT
04_AI_TEAM/CODEX_TASKS/REPORT_P1_03_DATA_RECON_OI.md

04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:47:| 06_RESEARCH/DATA/FUTURES/SOLUSDT_FUNDING_8H_2026H1.csv | 15 | 2026-06-01 00:00:00 | 2026-06-05 16:00:00 | OK | OK | 0 | 0.000000% | 78e9975b010cd52b992335b78f4423f417d251b684ac0f74c99c3e28f6246dcd |
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:51:### 06_RESEARCH/DATA/FUTURES/BNBUSDT_MARK_4H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:61:### 06_RESEARCH/DATA/FUTURES/BNBUSDT_FUNDING_8H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:65:### 06_RESEARCH/DATA/FUTURES/XRPUSDT_MARK_4H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:74:### 06_RESEARCH/DATA/FUTURES/XRPUSDT_FUNDING_8H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:78:### 06_RESEARCH/DATA/FUTURES/DOGEUSDT_MARK_4H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:85:### 06_RESEARCH/DATA/FUTURES/DOGEUSDT_FUNDING_8H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:89:### 06_RESEARCH/DATA/FUTURES/ADAUSDT_MARK_4H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:96:### 06_RESEARCH/DATA/FUTURES/ADAUSDT_FUNDING_8H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:100:### 06_RESEARCH/DATA/FUTURES/LTCUSDT_MARK_4H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:107:### 06_RESEARCH/DATA/FUTURES/LTCUSDT_FUNDING_8H.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:111:### 06_RESEARCH/DATA/FUTURES/BTCUSDT_MARK_4H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:115:### 06_RESEARCH/DATA/FUTURES/BTCUSDT_MARK_1H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:119:### 06_RESEARCH/DATA/FUTURES/BTCUSDT_FUNDING_8H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:123:### 06_RESEARCH/DATA/FUTURES/ETHUSDT_MARK_4H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:127:### 06_RESEARCH/DATA/FUTURES/ETHUSDT_MARK_1H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:131:### 06_RESEARCH/DATA/FUTURES/ETHUSDT_FUNDING_8H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:135:### 06_RESEARCH/DATA/FUTURES/SOLUSDT_MARK_4H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:139:### 06_RESEARCH/DATA/FUTURES/SOLUSDT_MARK_1H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_TSMOM_EXPANSION_DATA.md:143:### 06_RESEARCH/DATA/FUTURES/SOLUSDT_FUNDING_8H_2026H1.csv
04_AI_TEAM/CODEX_TASKS/REPORT_R1_A2_FEATURES.md:17:- 输入：`06_RESEARCH/DATA/FUTURES/{BTCUSDT,ETHUSDT,SOLUSDT}_FUNDING_8H.csv`
04_AI_TEAM/CODEX_TASKS/REPORT_R1_A2_FEATURES.md:39:| BTCUSDT | 540 |
04_AI_TEAM/CODEX_TASKS/REPORT_R1_A2_FEATURES.md:40:| ETHUSDT | 540 |
04_AI_TEAM/CODEX_TASKS/REPORT_R1_A2_FEATURES.md:41:| SOLUSDT | 540 |

exec
/bin/bash -lc "python3 - <<'PY'
import pandas as pd
from pathlib import Path
univ=pd.read_csv('06_RESEARCH/DATA/UNIVERSE_PIT.csv', dtype=str).fillna('')
existing=['BTCUSDT','ETHUSDT','SOLUSDT','BNBUSDT','XRPUSDT','DOGEUSDT','ADAUSDT','LTCUSDT']
print('| symbol | in_universe | onboard_date | delist_date |')
print('|---|---:|---:|---:|')
for sym in existing:
    row=univ[univ.symbol==sym]
    if row.empty:
        print(f'| {sym} | no |  |  |')
    else:
        r=row.iloc[0]
        print(f'| {sym} | yes | {r.onboard_date} | {r.delist_date or \"\"} |')
print('all present', set(existing).issubset(set(univ.symbol)))
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "python3 - <<'PY'
import sys
from pathlib import Path
sys.path.insert(0, '06_RESEARCH/CODE')
from build_universe_pit import pit_symbols_for_date
p=Path('06_RESEARCH/DATA/UNIVERSE_PIT.csv')
for day in ['2019-09-07','2019-09-08','2022-11-14','2025-01-31','2026-06-12']:
    syms=pit_symbols_for_date(p, day)
    print(day, len(syms), 'BTCUSDT' in syms, 'FTTUSDT' in syms, 'OMGUSDT' in syms)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "python3 - <<'PY'
import hashlib
from pathlib import Path
for p in ['06_RESEARCH/DATA/UNIVERSE_PIT.csv','06_RESEARCH/CODE/build_universe_pit.py','06_RESEARCH/CODE/tests/test_universe_pit.py']:
    data=Path(p).read_bytes()
    print(p, len(data), hashlib.sha256(data).hexdigest())
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/DATA/UNIVERSE_PIT.csv 67770 5c137f6c4a69bad6a9153b9cd5fd6966566ce3bb31f8359c8a8a7fc1060b8848
06_RESEARCH/CODE/build_universe_pit.py 10813 d966d22b9b4da11484af38188c90ae835c115621c60766ed9270d65ce846921b
06_RESEARCH/CODE/tests/test_universe_pit.py 1682 78661fe3d54e28c5e55312726335fe9d1e31294152c2fe2f3036f90acec3bb9a

diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

 succeeded in 871ms:
| symbol | in_universe | onboard_date | delist_date |
|---|---:|---:|---:|
| BTCUSDT | yes | 2019-09-08 |  |
| ETHUSDT | yes | 2019-11-27 |  |
| SOLUSDT | yes | 2020-09-14 |  |
| BNBUSDT | yes | 2020-02-10 |  |
| XRPUSDT | yes | 2020-01-06 |  |
| DOGEUSDT | yes | 2020-07-10 |  |
| ADAUSDT | yes | 2020-01-31 |  |
| LTCUSDT | yes | 2020-01-09 |  |
all present True

 succeeded in 899ms:
2019-09-07 0 False False False
2019-09-08 1 True False False
2022-11-14 133 True False True
2025-01-31 354 True False False
2026-06-12 527 True False False

codex
审计完成，PIT 查询在真实 CSV 上能正确排除未来上市和退市日后的合约。现在写执行报告，重点记录 S3 枚举失败的环境事实、官方归档 HEAD 探测兜底、以及“任务书称 9 品种但仓库可审计为 8”的差异。
exec
/bin/bash -lc "date -u '+%Y-%m-%dT%H:%M:%SZ'" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
2026-06-12T07:22:17Z

diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
报告会写入新文件 `04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md`，不覆盖已有文件。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
diff --git a/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md b/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
new file mode 100644
index 0000000000000000000000000000000000000000..6d18854259a03045467a4cbb9936f16465e41d84
--- /dev/null
+++ b/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
@@ -0,0 +1,133 @@
+# REPORT_UNIVERSE_PIT
+
+Generated at: 2026-06-12T07:22:17Z
+
+## 结论
+
+已生成 Binance USDT-M 永续 PIT universe 数据资产：
+
+- DATA: `06_RESEARCH/DATA/UNIVERSE_PIT.csv`
+- CODE: `06_RESEARCH/CODE/build_universe_pit.py`
+- TEST: `06_RESEARCH/CODE/tests/test_universe_pit.py`
+
+本任务只构建数据资产；未运行策略、信号、回测或统计检验；未读取 HOLDOUT 文件内容；未 git commit。
+
+## 口径
+
+- 主数据源：Binance 官方 `https://fapi.binance.com/fapi/v1/exchangeInfo`
+- 过滤条件：`contractType == PERPETUAL`，`quoteAsset == USDT`，`marginAsset == USDT`，排除 `PENDING_TRADING`
+- 日期口径：UTC calendar date
+- `onboard_date`：UTC 上市日期，PIT 查询中包含当日
+- `delist_date`：UTC 下架/交割日期，空值表示仍在市；PIT 查询中退市日为排除边界
+- 归档交叉验证：先尝试 `data.binance.vision` 页面声明的 S3 ListBucket 目录枚举；当前代理环境下 S3 TLS 路径失败，脚本自动回退为官方 `data.binance.vision` monthly/daily klines `HEAD` 探测
+
+## 汇总
+
+| metric | value |
+|---|---:|
+| total rows | 646 |
+| active rows (`delist_date` empty) | 527 |
+| delisted/settling rows | 119 |
+| archive cross-check hits | 646 |
+| archive cross-check misses | 0 |
+| CSV sha256 | `5c137f6c4a69bad6a9153b9cd5fd6966566ce3bb31f8359c8a8a7fc1060b8848` |
+
+## 各年度上市数
+
+| onboard year | count |
+|---:|---:|
+| 2019 | 3 |
+| 2020 | 67 |
+| 2021 | 46 |
+| 2022 | 19 |
+| 2023 | 96 |
+| 2024 | 129 |
+| 2025 | 246 |
+| 2026 | 40 |
+
+## 已知退市永续抽查
+
+| symbol | onboard_date | delist_date | in CSV |
+|---|---:|---:|---:|
+| SCUSDT | 2021-04-12 | 2022-06-17 | yes |
+| FTTUSDT | 2022-04-14 | 2022-11-14 | yes |
+| WAVESUSDT | 2020-08-12 | 2024-06-11 | yes |
+| FTMUSDT | 2020-09-24 | 2025-01-06 | yes |
+| OMGUSDT | 2020-07-02 | 2025-01-31 | yes |
+
+验收要求“退市合约必须出现，抽查 >=3 个”已满足。
+
+## 现有品种覆盖对照
+
+任务书写“现有 9 品种”。仓库内可审计到的现有 TSMOM/扩样本品种为 8 个：`prepare_tsmom_expansion_data.py` 中 `EXISTING_SYMBOLS = BTCUSDT, ETHUSDT, SOLUSDT`，`NEW_SYMBOLS = BNBUSDT, XRPUSDT, DOGEUSDT, ADAUSDT, LTCUSDT`。未发现可审计的第 9 个品种定义，因此未猜测补入。
+
+| symbol | in universe | onboard_date | delist_date |
+|---|---:|---:|---:|
+| BTCUSDT | yes | 2019-09-08 |  |
+| ETHUSDT | yes | 2019-11-27 |  |
+| SOLUSDT | yes | 2020-09-14 |  |
+| BNBUSDT | yes | 2020-02-10 |  |
+| XRPUSDT | yes | 2020-01-06 |  |
+| DOGEUSDT | yes | 2020-07-10 |  |
+| ADAUSDT | yes | 2020-01-31 |  |
+| LTCUSDT | yes | 2020-01-09 |  |
+
+## PIT 查询验证
+
+单元测试覆盖：
+
+- 上市日前不返回该 symbol
+- 上市日返回该 symbol
+- 退市日按排除边界不返回该 symbol
+- PIT 函数可从 CSV path 读取
+
+真实 CSV spot check：
+
+| date | universe size | BTCUSDT | FTTUSDT | OMGUSDT |
+|---:|---:|---:|---:|---:|
+| 2019-09-07 | 0 | no | no | no |
+| 2019-09-08 | 1 | yes | no | no |
+| 2022-11-14 | 133 | yes | no | yes |
+| 2025-01-31 | 354 | yes | no | no |
+| 2026-06-12 | 527 | yes | no | no |
+
+## 数据缺口与可信度
+
+- `exchangeInfo` 当前返回 646 个已上市 USDT-M perpetual 合约，包含 119 个 `SETTLING` 退市/下架合约；这是本资产的 PIT 日期主来源。
+- `data.binance.vision` 官方网页的 `list.js` 声明其目录表来自 `https://s3-ap-northeast-1.amazonaws.com/data.binance.vision`。当前代理 `HTTPS_PROXY=http://127.0.0.1:7897` 下，该 S3 XML 目录枚举路径出现 TLS EOF/证书路径问题，脚本记录失败并回退到官方归档 URL 的 `HEAD` 探测。
+- 回退探测仍使用 Binance 官方 `data.binance.vision/data/futures/um/...` 文件路径，646/646 symbol 至少命中一个 monthly 或 daily 1h kline 归档文件。
+- `exchangeInfo` 中存在 3 个非 ASCII 官方 symbol：`币安人生USDT`、`我踏马来了USDT`、`龙虾USDT`。CSV 保留官方原 symbol；归档 URL 探测对 path 做 percent-encoding。
+- 排除了 `PENDING_TRADING` symbol，因为其尚非历史可交易合约，不应进入 PIT 可交易集合。
+
+## 复算命令
+
+```bash
+export HTTPS_PROXY=http://127.0.0.1:7897
+python3 06_RESEARCH/CODE/build_universe_pit.py
+python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q
+```
+
+本次实际验证：
+
+```text
+python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q
+2 passed in 0.49s
+
+python3 06_RESEARCH/CODE/build_universe_pit.py
+rows=646 delisted=119 archive_verified=646
+```
+
+## 验收自检
+
+| requirement | status | evidence |
+|---|---|---|
+| `06_RESEARCH/DATA/UNIVERSE_PIT.csv` | PASS | 646 rows, sha256 above |
+| 含已退市/已下架 | PASS | 119 rows with non-empty `delist_date`; SCUSDT/FTTUSDT/WAVESUSDT/FTMUSDT/OMGUSDT present |
+| 数据来源标注 | PASS | CSV `source` column |
+| 官方 API + 归档交叉验证 | PASS with caveat | `exchangeInfo` main source; S3 enum attempted; official archive `HEAD` fallback verified 646/646 |
+| 可复跑脚本 | PASS | `06_RESEARCH/CODE/build_universe_pit.py` |
+| 简短 pytest | PASS | `06_RESEARCH/CODE/tests/test_universe_pit.py`, 2 passed |
+| UTC | PASS | UTC date conversion from exchangeInfo millisecond timestamps |
+| 不改既有文件 | PASS | only new task files/data asset created |
+| 不碰 HOLDOUT | PASS | no HOLDOUT file content read |
+| 不 git commit | PASS | no commit executed |
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

diff --git a/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md b/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
new file mode 100644
index 0000000000000000000000000000000000000000..6d18854259a03045467a4cbb9936f16465e41d84
--- /dev/null
+++ b/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
@@ -0,0 +1,133 @@
+# REPORT_UNIVERSE_PIT
+
+Generated at: 2026-06-12T07:22:17Z
+
+## 结论
+
+已生成 Binance USDT-M 永续 PIT universe 数据资产：
+
+- DATA: `06_RESEARCH/DATA/UNIVERSE_PIT.csv`
+- CODE: `06_RESEARCH/CODE/build_universe_pit.py`
+- TEST: `06_RESEARCH/CODE/tests/test_universe_pit.py`
+
+本任务只构建数据资产；未运行策略、信号、回测或统计检验；未读取 HOLDOUT 文件内容；未 git commit。
+
+## 口径
+
+- 主数据源：Binance 官方 `https://fapi.binance.com/fapi/v1/exchangeInfo`
+- 过滤条件：`contractType == PERPETUAL`，`quoteAsset == USDT`，`marginAsset == USDT`，排除 `PENDING_TRADING`
+- 日期口径：UTC calendar date
+- `onboard_date`：UTC 上市日期，PIT 查询中包含当日
+- `delist_date`：UTC 下架/交割日期，空值表示仍在市；PIT 查询中退市日为排除边界
+- 归档交叉验证：先尝试 `data.binance.vision` 页面声明的 S3 ListBucket 目录枚举；当前代理环境下 S3 TLS 路径失败，脚本自动回退为官方 `data.binance.vision` monthly/daily klines `HEAD` 探测
+
+## 汇总
+
+| metric | value |
+|---|---:|
+| total rows | 646 |
+| active rows (`delist_date` empty) | 527 |
+| delisted/settling rows | 119 |
+| archive cross-check hits | 646 |
+| archive cross-check misses | 0 |
+| CSV sha256 | `5c137f6c4a69bad6a9153b9cd5fd6966566ce3bb31f8359c8a8a7fc1060b8848` |
+
+## 各年度上市数
+
+| onboard year | count |
+|---:|---:|
+| 2019 | 3 |
+| 2020 | 67 |
+| 2021 | 46 |
+| 2022 | 19 |
+| 2023 | 96 |
+| 2024 | 129 |
+| 2025 | 246 |
+| 2026 | 40 |
+
+## 已知退市永续抽查
+
+| symbol | onboard_date | delist_date | in CSV |
+|---|---:|---:|---:|
+| SCUSDT | 2021-04-12 | 2022-06-17 | yes |
+| FTTUSDT | 2022-04-14 | 2022-11-14 | yes |
+| WAVESUSDT | 2020-08-12 | 2024-06-11 | yes |
+| FTMUSDT | 2020-09-24 | 2025-01-06 | yes |
+| OMGUSDT | 2020-07-02 | 2025-01-31 | yes |
+
+验收要求“退市合约必须出现，抽查 >=3 个”已满足。
+
+## 现有品种覆盖对照
+
+任务书写“现有 9 品种”。仓库内可审计到的现有 TSMOM/扩样本品种为 8 个：`prepare_tsmom_expansion_data.py` 中 `EXISTING_SYMBOLS = BTCUSDT, ETHUSDT, SOLUSDT`，`NEW_SYMBOLS = BNBUSDT, XRPUSDT, DOGEUSDT, ADAUSDT, LTCUSDT`。未发现可审计的第 9 个品种定义，因此未猜测补入。
+
+| symbol | in universe | onboard_date | delist_date |
+|---|---:|---:|---:|
+| BTCUSDT | yes | 2019-09-08 |  |
+| ETHUSDT | yes | 2019-11-27 |  |
+| SOLUSDT | yes | 2020-09-14 |  |
+| BNBUSDT | yes | 2020-02-10 |  |
+| XRPUSDT | yes | 2020-01-06 |  |
+| DOGEUSDT | yes | 2020-07-10 |  |
+| ADAUSDT | yes | 2020-01-31 |  |
+| LTCUSDT | yes | 2020-01-09 |  |
+
+## PIT 查询验证
+
+单元测试覆盖：
+
+- 上市日前不返回该 symbol
+- 上市日返回该 symbol
+- 退市日按排除边界不返回该 symbol
+- PIT 函数可从 CSV path 读取
+
+真实 CSV spot check：
+
+| date | universe size | BTCUSDT | FTTUSDT | OMGUSDT |
+|---:|---:|---:|---:|---:|
+| 2019-09-07 | 0 | no | no | no |
+| 2019-09-08 | 1 | yes | no | no |
+| 2022-11-14 | 133 | yes | no | yes |
+| 2025-01-31 | 354 | yes | no | no |
+| 2026-06-12 | 527 | yes | no | no |
+
+## 数据缺口与可信度
+
+- `exchangeInfo` 当前返回 646 个已上市 USDT-M perpetual 合约，包含 119 个 `SETTLING` 退市/下架合约；这是本资产的 PIT 日期主来源。
+- `data.binance.vision` 官方网页的 `list.js` 声明其目录表来自 `https://s3-ap-northeast-1.amazonaws.com/data.binance.vision`。当前代理 `HTTPS_PROXY=http://127.0.0.1:7897` 下，该 S3 XML 目录枚举路径出现 TLS EOF/证书路径问题，脚本记录失败并回退到官方归档 URL 的 `HEAD` 探测。
+- 回退探测仍使用 Binance 官方 `data.binance.vision/data/futures/um/...` 文件路径，646/646 symbol 至少命中一个 monthly 或 daily 1h kline 归档文件。
+- `exchangeInfo` 中存在 3 个非 ASCII 官方 symbol：`币安人生USDT`、`我踏马来了USDT`、`龙虾USDT`。CSV 保留官方原 symbol；归档 URL 探测对 path 做 percent-encoding。
+- 排除了 `PENDING_TRADING` symbol，因为其尚非历史可交易合约，不应进入 PIT 可交易集合。
+
+## 复算命令
+
+```bash
+export HTTPS_PROXY=http://127.0.0.1:7897
+python3 06_RESEARCH/CODE/build_universe_pit.py
+python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q
+```
+
+本次实际验证：
+
+```text
+python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q
+2 passed in 0.49s
+
+python3 06_RESEARCH/CODE/build_universe_pit.py
+rows=646 delisted=119 archive_verified=646
+```
+
+## 验收自检
+
+| requirement | status | evidence |
+|---|---|---|
+| `06_RESEARCH/DATA/UNIVERSE_PIT.csv` | PASS | 646 rows, sha256 above |
+| 含已退市/已下架 | PASS | 119 rows with non-empty `delist_date`; SCUSDT/FTTUSDT/WAVESUSDT/FTMUSDT/OMGUSDT present |
+| 数据来源标注 | PASS | CSV `source` column |
+| 官方 API + 归档交叉验证 | PASS with caveat | `exchangeInfo` main source; S3 enum attempted; official archive `HEAD` fallback verified 646/646 |
+| 可复跑脚本 | PASS | `06_RESEARCH/CODE/build_universe_pit.py` |
+| 简短 pytest | PASS | `06_RESEARCH/CODE/tests/test_universe_pit.py`, 2 passed |
+| UTC | PASS | UTC date conversion from exchangeInfo millisecond timestamps |
+| 不改既有文件 | PASS | only new task files/data asset created |
+| 不碰 HOLDOUT | PASS | no HOLDOUT file content read |
+| 不 git commit | PASS | no commit executed |
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
报告已写入。最后我会跑一次目标测试、检查生成文件存在性和 git 状态，确认没有提交且只新增本任务文件。
exec
/bin/bash -lc 'git status --short' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
 M 99_TEMP/codex_carry.log
 M 99_TEMP/codex_universe.log
?? 04_AI_TEAM/CODEX_TASKS/REPORT_CARRY_FEASIBILITY.md
?? 04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
?? 06_RESEARCH/CODE/build_universe_pit.py
?? 06_RESEARCH/CODE/carry_feasibility.py
?? 06_RESEARCH/CODE/output/carry_feasibility_capital_efficiency.csv
?? 06_RESEARCH/CODE/output/carry_feasibility_cost_sensitivity.csv
?? 06_RESEARCH/CODE/output/carry_feasibility_mark_proxy.csv
?? 06_RESEARCH/CODE/output/carry_feasibility_overall.csv
?? 06_RESEARCH/CODE/output/carry_feasibility_summary.json
?? 06_RESEARCH/CODE/output/carry_feasibility_yearly.csv
?? 06_RESEARCH/CODE/tests/test_universe_pit.py
?? 06_RESEARCH/DATA/UNIVERSE_PIT.csv
?? 06_RESEARCH/RESULTS/20260612_carry_feasibility.md

exec
/bin/bash -lc "python3 - <<'PY'
from pathlib import Path
for p in ['06_RESEARCH/DATA/UNIVERSE_PIT.csv','06_RESEARCH/CODE/build_universe_pit.py','06_RESEARCH/CODE/tests/test_universe_pit.py','04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md']:
    path=Path(p)
    print(p, path.exists(), path.stat().st_size if path.exists() else 0)
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/DATA/UNIVERSE_PIT.csv True 67770
06_RESEARCH/CODE/build_universe_pit.py True 10813
06_RESEARCH/CODE/tests/test_universe_pit.py True 1682
04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md True 5372

exec
/bin/bash -lc "python3 - <<'PY'
import pandas as pd
p='06_RESEARCH/DATA/UNIVERSE_PIT.csv'
df=pd.read_csv(p, dtype=str).fillna('')
assert len(df)==646
assert df['delist_date'].ne('').sum()==119
for sym in ['SCUSDT','FTTUSDT','WAVESUSDT','FTMUSDT','OMGUSDT']:
    assert not df[(df.symbol==sym) & (df.delist_date"'!='"'')].empty, sym
print('universe audit ok')
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc 'python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 565ms:
universe audit ok

 succeeded in 803ms:
..                                                                       [100%]
2 passed in 0.38s

diff --git a/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md b/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
new file mode 100644
index 0000000000000000000000000000000000000000..6d18854259a03045467a4cbb9936f16465e41d84
--- /dev/null
+++ b/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
@@ -0,0 +1,133 @@
+# REPORT_UNIVERSE_PIT
+
+Generated at: 2026-06-12T07:22:17Z
+
+## 结论
+
+已生成 Binance USDT-M 永续 PIT universe 数据资产：
+
+- DATA: `06_RESEARCH/DATA/UNIVERSE_PIT.csv`
+- CODE: `06_RESEARCH/CODE/build_universe_pit.py`
+- TEST: `06_RESEARCH/CODE/tests/test_universe_pit.py`
+
+本任务只构建数据资产；未运行策略、信号、回测或统计检验；未读取 HOLDOUT 文件内容；未 git commit。
+
+## 口径
+
+- 主数据源：Binance 官方 `https://fapi.binance.com/fapi/v1/exchangeInfo`
+- 过滤条件：`contractType == PERPETUAL`，`quoteAsset == USDT`，`marginAsset == USDT`，排除 `PENDING_TRADING`
+- 日期口径：UTC calendar date
+- `onboard_date`：UTC 上市日期，PIT 查询中包含当日
+- `delist_date`：UTC 下架/交割日期，空值表示仍在市；PIT 查询中退市日为排除边界
+- 归档交叉验证：先尝试 `data.binance.vision` 页面声明的 S3 ListBucket 目录枚举；当前代理环境下 S3 TLS 路径失败，脚本自动回退为官方 `data.binance.vision` monthly/daily klines `HEAD` 探测
+
+## 汇总
+
+| metric | value |
+|---|---:|
+| total rows | 646 |
+| active rows (`delist_date` empty) | 527 |
+| delisted/settling rows | 119 |
+| archive cross-check hits | 646 |
+| archive cross-check misses | 0 |
+| CSV sha256 | `5c137f6c4a69bad6a9153b9cd5fd6966566ce3bb31f8359c8a8a7fc1060b8848` |
+
+## 各年度上市数
+
+| onboard year | count |
+|---:|---:|
+| 2019 | 3 |
+| 2020 | 67 |
+| 2021 | 46 |
+| 2022 | 19 |
+| 2023 | 96 |
+| 2024 | 129 |
+| 2025 | 246 |
+| 2026 | 40 |
+
+## 已知退市永续抽查
+
+| symbol | onboard_date | delist_date | in CSV |
+|---|---:|---:|---:|
+| SCUSDT | 2021-04-12 | 2022-06-17 | yes |
+| FTTUSDT | 2022-04-14 | 2022-11-14 | yes |
+| WAVESUSDT | 2020-08-12 | 2024-06-11 | yes |
+| FTMUSDT | 2020-09-24 | 2025-01-06 | yes |
+| OMGUSDT | 2020-07-02 | 2025-01-31 | yes |
+
+验收要求“退市合约必须出现，抽查 >=3 个”已满足。
+
+## 现有品种覆盖对照
+
+任务书写“现有 9 品种”。仓库内可审计到的现有 TSMOM/扩样本品种为 8 个：`prepare_tsmom_expansion_data.py` 中 `EXISTING_SYMBOLS = BTCUSDT, ETHUSDT, SOLUSDT`，`NEW_SYMBOLS = BNBUSDT, XRPUSDT, DOGEUSDT, ADAUSDT, LTCUSDT`。未发现可审计的第 9 个品种定义，因此未猜测补入。
+
+| symbol | in universe | onboard_date | delist_date |
+|---|---:|---:|---:|
+| BTCUSDT | yes | 2019-09-08 |  |
+| ETHUSDT | yes | 2019-11-27 |  |
+| SOLUSDT | yes | 2020-09-14 |  |
+| BNBUSDT | yes | 2020-02-10 |  |
+| XRPUSDT | yes | 2020-01-06 |  |
+| DOGEUSDT | yes | 2020-07-10 |  |
+| ADAUSDT | yes | 2020-01-31 |  |
+| LTCUSDT | yes | 2020-01-09 |  |
+
+## PIT 查询验证
+
+单元测试覆盖：
+
+- 上市日前不返回该 symbol
+- 上市日返回该 symbol
+- 退市日按排除边界不返回该 symbol
+- PIT 函数可从 CSV path 读取
+
+真实 CSV spot check：
+
+| date | universe size | BTCUSDT | FTTUSDT | OMGUSDT |
+|---:|---:|---:|---:|---:|
+| 2019-09-07 | 0 | no | no | no |
+| 2019-09-08 | 1 | yes | no | no |
+| 2022-11-14 | 133 | yes | no | yes |
+| 2025-01-31 | 354 | yes | no | no |
+| 2026-06-12 | 527 | yes | no | no |
+
+## 数据缺口与可信度
+
+- `exchangeInfo` 当前返回 646 个已上市 USDT-M perpetual 合约，包含 119 个 `SETTLING` 退市/下架合约；这是本资产的 PIT 日期主来源。
+- `data.binance.vision` 官方网页的 `list.js` 声明其目录表来自 `https://s3-ap-northeast-1.amazonaws.com/data.binance.vision`。当前代理 `HTTPS_PROXY=http://127.0.0.1:7897` 下，该 S3 XML 目录枚举路径出现 TLS EOF/证书路径问题，脚本记录失败并回退到官方归档 URL 的 `HEAD` 探测。
+- 回退探测仍使用 Binance 官方 `data.binance.vision/data/futures/um/...` 文件路径，646/646 symbol 至少命中一个 monthly 或 daily 1h kline 归档文件。
+- `exchangeInfo` 中存在 3 个非 ASCII 官方 symbol：`币安人生USDT`、`我踏马来了USDT`、`龙虾USDT`。CSV 保留官方原 symbol；归档 URL 探测对 path 做 percent-encoding。
+- 排除了 `PENDING_TRADING` symbol，因为其尚非历史可交易合约，不应进入 PIT 可交易集合。
+
+## 复算命令
+
+```bash
+export HTTPS_PROXY=http://127.0.0.1:7897
+python3 06_RESEARCH/CODE/build_universe_pit.py
+python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q
+```
+
+本次实际验证：
+
+```text
+python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q
+2 passed in 0.49s
+
+python3 06_RESEARCH/CODE/build_universe_pit.py
+rows=646 delisted=119 archive_verified=646
+```
+
+## 验收自检
+
+| requirement | status | evidence |
+|---|---|---|
+| `06_RESEARCH/DATA/UNIVERSE_PIT.csv` | PASS | 646 rows, sha256 above |
+| 含已退市/已下架 | PASS | 119 rows with non-empty `delist_date`; SCUSDT/FTTUSDT/WAVESUSDT/FTMUSDT/OMGUSDT present |
+| 数据来源标注 | PASS | CSV `source` column |
+| 官方 API + 归档交叉验证 | PASS with caveat | `exchangeInfo` main source; S3 enum attempted; official archive `HEAD` fallback verified 646/646 |
+| 可复跑脚本 | PASS | `06_RESEARCH/CODE/build_universe_pit.py` |
+| 简短 pytest | PASS | `06_RESEARCH/CODE/tests/test_universe_pit.py`, 2 passed |
+| UTC | PASS | UTC date conversion from exchangeInfo millisecond timestamps |
+| 不改既有文件 | PASS | only new task files/data asset created |
+| 不碰 HOLDOUT | PASS | no HOLDOUT file content read |
+| 不 git commit | PASS | no commit executed |
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

codex
已完成 `TASK_UNIVERSE_PIT`，未 git commit。

新增文件：
- [UNIVERSE_PIT.csv](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/DATA/UNIVERSE_PIT.csv)
- [build_universe_pit.py](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py)
- [test_universe_pit.py](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/tests/test_universe_pit.py)
- [REPORT_UNIVERSE_PIT.md](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md)

关键结果：
- universe 总数：646
- 退市/下架：119
- 官方归档交叉验证：646/646 命中
- 已知退市抽查通过：`SCUSDT`、`FTTUSDT`、`WAVESUSDT`、`FTMUSDT`、`OMGUSDT`
- PIT pytest：`2 passed`

注意事项已写入报告：`data.binance.vision` 的 S3 XML 目录枚举在当前代理下 TLS 失败，脚本自动回退到官方归档 monthly/daily klines `HEAD` 探测；任务书称“现有 9 品种”，但仓库内可审计到的是 8 个，未猜测第 9 个。未读取 HOLDOUT 文件内容。
diff --git a/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md b/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
new file mode 100644
index 0000000000000000000000000000000000000000..6d18854259a03045467a4cbb9936f16465e41d84
--- /dev/null
+++ b/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md
@@ -0,0 +1,133 @@
+# REPORT_UNIVERSE_PIT
+
+Generated at: 2026-06-12T07:22:17Z
+
+## 结论
+
+已生成 Binance USDT-M 永续 PIT universe 数据资产：
+
+- DATA: `06_RESEARCH/DATA/UNIVERSE_PIT.csv`
+- CODE: `06_RESEARCH/CODE/build_universe_pit.py`
+- TEST: `06_RESEARCH/CODE/tests/test_universe_pit.py`
+
+本任务只构建数据资产；未运行策略、信号、回测或统计检验；未读取 HOLDOUT 文件内容；未 git commit。
+
+## 口径
+
+- 主数据源：Binance 官方 `https://fapi.binance.com/fapi/v1/exchangeInfo`
+- 过滤条件：`contractType == PERPETUAL`，`quoteAsset == USDT`，`marginAsset == USDT`，排除 `PENDING_TRADING`
+- 日期口径：UTC calendar date
+- `onboard_date`：UTC 上市日期，PIT 查询中包含当日
+- `delist_date`：UTC 下架/交割日期，空值表示仍在市；PIT 查询中退市日为排除边界
+- 归档交叉验证：先尝试 `data.binance.vision` 页面声明的 S3 ListBucket 目录枚举；当前代理环境下 S3 TLS 路径失败，脚本自动回退为官方 `data.binance.vision` monthly/daily klines `HEAD` 探测
+
+## 汇总
+
+| metric | value |
+|---|---:|
+| total rows | 646 |
+| active rows (`delist_date` empty) | 527 |
+| delisted/settling rows | 119 |
+| archive cross-check hits | 646 |
+| archive cross-check misses | 0 |
+| CSV sha256 | `5c137f6c4a69bad6a9153b9cd5fd6966566ce3bb31f8359c8a8a7fc1060b8848` |
+
+## 各年度上市数
+
+| onboard year | count |
+|---:|---:|
+| 2019 | 3 |
+| 2020 | 67 |
+| 2021 | 46 |
+| 2022 | 19 |
+| 2023 | 96 |
+| 2024 | 129 |
+| 2025 | 246 |
+| 2026 | 40 |
+
+## 已知退市永续抽查
+
+| symbol | onboard_date | delist_date | in CSV |
+|---|---:|---:|---:|
+| SCUSDT | 2021-04-12 | 2022-06-17 | yes |
+| FTTUSDT | 2022-04-14 | 2022-11-14 | yes |
+| WAVESUSDT | 2020-08-12 | 2024-06-11 | yes |
+| FTMUSDT | 2020-09-24 | 2025-01-06 | yes |
+| OMGUSDT | 2020-07-02 | 2025-01-31 | yes |
+
+验收要求“退市合约必须出现，抽查 >=3 个”已满足。
+
+## 现有品种覆盖对照
+
+任务书写“现有 9 品种”。仓库内可审计到的现有 TSMOM/扩样本品种为 8 个：`prepare_tsmom_expansion_data.py` 中 `EXISTING_SYMBOLS = BTCUSDT, ETHUSDT, SOLUSDT`，`NEW_SYMBOLS = BNBUSDT, XRPUSDT, DOGEUSDT, ADAUSDT, LTCUSDT`。未发现可审计的第 9 个品种定义，因此未猜测补入。
+
+| symbol | in universe | onboard_date | delist_date |
+|---|---:|---:|---:|
+| BTCUSDT | yes | 2019-09-08 |  |
+| ETHUSDT | yes | 2019-11-27 |  |
+| SOLUSDT | yes | 2020-09-14 |  |
+| BNBUSDT | yes | 2020-02-10 |  |
+| XRPUSDT | yes | 2020-01-06 |  |
+| DOGEUSDT | yes | 2020-07-10 |  |
+| ADAUSDT | yes | 2020-01-31 |  |
+| LTCUSDT | yes | 2020-01-09 |  |
+
+## PIT 查询验证
+
+单元测试覆盖：
+
+- 上市日前不返回该 symbol
+- 上市日返回该 symbol
+- 退市日按排除边界不返回该 symbol
+- PIT 函数可从 CSV path 读取
+
+真实 CSV spot check：
+
+| date | universe size | BTCUSDT | FTTUSDT | OMGUSDT |
+|---:|---:|---:|---:|---:|
+| 2019-09-07 | 0 | no | no | no |
+| 2019-09-08 | 1 | yes | no | no |
+| 2022-11-14 | 133 | yes | no | yes |
+| 2025-01-31 | 354 | yes | no | no |
+| 2026-06-12 | 527 | yes | no | no |
+
+## 数据缺口与可信度
+
+- `exchangeInfo` 当前返回 646 个已上市 USDT-M perpetual 合约，包含 119 个 `SETTLING` 退市/下架合约；这是本资产的 PIT 日期主来源。
+- `data.binance.vision` 官方网页的 `list.js` 声明其目录表来自 `https://s3-ap-northeast-1.amazonaws.com/data.binance.vision`。当前代理 `HTTPS_PROXY=http://127.0.0.1:7897` 下，该 S3 XML 目录枚举路径出现 TLS EOF/证书路径问题，脚本记录失败并回退到官方归档 URL 的 `HEAD` 探测。
+- 回退探测仍使用 Binance 官方 `data.binance.vision/data/futures/um/...` 文件路径，646/646 symbol 至少命中一个 monthly 或 daily 1h kline 归档文件。
+- `exchangeInfo` 中存在 3 个非 ASCII 官方 symbol：`币安人生USDT`、`我踏马来了USDT`、`龙虾USDT`。CSV 保留官方原 symbol；归档 URL 探测对 path 做 percent-encoding。
+- 排除了 `PENDING_TRADING` symbol，因为其尚非历史可交易合约，不应进入 PIT 可交易集合。
+
+## 复算命令
+
+```bash
+export HTTPS_PROXY=http://127.0.0.1:7897
+python3 06_RESEARCH/CODE/build_universe_pit.py
+python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q
+```
+
+本次实际验证：
+
+```text
+python3 -m pytest 06_RESEARCH/CODE/tests/test_universe_pit.py -q
+2 passed in 0.49s
+
+python3 06_RESEARCH/CODE/build_universe_pit.py
+rows=646 delisted=119 archive_verified=646
+```
+
+## 验收自检
+
+| requirement | status | evidence |
+|---|---|---|
+| `06_RESEARCH/DATA/UNIVERSE_PIT.csv` | PASS | 646 rows, sha256 above |
+| 含已退市/已下架 | PASS | 119 rows with non-empty `delist_date`; SCUSDT/FTTUSDT/WAVESUSDT/FTMUSDT/OMGUSDT present |
+| 数据来源标注 | PASS | CSV `source` column |
+| 官方 API + 归档交叉验证 | PASS with caveat | `exchangeInfo` main source; S3 enum attempted; official archive `HEAD` fallback verified 646/646 |
+| 可复跑脚本 | PASS | `06_RESEARCH/CODE/build_universe_pit.py` |
+| 简短 pytest | PASS | `06_RESEARCH/CODE/tests/test_universe_pit.py`, 2 passed |
+| UTC | PASS | UTC date conversion from exchangeInfo millisecond timestamps |
+| 不改既有文件 | PASS | only new task files/data asset created |
+| 不碰 HOLDOUT | PASS | no HOLDOUT file content read |
+| 不 git commit | PASS | no commit executed |
diff --git a/06_RESEARCH/CODE/build_universe_pit.py b/06_RESEARCH/CODE/build_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..cef83fcac4fb020645cbd8869b543d1836b0ff43
--- /dev/null
+++ b/06_RESEARCH/CODE/build_universe_pit.py
@@ -0,0 +1,300 @@
+"""Build Binance USD-M perpetual point-in-time universe.
+
+This script builds a data asset only. It does not run strategies,
+signals, backtests, or statistical tests.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import time
+import xml.etree.ElementTree as ET
+from concurrent.futures import ThreadPoolExecutor, as_completed
+from datetime import date, datetime, timezone
+from pathlib import Path
+from urllib.error import HTTPError, URLError
+from urllib.parse import quote, urlencode
+from urllib.request import Request, urlopen
+
+import pandas as pd
+
+
+PROJECT_ROOT = Path(__file__).resolve().parents[2]
+OUTPUT_PATH = PROJECT_ROOT / "06_RESEARCH/DATA/UNIVERSE_PIT.csv"
+EXCHANGE_INFO_URL = "https://fapi.binance.com/fapi/v1/exchangeInfo"
+ARCHIVE_BASE_URL = "https://data.binance.vision/data/futures/um"
+S3_LIST_URL = "https://s3-ap-northeast-1.amazonaws.com/data.binance.vision"
+SENTINEL_DELIVERY_MS = 4133404800000
+SOURCE_API = "Binance fapi/v1/exchangeInfo"
+SOURCE_ARCHIVE_ENUM = "data.binance.vision S3 ListBucket monthly klines"
+SOURCE_ARCHIVE_PROBE = "data.binance.vision monthly/daily klines HEAD probe"
+
+
+def utc_date_from_ms(value: int) -> date:
+    return datetime.fromtimestamp(value / 1000, tz=timezone.utc).date()
+
+
+def month_floor(day: date) -> date:
+    return date(day.year, day.month, 1)
+
+
+def next_month(day: date) -> date:
+    if day.month == 12:
+        return date(day.year + 1, 1, 1)
+    return date(day.year, day.month + 1, 1)
+
+
+def iter_months(start: date, end: date) -> list[str]:
+    months = []
+    cursor = month_floor(start)
+    end_month = month_floor(end)
+    while cursor <= end_month:
+        months.append(cursor.strftime("%Y-%m"))
+        cursor = next_month(cursor)
+    return months
+
+
+def fetch_json(url: str, retries: int = 3) -> dict:
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(url, timeout=45) as response:
+                return json.loads(response.read().decode("utf-8"))
+        except (TimeoutError, URLError, OSError) as exc:
+            if attempt == retries:
+                raise RuntimeError(f"failed to fetch {url}") from exc
+            time.sleep(1.5 * attempt)
+    raise AssertionError("unreachable")
+
+
+def fetch_exchange_info() -> list[dict]:
+    payload = fetch_json(EXCHANGE_INFO_URL)
+    rows = []
+    for item in payload["symbols"]:
+        if item.get("contractType") != "PERPETUAL":
+            continue
+        if item.get("quoteAsset") != "USDT" or item.get("marginAsset") != "USDT":
+            continue
+        if item.get("status") == "PENDING_TRADING":
+            continue
+        rows.append(item)
+    return rows
+
+
+def s3_list_prefixes(prefix: str, retries: int = 2) -> set[str]:
+    """List first-level S3 prefixes under the official Binance archive path.
+
+    The public website's list.js calls this same bucket endpoint. Some proxy
+    environments block the S3 TLS path; callers should fall back to direct
+    official archive URL probes when this raises.
+    """
+
+    prefixes: set[str] = set()
+    marker = ""
+    while True:
+        params = {"delimiter": "/", "prefix": prefix}
+        if marker:
+            params["marker"] = marker
+        url = f"{S3_LIST_URL}?{urlencode(params)}"
+        for attempt in range(1, retries + 1):
+            try:
+                with urlopen(url, timeout=45) as response:
+                    body = response.read()
+                break
+            except (TimeoutError, URLError, OSError) as exc:
+                if attempt == retries:
+                    raise RuntimeError(f"failed to list archive prefix {prefix}") from exc
+                time.sleep(1.5 * attempt)
+        root = ET.fromstring(body)
+        namespace = ""
+        if root.tag.startswith("{"):
+            namespace = root.tag.split("}", 1)[0] + "}"
+        for node in root.findall(f"{namespace}CommonPrefixes/{namespace}Prefix"):
+            if node.text:
+                prefixes.add(node.text)
+        truncated = root.findtext(f"{namespace}IsTruncated")
+        if truncated != "true":
+            return prefixes
+        next_marker = root.findtext(f"{namespace}NextMarker")
+        if not next_marker:
+            return prefixes
+        marker = next_marker
+
+
+def archive_url(symbol: str, interval: str, period: str, frequency: str) -> str:
+    quoted_symbol = quote(symbol, safe="")
+    return (
+        f"{ARCHIVE_BASE_URL}/{frequency}/klines/{quoted_symbol}/{interval}/"
+        f"{quoted_symbol}-{interval}-{period}.zip"
+    )
+
+
+def url_exists(url: str, retries: int = 2) -> bool:
+    request = Request(url, method="HEAD")
+    for attempt in range(1, retries + 1):
+        try:
+            with urlopen(request, timeout=20) as response:
+                return response.status == 200
+        except HTTPError as exc:
+            if exc.code == 404:
+                return False
+            if attempt == retries:
+                return False
+        except (TimeoutError, URLError, OSError):
+            if attempt == retries:
+                return False
+            time.sleep(1.0 * attempt)
+    return False
+
+
+def first_existing_archive(symbol: str, onboard: date, end: date) -> str | None:
+    monthly_periods = iter_months(onboard, end)
+    for month in monthly_periods:
+        url = archive_url(symbol, "1h", month, "monthly")
+        if url_exists(url):
+            return url
+
+    daily_start = max(onboard, end.replace(day=1))
+    daily_days = pd.date_range(daily_start, end, freq="D").date
+    for day in daily_days[:14]:
+        period = day.strftime("%Y-%m-%d")
+        url = archive_url(symbol, "1h", period, "daily")
+        if url_exists(url):
+            return url
+    return None
+
+
+def archive_presence_by_probe(rows: list[dict], workers: int) -> dict[str, str | None]:
+    today = datetime.now(timezone.utc).date()
+    last_complete_month = month_floor(today) - pd.Timedelta(days=1)
+    capped_today = today
+
+    def check(item: dict) -> tuple[str, str | None]:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if delivery_ms >= SENTINEL_DELIVERY_MS or item.get("status") == "TRADING":
+            end = capped_today
+        else:
+            end = min(utc_date_from_ms(delivery_ms), capped_today)
+        monthly_end = min(end, last_complete_month)
+        if monthly_end < onboard:
+            monthly_end = end
+        return item["symbol"], first_existing_archive(item["symbol"], onboard, monthly_end)
+
+    found: dict[str, str | None] = {}
+    with ThreadPoolExecutor(max_workers=workers) as executor:
+        futures = {executor.submit(check, row): row["symbol"] for row in rows}
+        for count, future in enumerate(as_completed(futures), start=1):
+            symbol, url = future.result()
+            found[symbol] = url
+            if count % 100 == 0 or count == len(futures):
+                print(f"Archive cross-check {count}/{len(futures)}", flush=True)
+    return found
+
+
+def build_frame(rows: list[dict], archive_ok: dict[str, bool], source_mode: str) -> pd.DataFrame:
+    output_rows = []
+    for item in rows:
+        onboard = utc_date_from_ms(int(item["onboardDate"]))
+        delivery_ms = int(item.get("deliveryDate") or SENTINEL_DELIVERY_MS)
+        if item.get("status") == "TRADING" or delivery_ms >= SENTINEL_DELIVERY_MS:
+            delist_text = ""
+        else:
+            delist_text = utc_date_from_ms(delivery_ms).isoformat()
+        source_parts = [SOURCE_API]
+        if archive_ok.get(item["symbol"], False):
+            source_parts.append(source_mode)
+        else:
+            source_parts.append(f"{source_mode} missing")
+        output_rows.append(
+            {
+                "symbol": item["symbol"],
+                "onboard_date": onboard.isoformat(),
+                "delist_date": delist_text,
+                "source": "; ".join(source_parts),
+            }
+        )
+    frame = pd.DataFrame(output_rows)
+    return frame.sort_values(["onboard_date", "symbol"]).reset_index(drop=True)
+
+
+def pit_symbols_for_date(universe: pd.DataFrame | str | Path, target_date: str | date) -> list[str]:
+    """Return symbols tradable for a UTC calendar date.
+
+    Day-level convention: onboard_date is inclusive, delist_date is exclusive.
+    """
+
+    if isinstance(universe, (str, Path)):
+        frame = pd.read_csv(universe, dtype=str).fillna("")
+    else:
+        frame = universe.copy().fillna("")
+    if isinstance(target_date, str):
+        day = datetime.fromisoformat(target_date).date()
+    else:
+        day = target_date
+    onboard = pd.to_datetime(frame["onboard_date"], utc=True).dt.date
+    delist_raw = frame["delist_date"].replace("", pd.NA)
+    delist = pd.to_datetime(delist_raw, utc=True, errors="coerce").dt.date
+    mask = (onboard <= day) & (delist.isna() | (day < delist))
+    return sorted(frame.loc[mask, "symbol"].tolist())
+
+
+def write_csv(frame: pd.DataFrame, path: Path) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    frame.to_csv(
+        path,
+        index=False,
+        quoting=csv.QUOTE_MINIMAL,
+        lineterminator="\n",
+    )
+
+
+def parse_args() -> argparse.Namespace:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--output", type=Path, default=OUTPUT_PATH)
+    parser.add_argument("--workers", type=int, default=24)
+    parser.add_argument(
+        "--skip-s3-enum",
+        action="store_true",
+        help="Skip S3 XML directory enumeration and use archive URL probes.",
+    )
+    return parser.parse_args()
+
+
+def main() -> None:
+    args = parse_args()
+    rows = fetch_exchange_info()
+    archive_ok: dict[str, bool]
+    source_mode = SOURCE_ARCHIVE_ENUM
+    if args.skip_s3_enum:
+        prefixes = set()
+    else:
+        try:
+            prefixes = s3_list_prefixes("data/futures/um/monthly/klines/")
+        except RuntimeError as exc:
+            print(f"S3 directory enumeration unavailable: {exc}", flush=True)
+            prefixes = set()
+
+    if prefixes:
+        symbols_in_archive = {
+            prefix.rstrip("/").split("/")[-1]
+            for prefix in prefixes
+            if prefix.rstrip("/").split("/")[-1].endswith("USDT")
+        }
+        archive_ok = {row["symbol"]: row["symbol"] in symbols_in_archive for row in rows}
+    else:
+        source_mode = SOURCE_ARCHIVE_PROBE
+        found = archive_presence_by_probe(rows, args.workers)
+        archive_ok = {symbol: url is not None for symbol, url in found.items()}
+
+    frame = build_frame(rows, archive_ok, source_mode)
+    write_csv(frame, args.output)
+    delisted = int(frame["delist_date"].ne("").sum())
+    verified = int((~frame["source"].str.contains("missing", regex=False)).sum())
+    print(f"Wrote {args.output}")
+    print(f"rows={len(frame)} delisted={delisted} archive_verified={verified}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/tests/test_universe_pit.py b/06_RESEARCH/CODE/tests/test_universe_pit.py
new file mode 100644
index 0000000000000000000000000000000000000000..542abec91cef5fd7091bf4ac9b219041132765a0
--- /dev/null
+++ b/06_RESEARCH/CODE/tests/test_universe_pit.py
@@ -0,0 +1,54 @@
+"""Tests for Binance USD-M point-in-time universe helpers."""
+
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from build_universe_pit import pit_symbols_for_date
+
+
+def test_pit_symbols_exclude_future_and_delisted_contracts() -> None:
+    universe = pd.DataFrame(
+        [
+            {
+                "symbol": "OLDUSDT",
+                "onboard_date": "2020-01-01",
+                "delist_date": "2020-02-01",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "LIVEUSDT",
+                "onboard_date": "2020-01-15",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+            {
+                "symbol": "FUTUREUSDT",
+                "onboard_date": "2020-03-01",
+                "delist_date": "",
+                "source": "synthetic",
+            },
+        ]
+    )
+
+    assert pit_symbols_for_date(universe, "2019-12-31") == []
+    assert pit_symbols_for_date(universe, "2020-01-01") == ["OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-01-15") == ["LIVEUSDT", "OLDUSDT"]
+    assert pit_symbols_for_date(universe, "2020-02-01") == ["LIVEUSDT"]
+
+
+def test_pit_symbols_can_read_csv_path(tmp_path: Path) -> None:
+    path = tmp_path / "universe.csv"
+    path.write_text(
+        "symbol,onboard_date,delist_date,source\n"
+        "BTCUSDT,2019-09-08,,synthetic\n"
+        "DELISTEDUSDT,2020-01-01,2021-01-01,synthetic\n",
+        encoding="utf-8",
+    )
+
+    assert pit_symbols_for_date(path, "2020-06-01") == ["BTCUSDT", "DELISTEDUSDT"]
+    assert pit_symbols_for_date(path, "2021-01-01") == ["BTCUSDT"]

tokens used
168,438
已完成 `TASK_UNIVERSE_PIT`，未 git commit。

新增文件：
- [UNIVERSE_PIT.csv](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/DATA/UNIVERSE_PIT.csv)
- [build_universe_pit.py](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/build_universe_pit.py)
- [test_universe_pit.py](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/tests/test_universe_pit.py)
- [REPORT_UNIVERSE_PIT.md](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/04_AI_TEAM/CODEX_TASKS/REPORT_UNIVERSE_PIT.md)

关键结果：
- universe 总数：646
- 退市/下架：119
- 官方归档交叉验证：646/646 命中
- 已知退市抽查通过：`SCUSDT`、`FTTUSDT`、`WAVESUSDT`、`FTMUSDT`、`OMGUSDT`
- PIT pytest：`2 passed`

注意事项已写入报告：`data.binance.vision` 的 S3 XML 目录枚举在当前代理下 TLS 失败，脚本自动回退到官方归档 monthly/daily klines `HEAD` 探测；任务书称“现有 9 品种”，但仓库内可审计到的是 8 个，未猜测第 9 个。未读取 HOLDOUT 文件内容。
