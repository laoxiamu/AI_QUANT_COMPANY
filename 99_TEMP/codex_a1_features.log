Reading additional input from stdin...
OpenAI Codex v0.139.0
--------
workdir: /Users/yaomingyu/Documents/AI_QUANT_COMPANY
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: none
session id: 019eb949-cf94-7753-ae74-688bb258880c
--------
user
执行任务书 04_AI_TEAM/CODEX_TASKS/TASK_A1_OI_FEATURES.md。先读任务书与项目根 AGENTS.md。只建特征层，严守禁止项（不事件/不Holdout/不收益）。
codex
我会先读取根目录 `AGENTS.md` 和指定任务书，确认任务边界、验收标准和禁止项后再动代码。
exec
/bin/bash -lc "sed -n '1,260p' 04_AI_TEAM/CODEX_TASKS/TASK_A1_OI_FEATURES.md" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "sed -n '1,240p' AGENTS.md" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
# TASK_A1_OI_FEATURES —— A-1 清算级联 OI 特征层（只特征，不事件、不Holdout、不收益）

**发包：** Claude 2026-06-12 ｜ **执行：** Codex（直调）｜ **定位：** A-1 预登记前的可复用数据层。**事件提取/阈值/Holdout 切分一律禁止**——那些必须等 A-1 预登记冻结后另案（A-2 的教训：先冻口径再动手）。

## 交付物

1. `06_RESEARCH/CODE/features/a1_oi_features.py`：
   - 读 `06_RESEARCH/DATA/FUTURES/{SYM}_METRICS_5M.csv`（BTC/ETH/SOL），`sum_open_interest` 重采样到 1H（取每小时最后值）。
   - 计算 Δ6h、Δ24h 百分比变化；各自做**滚动 365 天、最少 180 天起算的无前视分位**（midrank 口径与 `features/a2_funding_features.py` 一致——直接 import 复用 `empirical_percentile`/滚动逻辑，不要复制粘贴）。
   - 输出 `06_RESEARCH/CODE/output/a1_oi_features_{SYM}.csv`：列 = ts(UTC), oi, d6h_pct, d24h_pct, d6h_rolling_pctl, d24h_rolling_pctl, warmup。
2. `06_RESEARCH/CODE/features/test_a1_oi_features.py`（pytest）：①无前视（构造序列验证 t 分位不含 ≥t 数据）②重采样正确性（5M→1H 取最后值，含缺口小时为 NaN 不前向填充）。
3. 报告 `04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md`：各品种覆盖区间（注意 ETH/SOL METRICS 始于 2021-12）、warmup 排除量、1H 缺口统计、复算命令、验收自检。

## 验收标准
pytest 全绿且无前视测试非恒真；UTC；输出行数≈各品种 1H 网格；不读 MARK/FUNDING 之外多余数据（funding 不需要）；无任何事件/阈值/Holdout 代码路径。

## 禁止
事件提取、阈值选择、Holdout、读 HOLDOUT 目录、收益计算、git commit（Claude 验收后统一提交）、改 a2 既有模块。

 succeeded in 0ms:


# AGENTS.md — AI Quant Company（Codex 实现会话指令）

## 角色
你是本项目的实现工程师（Codex），对交付代码与报告的**正确性、可复现性、可审计性**负责。判断与方向由 Claude（主理人/CTO）负责，但你有**专业异议义务**：发现任务书的研究方向/方法有问题，先暂停并书面提异议，不机械执行（DEC-057）。

## 任务执行前强制自查（七问精简版）
①任务在验证什么机制？②任务书的验收标准是否可量化？③有无更便宜的等效实现？④是否会触碰禁止项（见下）？任一答不出 → 先问 Claude，不要猜。

## 研究铁律（Research Protocol v1.2 + v1.3 增补件，冲突以 v1.3 为准）
1. 单变量原则；预登记后禁改假设；结论诚实（禁"有一定效果"类模糊话）。
2. 成本完整：手续费 0.1%/边 + 滑点 0.1%（保守）+ 真实 funding。**事件类策略必须加报滑点 0.3/0.5/1.0% 压力档**（v1.3 §二）。
3. 验收四件套（v1.3 §一）：E[R]>0 / 赢亏比≥1.5 / 分档爆仓概率（块bootstrap，seed预登记）/ 年化log增长>0 且分年正期望占多数。旧门槛（Sharpe>1、MaxDD<25%、Expectancy>1.0）作废。
4. WF 硬门槛；Holdout 物理封存、**任何情况下不得读取**；事件研究按 v1.3 §四（episode<300 用池化+单调性，禁60/20/20三分）。
5. 阈值/分位数一律滚动或扩张窗口计算，**禁全样本分位（前视）**。

## 工程纪律
- 库纪律（DEC-061）：Triple Barrier/Purged CV/统计校验等一律**自实现为可审计小函数**；MLFinPy 等仅作参考，不引硬依赖。
- 持仓/状态以 DB 或文件为权威，禁内存字典（V4 事故根因）。
- 输出三件套：CODE（可复跑脚本+固定seed）/ RESULTS（结论md）/ CODEX_TASKS 执行报告（含验收标准逐条自检）。
- 数据：只读 06_RESEARCH/DATA/；产出写 06_RESEARCH/CODE/output/；UTC 时间戳。
- git：每任务完成 commit，message 带任务号。

## 禁止项
碰 Holdout｜改预登记文档｜简化成本模型｜全样本分位｜引入不可审计黑箱依赖｜把失败结果写成"部分成功"｜超任务书范围"顺手优化"。

## 交接
完成后报告写入 04_AI_TEAM/CODEX_TASKS/REPORT_[任务号].md，等 Claude 验收；验收不过按退回意见修，不抗辩成本与工期以外的验收标准。

exec
/bin/bash -lc "pwd && rg --files -g 'AGENTS.md' -g 'TASK_A1_OI_FEATURES.md'" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
/Users/yaomingyu/Documents/AI_QUANT_COMPANY
04_AI_TEAM/CODEX_TASKS/TASK_A1_OI_FEATURES.md
AGENTS.md

codex
任务边界清楚：这是预登记前的数据层建设，只允许读 FUTURES METRICS，不能做事件、阈值、Holdout 或收益。接下来我会检查现有 `a2_funding_features.py` 的滚动分位实现和项目测试布局，然后按同一口径接入。
exec
/bin/bash -lc "rg --files -g 'AGENTS.md' -g 'a2_funding_features.py' -g 'test_*features*.py' 06_RESEARCH 04_AI_TEAM" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/CODE/features/a2_funding_features.py
06_RESEARCH/CODE/features/test_a2_features.py

exec
/bin/bash -lc "find 06_RESEARCH/DATA/FUTURES -maxdepth 1 -type f -name '*_METRICS_5M.csv' -print" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/DATA/FUTURES/SOLUSDT_METRICS_5M.csv
06_RESEARCH/DATA/FUTURES/ETHUSDT_METRICS_5M.csv
06_RESEARCH/DATA/FUTURES/BTCUSDT_METRICS_5M.csv

exec
/bin/bash -lc "sed -n '1,260p' 06_RESEARCH/CODE/features/a2_funding_features.py" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
#!/usr/bin/env python3
"""Build A-2 funding extreme event tables with no lookahead."""

from __future__ import annotations

import json
from pathlib import Path

import pandas as pd


ROOT = Path(__file__).resolve().parents[3]
DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
OUTPUT_DIR = ROOT / "06_RESEARCH" / "CODE" / "output"
HOLDOUT_DIR = ROOT / "06_RESEARCH" / "DATA" / "HOLDOUT"

SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
FUNDING_COLUMN = "last_funding_rate"
WINDOW_DAYS = 365
MIN_HISTORY_DAYS = 180
MERGE_WINDOW = pd.Timedelta(hours=24)

NEGATIVE_TIERS = (("P5", 0.05), ("P2.5", 0.025), ("P1", 0.01))
POSITIVE_TIERS = (("P95", 0.95), ("P97.5", 0.975), ("P99", 0.99))
OUTPUT_COLUMNS = [
    "event_time",
    "symbol",
    "side",
    "tier",
    "funding_value",
    "rolling_pctl",
]


def empirical_percentile(prior_values: pd.Series, value: float) -> float:
    """Midrank percentile of value against prior observations only."""
    lower = (prior_values < value).sum()
    equal = (prior_values == value).sum()
    return float((lower + 0.5 * equal) / len(prior_values))


def add_rolling_percentiles(
    funding: pd.DataFrame,
    *,
    time_col: str = "datetime",
    value_col: str = FUNDING_COLUMN,
) -> pd.DataFrame:
    """Add no-lookahead rolling percentiles and warmup flags.

    The percentile for row t uses observations with timestamps in
    [t - 365 days, t). During the first year this is an expanding window,
    but rows with less than 180 calendar days of prior history are warmup.
    """
    df = funding.sort_values(time_col).reset_index(drop=True).copy()
    times = pd.to_datetime(df[time_col], utc=True)
    values = df[value_col].astype(float)
    percentiles: list[float | None] = []
    warmup: list[bool] = []

    left = 0
    for idx, current_time in enumerate(times):
        cutoff = current_time - pd.Timedelta(days=WINDOW_DAYS)
        while left < idx and times.iloc[left] < cutoff:
            left += 1

        prior_times = times.iloc[left:idx]
        prior_values = values.iloc[left:idx]
        has_min_history = (
            len(prior_times) > 0
            and prior_times.iloc[0] <= current_time - pd.Timedelta(days=MIN_HISTORY_DAYS)
        )
        is_warmup = not has_min_history
        warmup.append(is_warmup)
        if is_warmup:
            percentiles.append(None)
        else:
            percentiles.append(empirical_percentile(prior_values, values.iloc[idx]))

    df["event_time"] = times.dt.strftime("%Y-%m-%dT%H:%M:%SZ")
    df["rolling_pctl"] = percentiles
    df["warmup"] = warmup
    return df


def merge_extreme_readings(readings: pd.DataFrame) -> pd.DataFrame:
    """Collapse same-symbol/same-side/same-tier readings within 24h."""
    if readings.empty:
        return readings.loc[:, OUTPUT_COLUMNS].copy()

    rows = readings.sort_values("event_time_dt").to_dict("records")
    events: list[dict[str, object]] = []
    last_reading_time: pd.Timestamp | None = None
    for row in rows:
        reading_time = row["event_time_dt"]
        if last_reading_time is not None and reading_time - last_reading_time <= MERGE_WINDOW:
            last_reading_time = reading_time
            continue
        events.append(row)
        last_reading_time = reading_time

    return pd.DataFrame(events).loc[:, OUTPUT_COLUMNS]


def extract_symbol_events(symbol: str, funding: pd.DataFrame) -> tuple[pd.DataFrame, int]:
    featured = add_rolling_percentiles(funding)
    featured["symbol"] = symbol
    featured["funding_value"] = featured[FUNDING_COLUMN].astype(float)
    featured["event_time_dt"] = pd.to_datetime(featured["event_time"], utc=True)
    usable = featured.loc[~featured["warmup"]].copy()
    warmup_excluded = int(featured["warmup"].sum())

    event_frames: list[pd.DataFrame] = []
    for tier, threshold in NEGATIVE_TIERS:
        mask = usable["rolling_pctl"] <= threshold
        readings = usable.loc[mask].copy()
        readings["side"] = "neg"
        readings["tier"] = tier
        event_frames.append(merge_extreme_readings(readings))

    for tier, threshold in POSITIVE_TIERS:
        mask = usable["rolling_pctl"] >= threshold
        readings = usable.loc[mask].copy()
        readings["side"] = "pos"
        readings["tier"] = tier
        event_frames.append(merge_extreme_readings(readings))

    events = pd.concat(event_frames, ignore_index=True)
    return events, warmup_excluded


def build_events(data_dir: Path = DATA_DIR) -> tuple[pd.DataFrame, dict[str, int]]:
    frames: list[pd.DataFrame] = []
    warmup_by_symbol: dict[str, int] = {}
    for symbol in SYMBOLS:
        path = data_dir / f"{symbol}_FUNDING_8H.csv"
        funding = pd.read_csv(path, parse_dates=["datetime"])
        events, warmup_count = extract_symbol_events(symbol, funding)
        frames.append(events)
        warmup_by_symbol[symbol] = warmup_count

    all_events = pd.concat(frames, ignore_index=True)
    all_events = all_events.sort_values(
        ["event_time", "symbol", "side", "tier"],
        kind="mergesort",
    ).reset_index(drop=True)
    return all_events.loc[:, OUTPUT_COLUMNS], warmup_by_symbol


def split_work_holdout(events: pd.DataFrame) -> tuple[pd.DataFrame, pd.DataFrame]:
    ordinals = pd.Series(range(len(events)), index=events.index)
    holdout_mask = ordinals.mod(5) == 4
    holdout = events.loc[holdout_mask].reset_index(drop=True)
    work = events.loc[~holdout_mask].reset_index(drop=True)
    return work, holdout


def tier_counts(events: pd.DataFrame) -> dict[str, int]:
    if events.empty:
        return {}
    counts = (
        events.groupby(["side", "tier"], sort=True)
        .size()
        .rename("events")
        .reset_index()
    )
    return {
        f"{row.side}_{row.tier}": int(row.events)
        for row in counts.itertuples(index=False)
    }


def main() -> None:
    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    HOLDOUT_DIR.mkdir(parents=True, exist_ok=True)

    all_events, warmup_by_symbol = build_events(DATA_DIR)
    work, holdout = split_work_holdout(all_events)

    work_path = OUTPUT_DIR / "a2_events_work.csv"
    holdout_path = HOLDOUT_DIR / "a2_events_holdout.csv"
    summary_path = OUTPUT_DIR / "a2_events_summary.json"

    work.to_csv(work_path, index=False)
    holdout.to_csv(holdout_path, index=False)

    summary = {
        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
        "symbols": list(SYMBOLS),
        "window_days": WINDOW_DAYS,
        "min_history_days": MIN_HISTORY_DAYS,
        "merge_window_hours": int(MERGE_WINDOW / pd.Timedelta(hours=1)),
        "total_events": int(len(all_events)),
        "work_events": int(len(work)),
        "holdout_events": int(len(holdout)),
        "work_to_holdout_ratio": float(len(work) / len(holdout)) if len(holdout) else None,
        "warmup_excluded_by_symbol": warmup_by_symbol,
        "warmup_excluded_total": int(sum(warmup_by_symbol.values())),
        "tier_counts_all": tier_counts(all_events),
        "tier_counts_work": tier_counts(work),
        "tier_counts_holdout": tier_counts(holdout),
        "outputs": {
            "work": str(work_path.relative_to(ROOT)),
            "holdout": str(holdout_path.relative_to(ROOT)),
        },
    }
    summary_path.write_text(json.dumps(summary, indent=2) + "\n")
    print(json.dumps(summary, indent=2))


if __name__ == "__main__":
    main()

exec
/bin/bash -lc 'ls -la 06_RESEARCH/CODE && ls -la 06_RESEARCH/CODE/features && ls -la 06_RESEARCH/CODE/output' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
total 1040
drwxr-xr-x   39 yaomingyu  staff   1248 Jun 12 00:04 .
drwxr-xr-x    9 yaomingyu  staff    288 Jun 11 22:52 ..
drwxr-xr-x    6 yaomingyu  staff    192 Jun  6 10:48 .pytest_cache
-rw-r--r--    1 yaomingyu  staff    869 Jun 10 22:36 LIQUIDATION_COLLECTOR_RUNBOOK.md
drwxr-xr-x   28 yaomingyu  staff    896 Jun 12 00:19 __pycache__
-rw-r--r--    1 yaomingyu  staff  23586 Jun 11 22:46 a2_event_study.py
-rw-r--r--    1 yaomingyu  staff   2188 Jun  6 10:43 backtest_rules.py
-rw-r--r--    1 yaomingyu  staff  20129 Jun  6 02:04 backtest_v5_sweep_choch_fvg_bull_v1.py
-rw-r--r--    1 yaomingyu  staff  14267 Jun  6 10:43 bearish_sweep_event_study.py
-rw-r--r--    1 yaomingyu  staff   3368 Jun 12 08:29 collector.log
-rw-r--r--    1 yaomingyu  staff   1190 Jun 11 08:57 collector.log.old
-rw-r--r--    1 yaomingyu  staff   6639 Jun  6 01:59 download_futures_data.py
drwxr-xr-x    5 yaomingyu  staff    160 Jun 11 22:24 features
-rw-r--r--    1 yaomingyu  staff   2611 Jun 11 18:19 liquidation_collector.py
drwxr-xr-x  103 yaomingyu  staff   3296 Jun 11 22:38 output
-rw-r--r--    1 yaomingyu  staff  22171 Jun  6 18:25 p1_01_tsmom.py
-rw-r--r--    1 yaomingyu  staff  29802 Jun  6 19:52 p1_02_tsmom_voltarget.py
-rw-r--r--    1 yaomingyu  staff   6225 Jun  6 23:24 p1_03_data_recon_oi.py
-rw-r--r--    1 yaomingyu  staff  27780 Jun  6 23:26 p1_04_tsmom_regime_long.py
-rw-r--r--    1 yaomingyu  staff  14646 Jun  7 00:53 p1_05_regime_attribution.py
-rw-r--r--    1 yaomingyu  staff  34712 Jun  7 01:14 p1_06_tsmom_macro_bull.py
-rw-r--r--    1 yaomingyu  staff  21868 Jun 12 00:19 prepare_tsmom_expansion_data.py
-rw-r--r--    1 yaomingyu  staff   8220 Jun  6 00:44 signal_detector.py
-rw-r--r--    1 yaomingyu  staff  12446 Jun  6 10:03 sweep_dual_regime.py
-rw-r--r--    1 yaomingyu  staff  10515 Jun  6 09:45 sweep_event_study.py
-rw-r--r--    1 yaomingyu  staff   3969 Jun  6 02:04 test_backtest_rules.py
drwxr-xr-x   11 yaomingyu  staff    352 Jun 11 22:38 tests
-rw-r--r--    1 yaomingyu  staff  32971 Jun  6 11:21 v10_wider_stop_backtest.py
-rw-r--r--    1 yaomingyu  staff  10705 Jun  6 11:20 v11_regime_v3_event_study.py
-rw-r--r--    1 yaomingyu  staff  14164 Jun  6 14:38 v12_no_stop_diagnostic.py
-rw-r--r--    1 yaomingyu  staff   8304 Jun  6 14:57 v14_mae_analysis.py
-rw-r--r--    1 yaomingyu  staff  10047 Jun  6 15:04 v15_vol_compression.py
-rw-r--r--    1 yaomingyu  staff  11091 Jun  6 15:11 v16_atr_stop_diagnostic.py
-rw-r--r--    1 yaomingyu  staff  19345 Jun  6 16:09 v17_atr_strategy.py
-rw-r--r--    1 yaomingyu  staff  25592 Jun  6 16:53 v18_volpos_strategy.py
-rw-r--r--    1 yaomingyu  staff  11890 Jun  6 10:44 v4_bootstrap_validation.py
-rw-r--r--    1 yaomingyu  staff  27379 Jun  6 10:50 v4_strategy_backtest.py
-rw-r--r--    1 yaomingyu  staff  19556 Jun  6 11:21 v8_time_sensitivity.py
-rw-r--r--    1 yaomingyu  staff  10186 Jun  6 11:19 v9_stop_diagnostic.py
total 24
drwxr-xr-x   5 yaomingyu  staff   160 Jun 11 22:24 .
drwxr-xr-x  39 yaomingyu  staff  1248 Jun 12 00:04 ..
drwxr-xr-x   4 yaomingyu  staff   128 Jun 11 22:24 __pycache__
-rw-r--r--   1 yaomingyu  staff  7392 Jun 11 22:20 a2_funding_features.py
-rw-r--r--   1 yaomingyu  staff  2647 Jun 11 22:19 test_a2_features.py
total 33128
drwxr-xr-x  103 yaomingyu  staff     3296 Jun 11 22:38 .
drwxr-xr-x   39 yaomingyu  staff     1248 Jun 12 00:04 ..
-rw-r--r--    1 yaomingyu  staff    10771 Jun 11 22:46 a2_event_study_results.json
-rw-r--r--    1 yaomingyu  staff     1017 Jun 11 22:20 a2_events_summary.json
-rw-r--r--    1 yaomingyu  staff    59237 Jun 11 22:20 a2_events_work.csv
-rw-r--r--    1 yaomingyu  staff    97930 Jun  6 15:12 atr_stop_event_distances.csv
-rw-r--r--    1 yaomingyu  staff     8364 Jun  6 15:12 atr_stop_metrics.json
-rw-r--r--    1 yaomingyu  staff    67475 Jun  6 15:12 atr_vs_structure_stop_charts.png
-rw-r--r--    1 yaomingyu  staff   374266 Jun  6 02:04 equity_v5_sweep_choch_fvg_bull_v1.csv
-rw-r--r--    1 yaomingyu  staff  1495528 Jun  6 02:05 equity_v5_sweep_choch_fvg_bull_v2.csv
-rw-r--r--    1 yaomingyu  staff    82620 Jun  6 09:46 events_v5_sweep_only_bull_v3.csv
-rw-r--r--    1 yaomingyu  staff    39469 Jun  6 10:49 events_v5_sweep_regime_bear_v5.csv
-rw-r--r--    1 yaomingyu  staff    47069 Jun  6 10:04 events_v5_sweep_regime_bull_v4.csv
-rw-r--r--    1 yaomingyu  staff    47713 Jun  6 11:24 events_v5_sweep_regime_bull_v7.csv
-rw-r--r--    1 yaomingyu  staff    59761 Jun  6 14:58 mae_distribution_chart.png
-rw-r--r--    1 yaomingyu  staff     8585 Jun  6 14:58 mae_metrics.json
-rw-r--r--    1 yaomingyu  staff    51936 Jun  6 14:58 mae_trade_level.csv
-rw-r--r--    1 yaomingyu  staff    93798 Jun  6 14:58 mae_vs_return_scatter.png
-rw-r--r--    1 yaomingyu  staff     9927 Jun  6 02:04 metrics_v5_sweep_choch_fvg_bull_v1.json
-rw-r--r--    1 yaomingyu  staff    10000 Jun  6 02:05 metrics_v5_sweep_choch_fvg_bull_v2.json
-rw-r--r--    1 yaomingyu  staff    87892 Jun  6 14:38 no_stop_equity_curve.png
-rw-r--r--    1 yaomingyu  staff   492155 Jun  6 14:38 no_stop_t24_btc_eth_equity.csv
-rw-r--r--    1 yaomingyu  staff    55226 Jun  6 14:38 no_stop_t24_btc_eth_trades.csv
-rw-r--r--    1 yaomingyu  staff    14113 Jun  6 14:38 no_stop_t24_metrics.json
-rw-r--r--    1 yaomingyu  staff   517949 Jun  6 14:38 no_stop_t24_three_symbols_equity.csv
-rw-r--r--    1 yaomingyu  staff    69456 Jun  6 14:38 no_stop_t24_three_symbols_trades.csv
-rw-r--r--    1 yaomingyu  staff   494960 Jun  6 18:26 p1_01_tsmom_equity.csv
-rw-r--r--    1 yaomingyu  staff   117430 Jun  6 18:26 p1_01_tsmom_equity_curve.png
-rw-r--r--    1 yaomingyu  staff    10427 Jun  6 18:26 p1_01_tsmom_metrics.json
-rw-r--r--    1 yaomingyu  staff   211017 Jun  6 18:26 p1_01_tsmom_trades.csv
-rw-r--r--    1 yaomingyu  staff   497479 Jun  6 19:54 p1_02_tsmom_voltarget_equity.csv
-rw-r--r--    1 yaomingyu  staff   127080 Jun  6 19:54 p1_02_tsmom_voltarget_equity_curve.png
-rw-r--r--    1 yaomingyu  staff    13538 Jun  6 19:54 p1_02_tsmom_voltarget_metrics.json
-rw-r--r--    1 yaomingyu  staff   212700 Jun  6 19:54 p1_02_tsmom_voltarget_trades.csv
-rw-r--r--    1 yaomingyu  staff     9979 Jun  6 23:24 p1_03_data_recon_oi.json
-rw-r--r--    1 yaomingyu  staff   493960 Jun  6 23:27 p1_04_tsmom_regime_long_equity.csv
-rw-r--r--    1 yaomingyu  staff    77965 Jun  6 23:27 p1_04_tsmom_regime_long_equity_curve.png
-rw-r--r--    1 yaomingyu  staff    12936 Jun  6 23:27 p1_04_tsmom_regime_long_metrics.json
-rw-r--r--    1 yaomingyu  staff   165312 Jun  6 23:27 p1_04_tsmom_regime_long_trades.csv
-rw-r--r--    1 yaomingyu  staff    24569 Jun  7 00:53 p1_05_regime_attribution.json
-rw-r--r--    1 yaomingyu  staff   486832 Jun  7 01:15 p1_06_tsmom_macro_bull_equity.csv
-rw-r--r--    1 yaomingyu  staff    71647 Jun  7 01:15 p1_06_tsmom_macro_bull_equity_curve.png
-rw-r--r--    1 yaomingyu  staff    23708 Jun  7 01:15 p1_06_tsmom_macro_bull_metrics.json
-rw-r--r--    1 yaomingyu  staff   130329 Jun  7 01:15 p1_06_tsmom_macro_bull_trades.csv
-rw-r--r--    1 yaomingyu  staff    28219 Jun  6 09:46 stats_v5_sweep_only_bull_v3.json
-rw-r--r--    1 yaomingyu  staff    61102 Jun  6 10:49 stats_v5_sweep_regime_bear_v5.json
-rw-r--r--    1 yaomingyu  staff    56842 Jun  6 10:04 stats_v5_sweep_regime_bull_v4.json
-rw-r--r--    1 yaomingyu  staff    90823 Jun  6 11:24 stats_v5_sweep_regime_bull_v7.json
-rw-r--r--    1 yaomingyu  staff   490042 Jun  6 11:23 time_sensitivity_btc_eth_t12_equity.csv
-rw-r--r--    1 yaomingyu  staff   108953 Jun  6 11:23 time_sensitivity_btc_eth_t12_trades.csv
-rw-r--r--    1 yaomingyu  staff   490472 Jun  6 11:23 time_sensitivity_btc_eth_t24_equity.csv
-rw-r--r--    1 yaomingyu  staff   107635 Jun  6 11:23 time_sensitivity_btc_eth_t24_trades.csv
-rw-r--r--    1 yaomingyu  staff   486737 Jun  6 11:23 time_sensitivity_btc_eth_t6_equity.csv
-rw-r--r--    1 yaomingyu  staff   108289 Jun  6 11:23 time_sensitivity_btc_eth_t6_trades.csv
-rw-r--r--    1 yaomingyu  staff   171845 Jun  6 11:23 time_sensitivity_equity_curves.png
-rw-r--r--    1 yaomingyu  staff    55382 Jun  6 11:23 time_sensitivity_metrics.json
-rw-r--r--    1 yaomingyu  staff   518053 Jun  6 11:23 time_sensitivity_three_symbols_t12_equity.csv
-rw-r--r--    1 yaomingyu  staff   139114 Jun  6 11:23 time_sensitivity_three_symbols_t12_trades.csv
-rw-r--r--    1 yaomingyu  staff   517609 Jun  6 11:23 time_sensitivity_three_symbols_t24_equity.csv
-rw-r--r--    1 yaomingyu  staff   137003 Jun  6 11:23 time_sensitivity_three_symbols_t24_trades.csv
-rw-r--r--    1 yaomingyu  staff   517847 Jun  6 11:23 time_sensitivity_three_symbols_t6_equity.csv
-rw-r--r--    1 yaomingyu  staff   138938 Jun  6 11:23 time_sensitivity_three_symbols_t6_trades.csv
-rw-r--r--    1 yaomingyu  staff    20052 Jun  6 02:04 trades_BTCUSDT_v5_sweep_choch_fvg_bull_v1.csv
-rw-r--r--    1 yaomingyu  staff    72460 Jun  6 02:05 trades_BTCUSDT_v5_sweep_choch_fvg_bull_v2.csv
-rw-r--r--    1 yaomingyu  staff    17911 Jun  6 02:04 trades_ETHUSDT_v5_sweep_choch_fvg_bull_v1.csv
-rw-r--r--    1 yaomingyu  staff    66798 Jun  6 02:05 trades_ETHUSDT_v5_sweep_choch_fvg_bull_v2.csv
-rw-r--r--    1 yaomingyu  staff    11551 Jun  6 02:04 trades_SOLUSDT_v5_sweep_choch_fvg_bull_v1.csv
-rw-r--r--    1 yaomingyu  staff    51918 Jun  6 02:05 trades_SOLUSDT_v5_sweep_choch_fvg_bull_v2.csv
-rw-r--r--    1 yaomingyu  staff    52507 Jun  6 00:47 triple_signals_BTCUSDT_4H.csv
-rw-r--r--    1 yaomingyu  staff    41879 Jun  6 00:47 triple_signals_ETHUSDT_4H.csv
-rw-r--r--    1 yaomingyu  staff    33062 Jun  6 00:57 triple_signals_SOLUSDT_4H.csv
-rw-r--r--    1 yaomingyu  staff   516473 Jun  6 16:10 v17_atr_1.9_equity.csv
-rw-r--r--    1 yaomingyu  staff   119056 Jun  6 16:10 v17_atr_1.9_trades.csv
-rw-r--r--    1 yaomingyu  staff   517690 Jun  6 16:10 v17_atr_3.5_equity.csv
-rw-r--r--    1 yaomingyu  staff    97469 Jun  6 16:10 v17_atr_3.5_trades.csv
-rw-r--r--    1 yaomingyu  staff    94493 Jun  6 16:10 v17_atr_equity_curve.png
-rw-r--r--    1 yaomingyu  staff    22163 Jun  6 16:10 v17_atr_metrics.json
-rw-r--r--    1 yaomingyu  staff   119056 Jun  6 16:10 v17_atr_trades.csv
-rw-r--r--    1 yaomingyu  staff    57290 Jun  6 16:54 v18_volpos_candidates_audit.csv
-rw-r--r--    1 yaomingyu  staff   518276 Jun  6 16:54 v18_volpos_equity.csv
-rw-r--r--    1 yaomingyu  staff    77877 Jun  6 16:54 v18_volpos_equity_curve.png
-rw-r--r--    1 yaomingyu  staff    14734 Jun  6 16:54 v18_volpos_metrics.json
-rw-r--r--    1 yaomingyu  staff   109246 Jun  6 16:54 v18_volpos_trades.csv
-rw-r--r--    1 yaomingyu  staff     3944 Jun  6 10:49 v4_bootstrap_validation.json
-rw-r--r--    1 yaomingyu  staff    89178 Jun  6 11:21 v4_stop_diagnostic_charts.png
-rw-r--r--    1 yaomingyu  staff     5267 Jun  6 11:21 v4_stop_diagnostic_metrics.json
-rw-r--r--    1 yaomingyu  staff   514526 Jun  6 10:51 v4_strategy_equity_curve.csv
-rw-r--r--    1 yaomingyu  staff    74361 Jun  6 10:51 v4_strategy_equity_curve.png
-rw-r--r--    1 yaomingyu  staff     9645 Jun  6 10:51 v4_strategy_metrics.json
-rw-r--r--    1 yaomingyu  staff   134414 Jun  6 10:51 v4_strategy_trades.csv
-rw-r--r--    1 yaomingyu  staff   126360 Jun  6 11:24 v6b_strategy_baseline_btc_eth_trades.csv
-rw-r--r--    1 yaomingyu  staff   519356 Jun  6 11:24 v6b_strategy_baseline_equity_curve.csv
-rw-r--r--    1 yaomingyu  staff    72422 Jun  6 11:24 v6b_strategy_baseline_equity_curve.png
-rw-r--r--    1 yaomingyu  staff   158111 Jun  6 11:24 v6b_strategy_baseline_trades.csv
-rw-r--r--    1 yaomingyu  staff   124637 Jun  6 11:24 v6b_strategy_btc_eth_trades.csv
-rw-r--r--    1 yaomingyu  staff   517715 Jun  6 11:24 v6b_strategy_equity_curve.csv
-rw-r--r--    1 yaomingyu  staff    66827 Jun  6 11:24 v6b_strategy_equity_curve.png
-rw-r--r--    1 yaomingyu  staff    55114 Jun  6 11:24 v6b_strategy_metrics.json
-rw-r--r--    1 yaomingyu  staff    45788 Jun  6 11:24 v6b_strategy_stop_tightening_distribution.png
-rw-r--r--    1 yaomingyu  staff   157072 Jun  6 11:24 v6b_strategy_trades.csv
-rw-r--r--    1 yaomingyu  staff   111066 Jun  6 15:05 vol_compression_charts.png
-rw-r--r--    1 yaomingyu  staff   121194 Jun  6 15:05 vol_compression_event_groups.csv
-rw-r--r--    1 yaomingyu  staff     8087 Jun  6 15:05 vol_compression_metrics.json

exec
/bin/bash -lc "sed -n '1,260p' 06_RESEARCH/CODE/features/test_a2_features.py" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
from pathlib import Path
import sys

import pandas as pd

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from features.a2_funding_features import (
    add_rolling_percentiles,
    merge_extreme_readings,
    split_work_holdout,
)


def test_rolling_percentile_uses_only_prior_values() -> None:
    dates = pd.date_range("2020-01-01", periods=181, freq="D")
    df = pd.DataFrame(
        {
            "datetime": dates,
            "last_funding_rate": [0.0] * 180 + [1.0],
        }
    )

    base = add_rolling_percentiles(df)
    changed_future = df.copy()
    changed_future.loc[180, "last_funding_rate"] = -1_000.0
    perturbed = add_rolling_percentiles(changed_future)

    assert base.loc[179, "warmup"]
    assert not base.loc[180, "warmup"]
    assert base.loc[180, "rolling_pctl"] == 1.0
    assert perturbed.loc[180, "rolling_pctl"] == 0.0


def test_merge_rule_collapses_23h_but_keeps_25h_gap() -> None:
    readings = pd.DataFrame(
        {
            "event_time_dt": pd.to_datetime(
                [
                    "2021-01-01 00:00:00Z",
                    "2021-01-01 23:00:00Z",
                    "2021-01-03 00:00:00Z",
                ]
            ),
            "event_time": [
                "2021-01-01T00:00:00Z",
                "2021-01-01T23:00:00Z",
                "2021-01-03T00:00:00Z",
            ],
            "symbol": ["BTCUSDT", "BTCUSDT", "BTCUSDT"],
            "side": ["neg", "neg", "neg"],
            "tier": ["P5", "P5", "P5"],
            "funding_value": [-0.01, -0.02, -0.03],
            "rolling_pctl": [0.01, 0.02, 0.03],
        }
    )

    events = merge_extreme_readings(readings)

    assert events["event_time"].tolist() == [
        "2021-01-01T00:00:00Z",
        "2021-01-03T00:00:00Z",
    ]


def test_holdout_split_is_deterministic_mod_five() -> None:
    events = pd.DataFrame(
        {
            "event_time": [f"2021-01-{day:02d}T00:00:00Z" for day in range(1, 11)],
            "symbol": ["BTCUSDT"] * 10,
            "side": ["neg"] * 10,
            "tier": ["P5"] * 10,
            "funding_value": [-0.01] * 10,
            "rolling_pctl": [0.01] * 10,
        }
    )

    work, holdout = split_work_holdout(events)

    assert holdout["event_time"].tolist() == [
        "2021-01-05T00:00:00Z",
        "2021-01-10T00:00:00Z",
    ]
    assert work["event_time"].tolist() == [
        "2021-01-01T00:00:00Z",
        "2021-01-02T00:00:00Z",
        "2021-01-03T00:00:00Z",
        "2021-01-04T00:00:00Z",
        "2021-01-06T00:00:00Z",
        "2021-01-07T00:00:00Z",
        "2021-01-08T00:00:00Z",
        "2021-01-09T00:00:00Z",
    ]

exec
/bin/bash -lc "python - <<'PY'
import pandas as pd
from pathlib import Path
for p in Path('06_RESEARCH/DATA/FUTURES').glob('*_METRICS_5M.csv'):
    print(p.name)
    df = pd.read_csv(p, nrows=5)
    print(df.columns.tolist())
    print(df.head(2).to_string(index=False))
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 exited 127 in 0ms:
/bin/bash: python: command not found

exec
/bin/bash -lc 'git status --short' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
?? 04_AI_TEAM/CODEX_TASKS/TASK_A1_OI_FEATURES.md
?? 99_TEMP/codex_a1_features.log

codex
现有 A-2 模块把滚动分位实现拆成了可复用函数，符合任务书要求；我不会调用它的事件/holdout 主流程。现在只确认 METRICS 文件的时间列和 OI 字段形态，再写 A-1 模块和专门测试。
exec
/bin/bash -lc "python3 - <<'PY'
import pandas as pd
from pathlib import Path
for p in sorted(Path('06_RESEARCH/DATA/FUTURES').glob('*_METRICS_5M.csv')):
    print(p.name)
    df = pd.read_csv(p, nrows=5)
    print(df.columns.tolist())
    print(df.head(2).to_string(index=False))
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc 'python3 --version && python3 -m pytest --version' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 144ms:
pytest 9.0.3

exec
/bin/bash -lc "find 06_RESEARCH -path '*AGENTS.md' -print" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
 succeeded in 442ms:
BTCUSDT_METRICS_5M.csv
['create_time', 'symbol', 'sum_open_interest', 'sum_open_interest_value', 'count_toptrader_long_short_ratio', 'sum_toptrader_long_short_ratio', 'count_long_short_ratio', 'sum_taker_long_short_vol_ratio']
        create_time  symbol  sum_open_interest  sum_open_interest_value  count_toptrader_long_short_ratio  sum_toptrader_long_short_ratio  count_long_short_ratio  sum_taker_long_short_vol_ratio
2020-09-01 00:00:00 BTCUSDT          39080.231             4.561443e+08                          1.175479                        1.230127                1.357312                        0.783734
2020-09-01 00:05:00 BTCUSDT          39106.413             4.556938e+08                          1.179050                        1.229285                1.359244                        0.850632
ETHUSDT_METRICS_5M.csv
['create_time', 'symbol', 'sum_open_interest', 'sum_open_interest_value', 'count_toptrader_long_short_ratio', 'sum_toptrader_long_short_ratio', 'count_long_short_ratio', 'sum_taker_long_short_vol_ratio']
        create_time  symbol  sum_open_interest  sum_open_interest_value  count_toptrader_long_short_ratio  sum_toptrader_long_short_ratio  count_long_short_ratio  sum_taker_long_short_vol_ratio
2021-12-01 00:00:00 ETHUSDT         363886.490             1.683933e+09                          2.043676                        0.989130                1.833816                        0.726271
2021-12-01 00:05:00 ETHUSDT         363349.361             1.680349e+09                          2.046183                        0.987801                1.833097                        1.034256
SOLUSDT_METRICS_5M.csv
['create_time', 'symbol', 'sum_open_interest', 'sum_open_interest_value', 'count_toptrader_long_short_ratio', 'sum_toptrader_long_short_ratio', 'count_long_short_ratio', 'sum_taker_long_short_vol_ratio']
        create_time  symbol  sum_open_interest  sum_open_interest_value  count_toptrader_long_short_ratio  sum_toptrader_long_short_ratio  count_long_short_ratio  sum_taker_long_short_vol_ratio
2021-12-01 00:00:00 SOLUSDT          1163214.0             2.425442e+08                          6.175504                        0.996155                6.539242                        1.169838
2021-12-01 00:05:00 SOLUSDT          1164342.0             2.428263e+08                          6.227905                        0.998794                6.576195                        0.734694

codex
我准备新增一个独立 A-1 脚本：只读 `{SYM}_METRICS_5M.csv`，把 OI 重采样到 1H，计算 6h/24h 变化，再通过 A-2 的 `add_rolling_percentiles` 复用 midrank 和 365d/180d 无前视窗口。测试会覆盖无前视和缺口小时不填充。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/features/a1_oi_features.py
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..ef056d73a49052040d7c095271ef5ae09db5554c
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,172 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead.
+
+This module deliberately stops at the feature layer. It does not extract
+events, choose thresholds, touch holdout data, or compute returns.
+"""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()

diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..ef056d73a49052040d7c095271ef5ae09db5554c
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,172 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead.
+
+This module deliberately stops at the feature layer. It does not extract
+events, choose thresholds, touch holdout data, or compute returns.
+"""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()

codex
我已经加了 A-1 脚本，核心路径只产生特征 CSV 和 JSON 摘要到 stdout。接下来补 pytest，重点是让未来值扰动不会改变当前分位，同时确认 1H 空桶保持 NaN。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/features/test_a1_oi_features.py
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..ef056d73a49052040d7c095271ef5ae09db5554c
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,172 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead.
+
+This module deliberately stops at the feature layer. It does not extract
+events, choose thresholds, touch holdout data, or compute returns.
+"""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..ef056d73a49052040d7c095271ef5ae09db5554c
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,172 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead.
+
+This module deliberately stops at the feature layer. It does not extract
+events, choose thresholds, touch holdout data, or compute returns.
+"""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

codex
测试文件已加好；现在先跑 A-1 测试和相邻 A-2 测试，确认复用函数没有被新代码破坏。
exec
/bin/bash -lc 'python3 -m py_compile 06_RESEARCH/CODE/features/a1_oi_features.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
exec
/bin/bash -lc 'python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py 06_RESEARCH/CODE/features/test_a2_features.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..ef056d73a49052040d7c095271ef5ae09db5554c
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,172 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead.
+
+This module deliberately stops at the feature layer. It does not extract
+events, choose thresholds, touch holdout data, or compute returns.
+"""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

 succeeded in 1936ms:
============================= test session starts ==============================
platform darwin -- Python 3.13.5, pytest-9.0.3, pluggy-1.6.0
rootdir: /Users/yaomingyu/Documents/AI_QUANT_COMPANY
plugins: anyio-4.13.0
collected 5 items

06_RESEARCH/CODE/features/test_a1_oi_features.py ..                      [ 40%]
06_RESEARCH/CODE/features/test_a2_features.py ...                        [100%]

============================== 5 passed in 1.63s ===============================

codex
A-1/A-2 相关测试已经全绿。下一步运行脚本生成三个品种的特征 CSV；如果现有 A-2 滚动实现对 1H 数据过慢，我会在不改 A-2 的前提下只调整 A-1 的调用方式。
exec
/bin/bash -lc 'python3 06_RESEARCH/CODE/features/a1_oi_features.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..ef056d73a49052040d7c095271ef5ae09db5554c
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,172 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead.
+
+This module deliberately stops at the feature layer. It does not extract
+events, choose thresholds, touch holdout data, or compute returns.
+"""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

codex
生成进程还在跑，当前瓶颈大概率是 1H 序列上逐点计算 365 天窗口分位。先等它完成；如果运行时间不可接受，再做一个仍复用 A-2 `empirical_percentile` 口径的 A-1 专用滚动实现。
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..ef056d73a49052040d7c095271ef5ae09db5554c
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,172 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead.
+
+This module deliberately stops at the feature layer. It does not extract
+events, choose thresholds, touch holdout data, or compute returns.
+"""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

 succeeded in 42366ms:
{
  "generated_at_utc": "2026-06-12T00:48:46Z",
  "symbols": [
    "BTCUSDT",
    "ETHUSDT",
    "SOLUSDT"
  ],
  "outputs": [
    {
      "symbol": "BTCUSDT",
      "start_utc": "2020-09-01T00:00:00Z",
      "end_utc": "2026-06-06T00:00:00Z",
      "rows": 50497,
      "expected_1h_grid_rows": 50497,
      "warmup_rows": 4344,
      "oi_gap_hours": 22,
      "oi_gap_rate": 0.00043566944570964613,
      "output": "06_RESEARCH/CODE/output/a1_oi_features_BTCUSDT.csv"
    },
    {
      "symbol": "ETHUSDT",
      "start_utc": "2021-12-01T00:00:00Z",
      "end_utc": "2026-06-06T00:00:00Z",
      "rows": 39553,
      "expected_1h_grid_rows": 39553,
      "warmup_rows": 4344,
      "oi_gap_hours": 10,
      "oi_gap_rate": 0.00025282532298435013,
      "output": "06_RESEARCH/CODE/output/a1_oi_features_ETHUSDT.csv"
    },
    {
      "symbol": "SOLUSDT",
      "start_utc": "2021-12-01T00:00:00Z",
      "end_utc": "2026-06-06T00:00:00Z",
      "rows": 39553,
      "expected_1h_grid_rows": 39553,
      "warmup_rows": 4344,
      "oi_gap_hours": 10,
      "oi_gap_rate": 0.00025282532298435013,
      "output": "06_RESEARCH/CODE/output/a1_oi_features_SOLUSDT.csv"
    }
  ]
}

codex
三个输出 CSV 已生成，行数等于各自 1H 网格。现在我会做一次输出审计：列名、UTC 格式、缺口小时、warmup 与分位缺失是否符合预期，然后写执行报告。
exec
/bin/bash -lc "python3 - <<'PY'
import pandas as pd
from pathlib import Path
for p in sorted(Path('06_RESEARCH/CODE/output').glob('a1_oi_features_*.csv')):
    df = pd.read_csv(p)
    print(p.name)
    print('columns', df.columns.tolist())
    print('rows', len(df))
    print('head', df.head(2).to_dict('records'))
    print('tail', df.tail(2).to_dict('records'))
    print('warmup', int(df['warmup'].sum()))
    print('pctl_na', int(df['d6h_rolling_pctl'].isna().sum()), int(df['d24h_rolling_pctl'].isna().sum()))
    print('oi_na', int(df['oi'].isna().sum()))
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc 'rg -n "HOLDOUT|holdout|event|threshold|return|MARK|FUNDING|收益|阈值|事件" 06_RESEARCH/CODE/features/a1_oi_features.py 06_RESEARCH/CODE/features/test_a1_oi_features.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/CODE/features/a1_oi_features.py:5:events, choose thresholds, touch holdout data, or compute returns.
06_RESEARCH/CODE/features/a1_oi_features.py:47:    return df
06_RESEARCH/CODE/features/a1_oi_features.py:59:    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
06_RESEARCH/CODE/features/a1_oi_features.py:67:    return df
06_RESEARCH/CODE/features/a1_oi_features.py:83:        return out, None
06_RESEARCH/CODE/features/a1_oi_features.py:100:    return out, first_ready
06_RESEARCH/CODE/features/a1_oi_features.py:122:    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
06_RESEARCH/CODE/features/a1_oi_features.py:132:    return out
06_RESEARCH/CODE/features/a1_oi_features.py:139:    return {

exec
/bin/bash -lc "python3 - <<'PY'
import pandas as pd
from pathlib import Path
for p in sorted(Path('06_RESEARCH/CODE/output').glob('a1_oi_features_*.csv')):
    df = pd.read_csv(p, parse_dates=['ts'])
    gaps = df.loc[df['oi'].isna(), 'ts']
    print(p.name)
    print('gap_count', len(gaps))
    if len(gaps):
        print('first_gap', gaps.min().strftime('%Y-%m-%dT%H:%M:%SZ'))
        print('last_gap', gaps.max().strftime('%Y-%m-%dT%H:%M:%SZ'))
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc 'python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 449ms:
a1_oi_features_BTCUSDT.csv
columns ['ts', 'oi', 'd6h_pct', 'd24h_pct', 'd6h_rolling_pctl', 'd24h_rolling_pctl', 'warmup']
rows 50497
head [{'ts': '2020-09-01T00:00:00Z', 'oi': 38400.168, 'd6h_pct': nan, 'd24h_pct': nan, 'd6h_rolling_pctl': nan, 'd24h_rolling_pctl': nan, 'warmup': True}, {'ts': '2020-09-01T01:00:00Z', 'oi': 38466.221, 'd6h_pct': nan, 'd24h_pct': nan, 'd6h_rolling_pctl': nan, 'd24h_rolling_pctl': nan, 'warmup': True}]
tail [{'ts': '2026-06-05T23:00:00Z', 'oi': 101688.111, 'd6h_pct': -0.0253595161767542, 'd24h_pct': 0.0304205996830979, 'd6h_rolling_pctl': 0.0353881278538812, 'd24h_rolling_pctl': 0.8627853881278539, 'warmup': False}, {'ts': '2026-06-06T00:00:00Z', 'oi': 101597.505, 'd6h_pct': -0.0251347729100753, 'd24h_pct': 0.035178722951993, 'd6h_rolling_pctl': 0.0360730593607305, 'd24h_rolling_pctl': 0.8882420091324201, 'warmup': False}]
warmup 4344
pctl_na 4360 4380
oi_na 22
a1_oi_features_ETHUSDT.csv
columns ['ts', 'oi', 'd6h_pct', 'd24h_pct', 'd6h_rolling_pctl', 'd24h_rolling_pctl', 'warmup']
rows 39553
head [{'ts': '2021-12-01T00:00:00Z', 'oi': 375008.513, 'd6h_pct': nan, 'd24h_pct': nan, 'd6h_rolling_pctl': nan, 'd24h_rolling_pctl': nan, 'warmup': True}, {'ts': '2021-12-01T01:00:00Z', 'oi': 373018.158, 'd6h_pct': nan, 'd24h_pct': nan, 'd6h_rolling_pctl': nan, 'd24h_rolling_pctl': nan, 'warmup': True}]
tail [{'ts': '2026-06-05T23:00:00Z', 'oi': 2383139.272, 'd6h_pct': 0.009673352738186, 'd24h_pct': 0.0568526976651631, 'd6h_rolling_pctl': 0.76837899543379, 'd24h_rolling_pctl': 0.9312785388127854, 'warmup': False}, {'ts': '2026-06-06T00:00:00Z', 'oi': 2383549.065, 'd6h_pct': 0.0071458274148976, 'd24h_pct': 0.0551332064263445, 'd6h_rolling_pctl': 0.7144977168949772, 'd24h_rolling_pctl': 0.9267123287671232, 'warmup': False}]
warmup 4344
pctl_na 4342 4364
oi_na 10
a1_oi_features_SOLUSDT.csv
columns ['ts', 'oi', 'd6h_pct', 'd24h_pct', 'd6h_rolling_pctl', 'd24h_rolling_pctl', 'warmup']
rows 39553
head [{'ts': '2021-12-01T00:00:00Z', 'oi': 1161785.0, 'd6h_pct': nan, 'd24h_pct': nan, 'd6h_rolling_pctl': nan, 'd24h_rolling_pctl': nan, 'warmup': True}, {'ts': '2021-12-01T01:00:00Z', 'oi': 1153637.0, 'd6h_pct': nan, 'd24h_pct': nan, 'd6h_rolling_pctl': nan, 'd24h_rolling_pctl': nan, 'warmup': True}]
tail [{'ts': '2026-06-05T23:00:00Z', 'oi': 9722564.67, 'd6h_pct': -0.0107560901723406, 'd24h_pct': -0.0383839996359166, 'd6h_rolling_pctl': 0.2150684931506849, 'd24h_rolling_pctl': 0.111986301369863, 'warmup': False}, {'ts': '2026-06-06T00:00:00Z', 'oi': 9717591.85, 'd6h_pct': -0.0124930395263123, 'd24h_pct': -0.0399795040154707, 'd6h_rolling_pctl': 0.1784246575342465, 'd24h_rolling_pctl': 0.1026255707762557, 'warmup': False}]
warmup 4344
pctl_na 4342 4364
oi_na 10

 succeeded in 511ms:
a1_oi_features_BTCUSDT.csv
gap_count 22
first_gap 2021-01-20T01:00:00Z
last_gap 2024-02-16T23:00:00Z
a1_oi_features_ETHUSDT.csv
gap_count 10
first_gap 2024-02-16T14:00:00Z
last_gap 2024-02-16T23:00:00Z
a1_oi_features_SOLUSDT.csv
gap_count 10
first_gap 2024-02-16T14:00:00Z
last_gap 2024-02-16T23:00:00Z

diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..ef056d73a49052040d7c095271ef5ae09db5554c
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,172 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead.
+
+This module deliberately stops at the feature layer. It does not extract
+events, choose thresholds, touch holdout data, or compute returns.
+"""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

 succeeded in 1690ms:
============================= test session starts ==============================
platform darwin -- Python 3.13.5, pytest-9.0.3, pluggy-1.6.0
rootdir: /Users/yaomingyu/Documents/AI_QUANT_COMPANY
plugins: anyio-4.13.0
collected 2 items

06_RESEARCH/CODE/features/test_a1_oi_features.py ..                      [100%]

============================== 2 passed in 1.42s ===============================

codex
输出审计通过：列名匹配、时间戳是 `Z` 后缀 UTC、缺口小时保留为 NaN。为了避免静态检查误读，我会把脚本头部关于禁止项的说明改得更简洁，不在代码里写入无关禁词。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/features/a1_oi_features.py
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..41dcea2d71e8bc2a0a96968893b812bfd951547d
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,168 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead."""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..41dcea2d71e8bc2a0a96968893b812bfd951547d
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,168 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead."""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

exec
/bin/bash -lc "date -u '+%Y-%m-%dT%H:%M:%SZ'" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
2026-06-12T00:49:32Z

exec
/bin/bash -lc 'rg -n "HOLDOUT|holdout|MARK|FUNDING|split_work_holdout|extract_symbol_events|merge_extreme|tier|side|pnl|return|收益|阈值|事件" 06_RESEARCH/CODE/features/a1_oi_features.py 06_RESEARCH/CODE/features/test_a1_oi_features.py || true' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
06_RESEARCH/CODE/features/a1_oi_features.py:43:    return df
06_RESEARCH/CODE/features/a1_oi_features.py:55:    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
06_RESEARCH/CODE/features/a1_oi_features.py:63:    return df
06_RESEARCH/CODE/features/a1_oi_features.py:79:        return out, None
06_RESEARCH/CODE/features/a1_oi_features.py:96:    return out, first_ready
06_RESEARCH/CODE/features/a1_oi_features.py:118:    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
06_RESEARCH/CODE/features/a1_oi_features.py:128:    return out
06_RESEARCH/CODE/features/a1_oi_features.py:135:    return {

exec
/bin/bash -lc 'python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py 06_RESEARCH/CODE/features/test_a2_features.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
exec
/bin/bash -lc "python3 - <<'PY'
import pandas as pd
from pathlib import Path
print('| Symbol | Coverage UTC | Rows | Expected 1H rows | Warmup rows | OI gap hours | First gap | Last gap |')
print('|---|---:|---:|---:|---:|---:|---|---|')
for p in sorted(Path('06_RESEARCH/CODE/output').glob('a1_oi_features_*.csv')):
    sym=p.stem.replace('a1_oi_features_','')
    df=pd.read_csv(p, parse_dates=['ts'])
    ts=df['ts']
    expected=int(((ts.max()-ts.min())/pd.Timedelta(hours=1))+1)
    gaps=df.loc[df['oi'].isna(),'ts']
    first_gap=gaps.min().strftime('%Y-%m-%dT%H:%M:%SZ') if len(gaps) else '-'
    last_gap=gaps.max().strftime('%Y-%m-%dT%H:%M:%SZ') if len(gaps) else '-'
    print(f\"| {sym} | {ts.min().strftime('%Y-%m-%dT%H:%M:%SZ')} to {ts.max().strftime('%Y-%m-%dT%H:%M:%SZ')} | {len(df)} | {expected} | {int(df['warmup'].sum())} | {int(df['oi'].isna().sum())} | {first_gap} | {last_gap} |\")
PY" in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 642ms:
| Symbol | Coverage UTC | Rows | Expected 1H rows | Warmup rows | OI gap hours | First gap | Last gap |
|---|---:|---:|---:|---:|---:|---|---|
| BTCUSDT | 2020-09-01T00:00:00Z to 2026-06-06T00:00:00Z | 50497 | 50497 | 4344 | 22 | 2021-01-20T01:00:00Z | 2024-02-16T23:00:00Z |
| ETHUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
| SOLUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |

diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..41dcea2d71e8bc2a0a96968893b812bfd951547d
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,168 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead."""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

 succeeded in 1779ms:
============================= test session starts ==============================
platform darwin -- Python 3.13.5, pytest-9.0.3, pluggy-1.6.0
rootdir: /Users/yaomingyu/Documents/AI_QUANT_COMPANY
plugins: anyio-4.13.0
collected 5 items

06_RESEARCH/CODE/features/test_a1_oi_features.py ..                      [ 40%]
06_RESEARCH/CODE/features/test_a2_features.py ...                        [100%]

============================== 5 passed in 1.49s ===============================

exec
/bin/bash -lc 'rg -n "HOLDOUT|holdout|MARK|FUNDING|收益|阈值|事件" 06_RESEARCH/CODE/features/a1_oi_features.py 06_RESEARCH/CODE/features/test_a1_oi_features.py || true' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..41dcea2d71e8bc2a0a96968893b812bfd951547d
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,168 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead."""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

codex
复跑验证完成，A-1 和 A-2 相关测试仍然全绿，A-1 文件中也没有 Holdout/MARK/FUNDING/收益/事件/阈值相关路径。现在写交接报告，包含覆盖区间、warmup、缺口统计、复算命令和验收自检。
apply patch
patch: completed
/Users/yaomingyu/Documents/AI_QUANT_COMPANY/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
diff --git a/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md b/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
new file mode 100644
index 0000000000000000000000000000000000000000..35e76ddaba7889c5da218ec19f5d80c82e7c46cd
--- /dev/null
+++ b/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
@@ -0,0 +1,47 @@
+# REPORT_A1_OI_FEATURES
+
+生成时间（UTC）：2026-06-12T00:49:32Z
+
+## 交付物
+
+- CODE：`06_RESEARCH/CODE/features/a1_oi_features.py`
+- TEST：`06_RESEARCH/CODE/features/test_a1_oi_features.py`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_BTCUSDT.csv`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_ETHUSDT.csv`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_SOLUSDT.csv`
+
+## 实现口径
+
+- 数据源仅为 `06_RESEARCH/DATA/FUTURES/{SYM}_METRICS_5M.csv`。
+- 读取列仅为 `create_time` 与 `sum_open_interest`。
+- `sum_open_interest` 按 UTC 1H 重采样，取每小时最后一条；缺口小时保留 `NaN`，不前向填充。
+- `d6h_pct` / `d24h_pct` 使用 `pct_change(periods=6/24, fill_method=None)`。
+- 滚动分位复用 `features.a2_funding_features.add_rolling_percentiles`，继承 365 天窗口、最少 180 天起算、midrank、仅使用 t 之前样本的口径。
+- 本任务未实现事件提取、阈值选择、Holdout 切分或收益计算。
+
+## 覆盖与缺口
+
+| Symbol | Coverage UTC | Rows | Expected 1H rows | Warmup rows | OI gap hours | First gap | Last gap |
+|---|---:|---:|---:|---:|---:|---|---|
+| BTCUSDT | 2020-09-01T00:00:00Z to 2026-06-06T00:00:00Z | 50497 | 50497 | 4344 | 22 | 2021-01-20T01:00:00Z | 2024-02-16T23:00:00Z |
+| ETHUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
+| SOLUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
+
+说明：`warmup` 标记滚动分位尚未满足 180 天历史要求的 1H 行；ETH/SOL 覆盖始于 2021-12-01。
+
+## 复算命令
+
+```bash
+python3 06_RESEARCH/CODE/features/a1_oi_features.py
+python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py 06_RESEARCH/CODE/features/test_a2_features.py
+```
+
+## 验收自检
+
+- pytest：`5 passed in 1.49s`。
+- 无前视测试：构造序列验证 t 分位不含当前值进入历史样本，且扰动 t+1 不改变 t 分位；同时验证测试对 t+1 扰动非恒真。
+- 重采样测试：5M 到 1H 取小时最后值；中间缺口小时为 `NaN`，无前向填充。
+- UTC：输出 `ts` 均为 `YYYY-MM-DDTHH:MM:SSZ`。
+- 行数：三个输出均等于各自覆盖区间的完整 1H 网格。
+- 数据边界：A-1 脚本只读取 FUTURES METRICS 文件；未读取 MARK、FUNDING、HOLDOUT 或其他数据目录。
+- 禁止项：未新增事件、阈值、Holdout、收益代码路径；未修改 A-2 既有模块；未执行 git commit。
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..41dcea2d71e8bc2a0a96968893b812bfd951547d
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,168 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead."""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

diff --git a/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md b/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
new file mode 100644
index 0000000000000000000000000000000000000000..35e76ddaba7889c5da218ec19f5d80c82e7c46cd
--- /dev/null
+++ b/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
@@ -0,0 +1,47 @@
+# REPORT_A1_OI_FEATURES
+
+生成时间（UTC）：2026-06-12T00:49:32Z
+
+## 交付物
+
+- CODE：`06_RESEARCH/CODE/features/a1_oi_features.py`
+- TEST：`06_RESEARCH/CODE/features/test_a1_oi_features.py`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_BTCUSDT.csv`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_ETHUSDT.csv`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_SOLUSDT.csv`
+
+## 实现口径
+
+- 数据源仅为 `06_RESEARCH/DATA/FUTURES/{SYM}_METRICS_5M.csv`。
+- 读取列仅为 `create_time` 与 `sum_open_interest`。
+- `sum_open_interest` 按 UTC 1H 重采样，取每小时最后一条；缺口小时保留 `NaN`，不前向填充。
+- `d6h_pct` / `d24h_pct` 使用 `pct_change(periods=6/24, fill_method=None)`。
+- 滚动分位复用 `features.a2_funding_features.add_rolling_percentiles`，继承 365 天窗口、最少 180 天起算、midrank、仅使用 t 之前样本的口径。
+- 本任务未实现事件提取、阈值选择、Holdout 切分或收益计算。
+
+## 覆盖与缺口
+
+| Symbol | Coverage UTC | Rows | Expected 1H rows | Warmup rows | OI gap hours | First gap | Last gap |
+|---|---:|---:|---:|---:|---:|---|---|
+| BTCUSDT | 2020-09-01T00:00:00Z to 2026-06-06T00:00:00Z | 50497 | 50497 | 4344 | 22 | 2021-01-20T01:00:00Z | 2024-02-16T23:00:00Z |
+| ETHUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
+| SOLUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
+
+说明：`warmup` 标记滚动分位尚未满足 180 天历史要求的 1H 行；ETH/SOL 覆盖始于 2021-12-01。
+
+## 复算命令
+
+```bash
+python3 06_RESEARCH/CODE/features/a1_oi_features.py
+python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py 06_RESEARCH/CODE/features/test_a2_features.py
+```
+
+## 验收自检
+
+- pytest：`5 passed in 1.49s`。
+- 无前视测试：构造序列验证 t 分位不含当前值进入历史样本，且扰动 t+1 不改变 t 分位；同时验证测试对 t+1 扰动非恒真。
+- 重采样测试：5M 到 1H 取小时最后值；中间缺口小时为 `NaN`，无前向填充。
+- UTC：输出 `ts` 均为 `YYYY-MM-DDTHH:MM:SSZ`。
+- 行数：三个输出均等于各自覆盖区间的完整 1H 网格。
+- 数据边界：A-1 脚本只读取 FUTURES METRICS 文件；未读取 MARK、FUNDING、HOLDOUT 或其他数据目录。
+- 禁止项：未新增事件、阈值、Holdout、收益代码路径；未修改 A-2 既有模块；未执行 git commit。
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..41dcea2d71e8bc2a0a96968893b812bfd951547d
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,168 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead."""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

codex
报告已写入指定位置。最后我检查工作区变更清单，确认只新增/修改本任务相关文件和输出，不提交 commit。
exec
/bin/bash -lc 'ls -lh 06_RESEARCH/CODE/output/a1_oi_features_*.csv 04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md 06_RESEARCH/CODE/features/a1_oi_features.py 06_RESEARCH/CODE/features/test_a1_oi_features.py' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
-rw-r--r--  1 yaomingyu  staff   2.5K Jun 12 08:50 04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
-rw-r--r--  1 yaomingyu  staff   5.6K Jun 12 08:49 06_RESEARCH/CODE/features/a1_oi_features.py
-rw-r--r--  1 yaomingyu  staff   2.1K Jun 12 08:47 06_RESEARCH/CODE/features/test_a1_oi_features.py
-rw-r--r--  1 yaomingyu  staff   5.5M Jun 12 08:48 06_RESEARCH/CODE/output/a1_oi_features_BTCUSDT.csv
-rw-r--r--  1 yaomingyu  staff   4.3M Jun 12 08:48 06_RESEARCH/CODE/output/a1_oi_features_ETHUSDT.csv
-rw-r--r--  1 yaomingyu  staff   4.3M Jun 12 08:48 06_RESEARCH/CODE/output/a1_oi_features_SOLUSDT.csv

exec
/bin/bash -lc 'git status --short' in /Users/yaomingyu/Documents/AI_QUANT_COMPANY
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
?? 04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
?? 04_AI_TEAM/CODEX_TASKS/TASK_A1_OI_FEATURES.md
?? 06_RESEARCH/CODE/features/a1_oi_features.py
?? 06_RESEARCH/CODE/features/test_a1_oi_features.py
?? 06_RESEARCH/CODE/output/a1_oi_features_BTCUSDT.csv
?? 06_RESEARCH/CODE/output/a1_oi_features_ETHUSDT.csv
?? 06_RESEARCH/CODE/output/a1_oi_features_SOLUSDT.csv
?? 99_TEMP/codex_a1_features.log

diff --git a/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md b/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
new file mode 100644
index 0000000000000000000000000000000000000000..35e76ddaba7889c5da218ec19f5d80c82e7c46cd
--- /dev/null
+++ b/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
@@ -0,0 +1,47 @@
+# REPORT_A1_OI_FEATURES
+
+生成时间（UTC）：2026-06-12T00:49:32Z
+
+## 交付物
+
+- CODE：`06_RESEARCH/CODE/features/a1_oi_features.py`
+- TEST：`06_RESEARCH/CODE/features/test_a1_oi_features.py`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_BTCUSDT.csv`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_ETHUSDT.csv`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_SOLUSDT.csv`
+
+## 实现口径
+
+- 数据源仅为 `06_RESEARCH/DATA/FUTURES/{SYM}_METRICS_5M.csv`。
+- 读取列仅为 `create_time` 与 `sum_open_interest`。
+- `sum_open_interest` 按 UTC 1H 重采样，取每小时最后一条；缺口小时保留 `NaN`，不前向填充。
+- `d6h_pct` / `d24h_pct` 使用 `pct_change(periods=6/24, fill_method=None)`。
+- 滚动分位复用 `features.a2_funding_features.add_rolling_percentiles`，继承 365 天窗口、最少 180 天起算、midrank、仅使用 t 之前样本的口径。
+- 本任务未实现事件提取、阈值选择、Holdout 切分或收益计算。
+
+## 覆盖与缺口
+
+| Symbol | Coverage UTC | Rows | Expected 1H rows | Warmup rows | OI gap hours | First gap | Last gap |
+|---|---:|---:|---:|---:|---:|---|---|
+| BTCUSDT | 2020-09-01T00:00:00Z to 2026-06-06T00:00:00Z | 50497 | 50497 | 4344 | 22 | 2021-01-20T01:00:00Z | 2024-02-16T23:00:00Z |
+| ETHUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
+| SOLUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
+
+说明：`warmup` 标记滚动分位尚未满足 180 天历史要求的 1H 行；ETH/SOL 覆盖始于 2021-12-01。
+
+## 复算命令
+
+```bash
+python3 06_RESEARCH/CODE/features/a1_oi_features.py
+python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py 06_RESEARCH/CODE/features/test_a2_features.py
+```
+
+## 验收自检
+
+- pytest：`5 passed in 1.49s`。
+- 无前视测试：构造序列验证 t 分位不含当前值进入历史样本，且扰动 t+1 不改变 t 分位；同时验证测试对 t+1 扰动非恒真。
+- 重采样测试：5M 到 1H 取小时最后值；中间缺口小时为 `NaN`，无前向填充。
+- UTC：输出 `ts` 均为 `YYYY-MM-DDTHH:MM:SSZ`。
+- 行数：三个输出均等于各自覆盖区间的完整 1H 网格。
+- 数据边界：A-1 脚本只读取 FUTURES METRICS 文件；未读取 MARK、FUNDING、HOLDOUT 或其他数据目录。
+- 禁止项：未新增事件、阈值、Holdout、收益代码路径；未修改 A-2 既有模块；未执行 git commit。
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..41dcea2d71e8bc2a0a96968893b812bfd951547d
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,168 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead."""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

codex
已完成 A-1 OI 特征层，未做事件、阈值、Holdout、收益，也未提交 git commit。

新增交付物：
- [a1_oi_features.py](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/features/a1_oi_features.py)
- [test_a1_oi_features.py](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/features/test_a1_oi_features.py)
- [REPORT_A1_OI_FEATURES.md](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md)
- `06_RESEARCH/CODE/output/a1_oi_features_{BTCUSDT,ETHUSDT,SOLUSDT}.csv`

验证结果：
- `python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py 06_RESEARCH/CODE/features/test_a2_features.py`
- 结果：`5 passed in 1.49s`

输出概况：
- BTCUSDT：50,497 行，warmup 4,344，OI 缺口 22 小时
- ETHUSDT：39,553 行，warmup 4,344，OI 缺口 10 小时
- SOLUSDT：39,553 行，warmup 4,344，OI 缺口 10 小时

注意：`git status` 里原本已有未跟踪的 `TASK_A1_OI_FEATURES.md` 和 `99_TEMP/codex_a1_features.log`，我没有改动它们。
diff --git a/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md b/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
new file mode 100644
index 0000000000000000000000000000000000000000..35e76ddaba7889c5da218ec19f5d80c82e7c46cd
--- /dev/null
+++ b/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md
@@ -0,0 +1,47 @@
+# REPORT_A1_OI_FEATURES
+
+生成时间（UTC）：2026-06-12T00:49:32Z
+
+## 交付物
+
+- CODE：`06_RESEARCH/CODE/features/a1_oi_features.py`
+- TEST：`06_RESEARCH/CODE/features/test_a1_oi_features.py`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_BTCUSDT.csv`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_ETHUSDT.csv`
+- RESULTS：`06_RESEARCH/CODE/output/a1_oi_features_SOLUSDT.csv`
+
+## 实现口径
+
+- 数据源仅为 `06_RESEARCH/DATA/FUTURES/{SYM}_METRICS_5M.csv`。
+- 读取列仅为 `create_time` 与 `sum_open_interest`。
+- `sum_open_interest` 按 UTC 1H 重采样，取每小时最后一条；缺口小时保留 `NaN`，不前向填充。
+- `d6h_pct` / `d24h_pct` 使用 `pct_change(periods=6/24, fill_method=None)`。
+- 滚动分位复用 `features.a2_funding_features.add_rolling_percentiles`，继承 365 天窗口、最少 180 天起算、midrank、仅使用 t 之前样本的口径。
+- 本任务未实现事件提取、阈值选择、Holdout 切分或收益计算。
+
+## 覆盖与缺口
+
+| Symbol | Coverage UTC | Rows | Expected 1H rows | Warmup rows | OI gap hours | First gap | Last gap |
+|---|---:|---:|---:|---:|---:|---|---|
+| BTCUSDT | 2020-09-01T00:00:00Z to 2026-06-06T00:00:00Z | 50497 | 50497 | 4344 | 22 | 2021-01-20T01:00:00Z | 2024-02-16T23:00:00Z |
+| ETHUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
+| SOLUSDT | 2021-12-01T00:00:00Z to 2026-06-06T00:00:00Z | 39553 | 39553 | 4344 | 10 | 2024-02-16T14:00:00Z | 2024-02-16T23:00:00Z |
+
+说明：`warmup` 标记滚动分位尚未满足 180 天历史要求的 1H 行；ETH/SOL 覆盖始于 2021-12-01。
+
+## 复算命令
+
+```bash
+python3 06_RESEARCH/CODE/features/a1_oi_features.py
+python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py 06_RESEARCH/CODE/features/test_a2_features.py
+```
+
+## 验收自检
+
+- pytest：`5 passed in 1.49s`。
+- 无前视测试：构造序列验证 t 分位不含当前值进入历史样本，且扰动 t+1 不改变 t 分位；同时验证测试对 t+1 扰动非恒真。
+- 重采样测试：5M 到 1H 取小时最后值；中间缺口小时为 `NaN`，无前向填充。
+- UTC：输出 `ts` 均为 `YYYY-MM-DDTHH:MM:SSZ`。
+- 行数：三个输出均等于各自覆盖区间的完整 1H 网格。
+- 数据边界：A-1 脚本只读取 FUTURES METRICS 文件；未读取 MARK、FUNDING、HOLDOUT 或其他数据目录。
+- 禁止项：未新增事件、阈值、Holdout、收益代码路径；未修改 A-2 既有模块；未执行 git commit。
diff --git a/06_RESEARCH/CODE/features/a1_oi_features.py b/06_RESEARCH/CODE/features/a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..41dcea2d71e8bc2a0a96968893b812bfd951547d
--- /dev/null
+++ b/06_RESEARCH/CODE/features/a1_oi_features.py
@@ -0,0 +1,168 @@
+#!/usr/bin/env python3
+"""Build A-1 open-interest feature tables with no lookahead."""
+
+from __future__ import annotations
+
+import json
+import sys
+from pathlib import Path
+
+import pandas as pd
+
+
+ROOT = Path(__file__).resolve().parents[3]
+CODE_DIR = ROOT / "06_RESEARCH" / "CODE"
+DATA_DIR = ROOT / "06_RESEARCH" / "DATA" / "FUTURES"
+OUTPUT_DIR = CODE_DIR / "output"
+
+if str(CODE_DIR) not in sys.path:
+    sys.path.insert(0, str(CODE_DIR))
+
+from features.a2_funding_features import add_rolling_percentiles  # noqa: E402
+
+
+SYMBOLS = ("BTCUSDT", "ETHUSDT", "SOLUSDT")
+OI_COLUMN = "sum_open_interest"
+TIME_COLUMN = "create_time"
+OUTPUT_COLUMNS = [
+    "ts",
+    "oi",
+    "d6h_pct",
+    "d24h_pct",
+    "d6h_rolling_pctl",
+    "d24h_rolling_pctl",
+    "warmup",
+]
+
+
+def read_metrics(path: Path) -> pd.DataFrame:
+    """Read only the columns needed for the A-1 OI feature layer."""
+    df = pd.read_csv(path, usecols=[TIME_COLUMN, OI_COLUMN])
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df[OI_COLUMN] = pd.to_numeric(df[OI_COLUMN], errors="coerce")
+    return df
+
+
+def resample_oi_1h(metrics: pd.DataFrame) -> pd.DataFrame:
+    """Resample 5-minute OI to a UTC 1H grid using each hour's last row."""
+    df = metrics.loc[:, [TIME_COLUMN, OI_COLUMN]].copy()
+    df[TIME_COLUMN] = pd.to_datetime(df[TIME_COLUMN], utc=True)
+    df = df.sort_values(TIME_COLUMN).set_index(TIME_COLUMN)
+
+    hourly = df[OI_COLUMN].resample("1h").agg(
+        lambda values: values.iloc[-1] if len(values) else pd.NA
+    )
+    return hourly.rename("oi").reset_index().rename(columns={TIME_COLUMN: "ts"})
+
+
+def add_pct_changes(hourly: pd.DataFrame) -> pd.DataFrame:
+    """Add 6h and 24h OI percentage changes without filling gaps."""
+    df = hourly.copy()
+    df["d6h_pct"] = df["oi"].pct_change(periods=6, fill_method=None)
+    df["d24h_pct"] = df["oi"].pct_change(periods=24, fill_method=None)
+    return df
+
+
+def _rolling_percentile_feature(
+    features: pd.DataFrame,
+    *,
+    value_col: str,
+    out_col: str,
+    warmup_col: str,
+) -> tuple[pd.DataFrame, pd.Timestamp | None]:
+    """Return no-lookahead rolling percentile for a single feature column."""
+    valid = features.loc[features[value_col].notna(), ["ts", value_col]].copy()
+    if valid.empty:
+        out = features.loc[:, ["ts"]].copy()
+        out[out_col] = pd.NA
+        out[warmup_col] = True
+        return out, None
+
+    rolled = add_rolling_percentiles(valid, time_col="ts", value_col=value_col)
+    pctls = rolled.loc[:, ["ts", "rolling_pctl", "warmup"]].rename(
+        columns={"rolling_pctl": out_col, "warmup": warmup_col}
+    )
+    out = features.loc[:, ["ts"]].merge(pctls, on="ts", how="left")
+
+    ready = pctls.loc[~pctls[warmup_col], "ts"]
+    first_ready = ready.min() if not ready.empty else None
+    if first_ready is None or pd.isna(first_ready):
+        out[warmup_col] = True
+    else:
+        missing_warmup = out[warmup_col].isna()
+        out.loc[missing_warmup, warmup_col] = out.loc[missing_warmup, "ts"] < first_ready
+        out[warmup_col] = out[warmup_col].astype(bool)
+
+    return out, first_ready
+
+
+def add_rolling_oi_percentiles(features: pd.DataFrame) -> pd.DataFrame:
+    """Add A-1 no-lookahead rolling percentiles for 6h and 24h OI changes."""
+    d6, d6_ready = _rolling_percentile_feature(
+        features,
+        value_col="d6h_pct",
+        out_col="d6h_rolling_pctl",
+        warmup_col="_d6h_warmup",
+    )
+    d24, d24_ready = _rolling_percentile_feature(
+        features,
+        value_col="d24h_pct",
+        out_col="d24h_rolling_pctl",
+        warmup_col="_d24h_warmup",
+    )
+
+    df = features.merge(d6, on="ts", how="left").merge(d24, on="ts", how="left")
+    df["warmup"] = df["_d6h_warmup"] | df["_d24h_warmup"]
+    if d6_ready is None or d24_ready is None:
+        df["warmup"] = True
+    return df.drop(columns=["_d6h_warmup", "_d24h_warmup"])
+
+
+def build_symbol_features(symbol: str, data_dir: Path = DATA_DIR) -> pd.DataFrame:
+    path = data_dir / f"{symbol}_METRICS_5M.csv"
+    metrics = read_metrics(path)
+    features = add_pct_changes(resample_oi_1h(metrics))
+    features = add_rolling_oi_percentiles(features)
+    out = features.loc[:, OUTPUT_COLUMNS].copy()
+    out["ts"] = pd.to_datetime(out["ts"], utc=True).dt.strftime("%Y-%m-%dT%H:%M:%SZ")
+    return out
+
+
+def summarize_features(symbol: str, features: pd.DataFrame) -> dict[str, object]:
+    ts = pd.to_datetime(features["ts"], utc=True)
+    expected_rows = int(((ts.max() - ts.min()) / pd.Timedelta(hours=1)) + 1)
+    gap_hours = int(features["oi"].isna().sum())
+    return {
+        "symbol": symbol,
+        "start_utc": ts.min().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "end_utc": ts.max().strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "rows": int(len(features)),
+        "expected_1h_grid_rows": expected_rows,
+        "warmup_rows": int(features["warmup"].sum()),
+        "oi_gap_hours": gap_hours,
+        "oi_gap_rate": float(gap_hours / len(features)) if len(features) else None,
+    }
+
+
+def main() -> None:
+    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
+    summaries: list[dict[str, object]] = []
+
+    for symbol in SYMBOLS:
+        features = build_symbol_features(symbol, DATA_DIR)
+        out_path = OUTPUT_DIR / f"a1_oi_features_{symbol}.csv"
+        features.to_csv(out_path, index=False)
+        summary = summarize_features(symbol, features)
+        summary["output"] = str(out_path.relative_to(ROOT))
+        summaries.append(summary)
+
+    payload = {
+        "generated_at_utc": pd.Timestamp.now(tz="UTC").strftime("%Y-%m-%dT%H:%M:%SZ"),
+        "symbols": list(SYMBOLS),
+        "outputs": summaries,
+    }
+    print(json.dumps(payload, indent=2))
+
+
+if __name__ == "__main__":
+    main()
diff --git a/06_RESEARCH/CODE/features/test_a1_oi_features.py b/06_RESEARCH/CODE/features/test_a1_oi_features.py
new file mode 100644
index 0000000000000000000000000000000000000000..838c4381520bdcea76348bc419f631df909d70dd
--- /dev/null
+++ b/06_RESEARCH/CODE/features/test_a1_oi_features.py
@@ -0,0 +1,68 @@
+from pathlib import Path
+import sys
+
+import pandas as pd
+
+
+sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
+
+from features.a1_oi_features import add_rolling_oi_percentiles, resample_oi_1h
+
+
+def test_rolling_percentile_excludes_current_and_future_values() -> None:
+    target_idx = 24 * 180
+    dates = pd.date_range("2021-01-01", periods=target_idx + 2, freq="h", tz="UTC")
+    base = pd.DataFrame(
+        {
+            "ts": dates,
+            "oi": 1.0,
+            "d6h_pct": 0.0,
+            "d24h_pct": 0.0,
+        }
+    )
+    base.loc[target_idx, ["d6h_pct", "d24h_pct"]] = 1.0
+    base.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = 2.0
+
+    changed_future = base.copy()
+    changed_future.loc[target_idx + 1, ["d6h_pct", "d24h_pct"]] = -1_000.0
+
+    featured = add_rolling_oi_percentiles(base)
+    perturbed = add_rolling_oi_percentiles(changed_future)
+
+    assert not featured.loc[target_idx, "warmup"]
+    assert featured.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert featured.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d6h_rolling_pctl"] == 1.0
+    assert perturbed.loc[target_idx, "d24h_rolling_pctl"] == 1.0
+    assert (
+        featured.loc[target_idx + 1, "d6h_rolling_pctl"]
+        != perturbed.loc[target_idx + 1, "d6h_rolling_pctl"]
+    )
+
+
+def test_resample_5m_to_1h_uses_last_value_without_forward_fill() -> None:
+    metrics = pd.DataFrame(
+        {
+            "create_time": pd.to_datetime(
+                [
+                    "2024-01-01T00:00:00Z",
+                    "2024-01-01T00:55:00Z",
+                    "2024-01-01T02:00:00Z",
+                    "2024-01-01T02:55:00Z",
+                ],
+                utc=True,
+            ),
+            "sum_open_interest": [10.0, 12.0, 20.0, 24.0],
+        }
+    )
+
+    hourly = resample_oi_1h(metrics)
+
+    assert hourly["ts"].dt.strftime("%Y-%m-%dT%H:%M:%SZ").tolist() == [
+        "2024-01-01T00:00:00Z",
+        "2024-01-01T01:00:00Z",
+        "2024-01-01T02:00:00Z",
+    ]
+    assert hourly.loc[0, "oi"] == 12.0
+    assert pd.isna(hourly.loc[1, "oi"])
+    assert hourly.loc[2, "oi"] == 24.0

tokens used
102,616
已完成 A-1 OI 特征层，未做事件、阈值、Holdout、收益，也未提交 git commit。

新增交付物：
- [a1_oi_features.py](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/features/a1_oi_features.py)
- [test_a1_oi_features.py](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/06_RESEARCH/CODE/features/test_a1_oi_features.py)
- [REPORT_A1_OI_FEATURES.md](/Users/yaomingyu/Documents/AI_QUANT_COMPANY/04_AI_TEAM/CODEX_TASKS/REPORT_A1_OI_FEATURES.md)
- `06_RESEARCH/CODE/output/a1_oi_features_{BTCUSDT,ETHUSDT,SOLUSDT}.csv`

验证结果：
- `python3 -m pytest 06_RESEARCH/CODE/features/test_a1_oi_features.py 06_RESEARCH/CODE/features/test_a2_features.py`
- 结果：`5 passed in 1.49s`

输出概况：
- BTCUSDT：50,497 行，warmup 4,344，OI 缺口 22 小时
- ETHUSDT：39,553 行，warmup 4,344，OI 缺口 10 小时
- SOLUSDT：39,553 行，warmup 4,344，OI 缺口 10 小时

注意：`git status` 里原本已有未跟踪的 `TASK_A1_OI_FEATURES.md` 和 `99_TEMP/codex_a1_features.log`，我没有改动它们。
